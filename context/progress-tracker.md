# Progress Tracker & Build Plan — School ERP

Update this file whenever the current unit, phase, or implementation state changes. Progress must reflect **actual** state, not intent.

## Current Phase

- **Milestone 9 — Harden & Launch: complete (Units 32–35 built). The v1 "Complete Core" build plan (Units 1–35) is fully implemented** — see Unit 35's honest launch-readiness checklist below before treating it as sales-ready.
- **Milestone 11 — implementing the remaining feature specs (Units 36–69) one at a time, in order.** Units 36–38 done (see below); continuing sequentially.

## Current Goal

- Implement Units 36–69 (drafted specs, see Milestone 10+ below) one at a time. Units 36–40, 42, 43 (Platform Configuration, Global Search, Fee Reconciliation & Receipt Corrections, DPDP Consent/Retention/Delete, Real Notification Providers, Staff HR Depth, Academic Structure Depth) are done and tested.
- **Unit 41 (Admissions Depth) deliberately skipped for now** — its own spec needs the school's real admission process (reg-fee timing, whether there's a test/interview, how offers are decided) before a rigid workflow can be built around it; user chose to describe this later rather than guess. Units 44 (Attendance Depth), 45 (Homework Depth), and 46 (Exams & Report Cards Depth) are now done too. Continuing to **47 — Timetable Depth** and other units that don't need business-process input; will circle back to 41 once that's answered.
- Remaining real gaps not yet closed by this pass: mobile app store listing, DLT-approved SMS/WhatsApp templates (real telecom account setup, not engineering), real provider accounts (MSG91/Gupshup/FCM/SES), messy-Excel import validation, full Hindi UI coverage — tracked as their own units below, not silently bundled in.

---

## The Complete Build Plan (v1 "Complete Core")

Build in this order. Each unit gets a spec in `feature-specs/NN-*.md` (authored when the unit starts). Scope line & rationale: `build-approach.md`. Feature detail: `feature-catalog.md`. Ship this whole list before selling; everything else is On-Demand (bottom).

### Milestone 0 — Foundation
- **01 · Web + Design System Foundation** — monorepo (pnpm+Turborepo) scaffold, `web-app` Next.js, Tailwind + shadcn/ui, design tokens (`ui-context.md`), i18n (Hindi/English), lucide, `cn()`. *(spec ready)*
- **02 · Database & Tenancy Foundation** — Postgres + Prisma, `Tenant`/`Branch` models, `tenant_id` convention, **RLS policies + `withTenant()` helper**, migration baseline, pooling (Accelerate/Neon).
- **03 · Auth & RBAC** — JWT access+refresh (rotation), parent **OTP**, staff password+**2FA** (argon2), roles/permissions model, guards.
- **04 · API Skeleton + Jobs Infra** — Express (always-on Node, `app.listen`), middleware pipeline (rate-limit → auth → tenant-context → RBAC → Zod → handler), OpenAPI, error shape; **Redis + BullMQ worker** + `jobs` interface (first job end-to-end); object storage (R2/S3).
- **05 · Super-Admin: Tenants & Plans** — create/suspend tenant, plan + **module/feature toggles**, `app_type` (shared/dedicated), basic usage metering.

### Milestone 1 — Academic Core
- **06 · Academic Structure** — sessions, classes, sections, subjects, teacher–subject–class mapping, class-teacher.
- **07 · Students + Bulk Import** — student CRUD, adm/roll no., ID basics; Excel import (template + validation + de-dupe via a background job).
- **08 · Parents/Guardians** — guardian accounts, student linking, multi-child, invite.
- **09 · Staff/HR + Leave** — staff records, roles, leave apply/approve.
- **10 · Admissions/Enquiry** — enquiry → application → convert to student; online admission form.

### Milestone 2 — Fees (deep · #1 paying module)
- **11 · Fee Setup** — heads, structures per class/category/session, installments, concessions (RTE/BPL/sibling/staff), fines.
- **12 · Fee Collection + Ledger** — counter collection (cash/cheque/UPI), instant receipt (PDF via background job), ledger, dues/defaulter reports, opening balances.
- **13 · Online Payment + Reconciliation** — Razorpay (UPI), webhooks, reconciliation, platform fee, refunds.
- **14 · Fee Reminders** — scheduled reminders (cron job) via SMS/WhatsApp/push; SMS wallet.

### Milestone 3 — Attendance
- **15 · Attendance (API + Web)** — daily marking, registers, monthly %/defaulter reports.
- **15b · Mobile App Shell (PULLED FORWARD from Unit 24)** — single role-based RN/Expo app: OTP login, **`schoolCode`→tenant resolver** (replaces the `tenantSlug` placeholder from Unit 03), role routing, dynamic theming (shared mode), i18n, offline base + sync engine. **Build this before Unit 16** — teacher offline attendance needs an app to live in. Prereqs to line up first: Google Play Developer account, EAS/Expo setup, Vidyut app icon (`brand.md`).
- **16 · Teacher Mobile Attendance + Parent Alerts** — offline marking + sync (on the 15b shell); auto absence alert to parents.

> **⚠ Plan fix (mobile sequencing):** the Mobile App Shell was originally Unit 24 but Unit 16 depends on it — so it's pulled forward to **15b** (above). Milestone 7's "24 · Mobile App Shell" is now **just the parent/student feature layers on top of the 15b shell**, not building the shell. Write the 15b mobile-shell spec when reaching Unit 15, and renumber if preferred.

### Milestone 4 — Exams & Report Cards (deep)
- **17 · Exam Setup & Grading** — terms, grading schemes (marks/%/grades/CCE), board configs.
- **18 · Marks Entry + Moderation** — grid entry (web + teacher app), >100% guard, review/lock.
- **19 · Report Cards** — configurable templates, bulk PDF (Puppeteer), publish to parents, marks alerts.

### Milestone 5 — Communication & Certificates
- **20 · Notifications & Announcements** — push/SMS/WhatsApp engine, announcements/circulars, events/calendar, delivery logs.
- **21 · Certificates & IDs** — TC/bonafide/character, ID cards, admit cards, issue register.

### Milestone 6 — Scheduling & Homework
- **22 · Timetable** — class + teacher timetable; app views.
- **23 · Homework/Assignments** — assign + parent/student view + calendar.

### Milestone 7 — Apps & Dashboards
- **24 · Mobile — Parent/Student layers** — (shell already built at **15b**) parent + student role experiences on top of the shared shell. *(Was "Mobile App Shell" — shell moved to 15b; see Milestone 3 note.)*
- **25 · Parent App** — fees + online pay, attendance, results/report cards, notices, homework, timetable.
- **26 · Teacher App** — attendance (offline), marks, homework; simple + fast.
- **27 · Web Admin Panel** — role-scoped surfacing of all core modules.
- **28 · Owner/Principal Dashboard** — collection %, dues, attendance, admissions KPIs.
- **29 · Public Site + Online Admission + Branded PWA** — `web-site` SSR, admission form, day-1 branded PWA fallback.

### Milestone 8 — White-Label & Billing
- **30 · Billing & Subscriptions** — plans, invoices, SMS wallet, usage metering, platform-fee revenue.
- **31 · White-Label App Pipeline** — `app.config.js` + `eas.json` per-tenant builds, EAS Build/Submit from super-admin, dedicated mode, **OTA updates**, version tracking. (See `architecture-context.md` Part 2.)

### Milestone 9 — Harden & Launch
- **32 · Offline Sync Hardening** — conflict rules, idempotent sync, delta sync, SMS fallback.
- **33 · Academic-Year Rollover** — promote students to new session, preserve history (churn-critical).
- **34 · Security & Isolation Pass** — tenant-isolation test suite, RBAC audit, backups + restore drill, data export (DPDP), audit logs.
- **35 · Launch Readiness** — seed/demo tenant, Sentry + logs, CI/CD, staging→prod, launch checklist (`build-approach.md` §5).

### On-Demand (AFTER launch, when a paying school needs it)
Transport+GPS · Library · Hostel · Payroll · Accounting · Inventory/procurement · Canteen · Online exams/question bank · LMS/live classes · Discussion/blog/placement · AI features · custom analytics. Build only per the trigger rule in `build-approach.md` §6.

### Milestone 10+ — Remaining Feature Specs (drafted, not implemented)

**Specs only — written 2026-07-27 in response to a full re-audit of `feature-catalog.md`, per an explicit user request covering everything remaining except Part G (AI), which is deliberately excluded from this batch.** These are ready to implement when picked up, in roughly priority order; several genuinely require a user decision before coding starts (flagged below) rather than an engineering default.

- **36 · Platform Configuration** — Settings, Branch mgmt, User & Role mgmt. Closes Unit 34's 3 confirmed core gaps. **✅ Implemented — see Milestone 11 below.**
- **37 · Global Search** — students/staff/invoices, `pg_trgm`-based. **✅ Implemented — see Milestone 11 below.**
- **38 · Fee Reconciliation & Receipt Corrections.** **✅ Implemented — see Milestone 11 below.**
- **39 · DPDP Consent, Retention & Delete** — **✅ Mechanism implemented — see Milestone 11 below. Still needs real legal sign-off on consent text/retention windows before shipping**, not just engineering (flagged in `context/dpdp-policy.md`).
- **40 · Real Notification Providers** — SMS/WhatsApp/Push/Email + inbox + DLT templates. **✅ Implemented — see Milestone 11 below. Real provider accounts (MSG91/Gupshup/FCM/SES) still don't exist**, so the real-call code paths are built and gated behind env vars but have never run against a live account — same posture as Unit 31.
- **41 · Admissions Depth** — reg fee at apply, test/interview scheduling, merit list, offer letters. **Needs the school's actual admission process described** before a rigid workflow is built around a guess.
- **42 · Staff HR Depth** — documents, staff attendance, staff ID cards. **✅ Implemented, incl. web UI — see Milestone 11 below.** Recruitment/appraisal explicitly deferred, no source process to model.
- **43 · Academic Structure Depth** — electives, house system. **✅ Implemented, incl. web UI — see Milestone 11 below.** Streams confirmed with the user: a separate `Class` per stream, zero new schema.
- **44 · Attendance Depth** — period-wise attendance, device-scan ingestion, analytics/chronic-absentee trend. **✅ Implemented, incl. web UI (analytics + device-token tabs) — see Milestone 11 below.** No real biometric vendor integrated (generic webhook-shaped endpoint only, per its own Open Question 1).
- **45 · Homework Depth** — digital submission, grading/feedback, calendar view. **✅ Implemented — see Milestone 11 below.** Plagiarism detection out of scope (needs a real corpus/tool).
- **46 · Exams & Report Cards Depth** — exam datesheet, co-scholastic grades, rank/toppers, transcripts, MCQ online exams + question bank. **✅ Implemented, incl. web UI — see Milestone 11 below.** Board-specific `ReportCardTemplate.layout` schema extension (that spec's own scope #7) deliberately **not** done — needs a real board report-card sample confirmed with the user first, same posture as Unit 41. Descriptive/subjective online exams and proctoring also out of scope (spec's own Open Question 1 / out-of-scope line).
- **44 · Attendance Depth** — period-wise, generic device-scan ingestion (vendor-agnostic), analytics/trends.
- **45 · Homework Depth** — digital submission, grading, calendar.
- **46 · Exams & Report Cards Depth** — exam datesheet, co-scholastic grades, rank, transcripts, MCQ-only online exams, question bank, board-specific layouts (**needs a real board report-card sample from the user** before locking a layout JSON schema).
- **47 · Timetable Depth** — substitution, room allocation. Auto-generation explicitly deferred (real constraint-solving effort, no validated demand). **✅ Implemented, incl. web UI — see Milestone 11 below.** Room kept as free text, not promoted to a `Room` model (Open Question 2, resolved).
- **48 · Fee Management Depth** — cheque/PDC tracking only; multi-currency explicitly skipped as pure speculation. **✅ Implemented, incl. web UI — see Milestone 11 below.**
- **49 · Messaging & Engagement** — circulars, PTM booking, unified calendar, complaints, surveys, gallery, SOS broadcast. **✅ Implemented, incl. web + mobile UI — see Milestone 11 below.** SOS deliberately skipped per user instruction, pending sign-off on the SMS-wallet-balance bypass policy. Parent-teacher chat confirmed in scope and built async/polling, not real-time.
- **50 · Certificates Depth** — token-based template builder, bulk ID cards w/ QR, DMS, e-sign (gated on a real e-sign provider account). **✅ Implemented, web UI only (explicit scope decision) — see Milestone 11 below.**
- **51 · Web Admin Panel: Remaining Modules** — UI-only for the 10 modules Unit 27 deferred; no backend ambiguity, purely mechanical. **✅ Implemented — see Milestone 11 below.** 2 of the original 10 items were already done by later units (exam/term setup — Unit 46; certificates — Unit 50) and not rebuilt.
- **52 · Mobile App Depth** — leave apply (teacher) + PTM booking (parent) closed the only two real gaps; messaging/calendar/online-exams were already built by Units 46/49. **✅ Implemented — see Milestone 11 below.** Study-material library resolved out of scope (smaller, spec-endorsed option).
- **53 · Owner Dashboard Depth** — enrollment trends, staff metrics. **✅ Implemented — see Milestone 11 below.**
- **54 · Public Site Depth** — notices, gallery, contact; a fixed-section CMS, not a general page builder. **✅ Implemented — see Milestone 11 below.**
- **55 · Analytics & Reporting** — 5 standard reports + CSV export + scheduled email + KPI summary. **✅ Implemented — see Milestone 11 below.** UDISE+/board-reporting exports **blocked on sourcing the real government format spec** (not built, not guessed). Predictive analytics excluded (Part G-adjacent).
- **56 · Super-Admin Console Depth** — global announcements, support tickets, monitoring summary. **✅ Implemented — see Milestone 11 below.** Reseller/partner mgmt **blocked on the user confirming a real commission model** (not built, not guessed).
- **57 · Transport Management** (On-Demand). **✅ Implemented at explicit user request** — see Milestone 11 below. Built despite `build-approach.md`'s "on-demand only when a paying school needs it" rule; no real school demand was confirmed, flagged for visibility.
- **58 · Library Management** (On-Demand). **✅ Implemented at explicit user request** — see Milestone 11 below. Same posture as Unit 57: no real school demand confirmed, flagged for visibility. ISBN auto-fetch (Open Question 1) not built — manual catalog entry only.
- **59 · Hostel/Dormitory** (On-Demand, lowest priority). **✅ Implemented at explicit user request** — see Milestone 11 below. Same posture as Units 57/58: no real school demand confirmed, flagged for visibility. Mess/meal management (Open Question 1) not built at all, not even the simplified text version — no confirmed demand.
- **60 · Front Office & Gate** (On-Demand). **✅ Implemented at explicit user request** — see Milestone 11 below. Same posture as Units 57–59: no real school demand confirmed, flagged for visibility.
- **61 · Health, Discipline & Others** (On-Demand). **✅ Implemented at explicit user request** — see Milestone 11 below. Merit/demerit scoring (Open Question 1) resolved as a plain log, no downstream house-points/report-card wiring.
- **62 · Accounting & Finance** (On-Demand). **✅ Implemented, export-first path — see Milestone 11 below.** The build-vs-buy call was put to the user directly (not defaulted); the user confirmed export-first over a native general ledger.
- **63 · Payroll & Salary** (On-Demand). **✅ Implemented, export-first path** — see Milestone 11 below. Same compliance-risk build-vs-buy call as Unit 62, put to the user directly; the user confirmed export-first over native PF/ESI/TDS/PT calculation.
- **64 · Inventory, Assets & Procurement** (On-Demand). **✅ Implemented, including the parent-facing store** — see Milestone 11 below. The user confirmed the store is in scope when asked directly.
- **65 · Misc Engagement & Productivity** — **✅ Implemented** — see Milestone 11 below. Only task/to-do + polls built, per the spec's own recommendation; discussion forums/blog/CMS/form builder/question-paper generator/video conferencing explicitly deferred as low-value/speculative even within this batch.

**Explicitly out of this batch:** Part G (AI & Intelligent Features) — per the user's own instruction to leave AI features for now.

**Re-audit correction (same day):** the user asked to verify Units 36–65 actually covered every remaining catalog row. A systematic row-by-row check found ~20 real gaps that had slipped through (A1 lifecycle rows, all of A10/LMS, a few A2/A3/C2/D6/E1/F/H items). Closed with four more specs:

- **66 · Student Lifecycle Depth** — sibling linking, cross-branch transfer, alumni, timeline, standalone re-admission (Unit 33's rollover only covers year-end REPEAT/WITHDRAW, not a later re-admit). **✅ Implemented — see Milestone 11 below.**
- **67 · Lesson Planning, Curriculum & LMS** — A10 in full; this module was correctly identified as On-Demand in the catalog but never actually got a spec written, unlike every other deferred module. **✅ Implemented — see Milestone 11 below.**
- **68 · Communication Extras** — alternate/WhatsApp contact fields, opt-in preferences (only meaningful once Unit 40 lands), newsletter, birthday automation (**default on/off needs the user's sign-off**, not an assumption). **✅ Implemented — see Milestone 11 below.**
- **69 · Platform Extras** — password policy (**needs a real confirmed policy, not a guess**), Google/Microsoft SSO, a security-audit pass, in-app onboarding tours, feedback capture, per-role (teacher/accountant) dashboard slices, and the white-label branding admin screen (Unit 31 made the mobile app parameterizable; this is the missing UI to actually set it). Webhooks/developer API explicitly stays unbuilt pending a real partner request, per `build-approach.md` §6. **✅ Implemented — see Milestone 11 below (this closes the batch).**

**One row remains deliberately unspecced**: Placement/career management (H-bis) — primarily a higher-ed feature, minor even in the catalog's own original assessment for K-12. Flagged here rather than silently dropped a second time.

---

## Completed

- **Unit 01 — Web + Design System Foundation.** pnpm+Turborepo monorepo (`apps/web-app`, `packages/config`, `packages/ui`); Next.js App Router + TS strict; Tailwind v4 wired to Vidyut tokens as CSS custom properties (`packages/config/tailwind-tokens.css`, imported by `web-app`, light theme only — no dark/neutral shadcn leakage); shadcn/ui (Button, Card, Dialog, Input, Label, Select, Table, Tabs, Sonner, Badge, Sheet) with a hand-authored semantic bridge from shadcn's expected variables to our tokens; `lib/utils.ts` `cn()`; lucide-react icons; i18next + react-i18next with `en`/`hi` namespaces, a working language switcher, and Devanagari rendering verified in-browser; app shell (sidebar/topbar/content) using tokens + components, non-functional (no auth/data yet, as scoped). Lint, typecheck, and build all pass; verified visually in both languages. External coding skills installed project-locally under `.claude/skills/` (`vercel-react-best-practices`, `react-native-best-practices`, plus the rest of each source repo) — see `context/coding-skills.md`; `frontend-design` is a Claude Code marketplace plugin that needs an interactive `claude` session to install (`/plugins install frontend-design`) and could not be installed from this non-interactive session.

- **Unit 02 — Database & Tenancy Foundation.** `packages/db` (chosen over `apps/api`, since the Express skeleton itself is Unit 04 — this package is what Unit 04 will import): Prisma 6.19.3, multi-file schema (`prisma/schema/schema.prisma` + `tenancy.prisma`) with `Tenant`, `Branch`, `AcademicSession` + `TenantStatus`/`AppDelivery`/`Board` enums, cuid ids, `createdAt`/`updatedAt`/`deletedAt` on tenant-owned tables, per `data-model.md` §2. `Tenant` itself carries no RLS (platform-level, accessed by super-admin later); `Branch`/`AcademicSession` have RLS **enabled and FORCEd**, policy `USING/WITH CHECK ("tenantId" = current_setting('app.tenant_id', true))`, in a hand-written raw-SQL migration (`prisma/schema/migrations/20260725202700_enable_rls`) since Prisma doesn't manage RLS. `withTenant(tenantId, fn)` (`packages/db/src/with-tenant.ts`) opens a transaction and sets the session var via `set_config(..., true)` (`SET LOCAL` semantics, safe under pooling) — all tenant DB access must go through it. Local Postgres via `docker-compose.yml` (Postgres 16); idempotent seed script (demo tenant/branch/2026-27 session), itself routed through `withTenant()`. Vitest tenant-isolation suite (`packages/db/test/tenant-isolation.test.ts`, 3 tests, all passing): `withTenant` scopes rows correctly, a cross-tenant `WITH CHECK` write is rejected, and a deliberately unscoped query (no `withTenant`) sees zero rows. Typecheck clean; no lint script in this package (backend-only, no ESLint config yet — will be added when `apps/api`/Unit 04 introduces the shared backend lint setup).
  - **Real bug caught and fixed during this unit:** the default Docker Postgres bootstrap user (`POSTGRES_USER`) is a true Postgres superuser, which unconditionally bypasses RLS — including `FORCE ROW LEVEL SECURITY` — so the seed script initially succeeded even though it wasn't using `withTenant()`. Fixed by adding a non-superuser `vidyut_app` role (`docker/postgres-init/01-app-role.sql`, `02-create-databases.sql`) that owns both databases and is used for all migrations/app/test connections — this mirrors how a real RDS master user behaves (not a true superuser, does respect `FORCE RLS`) and is what makes the isolation tests meaningful rather than a false green.

- **Unit 03 — Auth & RBAC.** New packages: `packages/types` (permission catalog transcribed from `rbac.md`'s matrix, `RoleKey`/`UserStatus`, `DEFAULT_ROLE_PERMISSIONS`, JWT claim shapes) and `packages/validation` (Zod schemas for all 6 auth endpoints, shared front+back). `packages/db` gained `identity.prisma`: `User`, `Role`, `RolePermission`, `UserRole` (branch-scoped), `BranchMembership`, plus a `RefreshToken` model not itemized in `data-model.md` §3 but required unambiguously by `api-conventions.md`'s "refresh token stored hashed, revocable" — added and documented here rather than as a blocking Open Question, since it's a direct implication of an already-explicit requirement. `RolePermission`/`UserRole`/`BranchMembership` also gained a denormalized `tenantId` column beyond their §3 shorthand, because data-model.md §1's "every tenant-owned table carries tenantId" is the actual RLS requirement — RLS needs the column on the row itself, not just reachable transitively through a join. All 5 new tables got the same ENABLE+FORCE+policy treatment as Unit 02, in a follow-up migration (`20260725204018_enable_rls_identity`).
  - **`apps/api` created in this unit** (not Unit 04): `feature-specs/03-auth-rbac.md` explicitly lists 6 HTTP endpoints and says "the middleware built here plugs into it [Unit 04]" — meaning some of Unit 04's target existed by the time it starts. Built the minimum to make that true: `src/app.ts` exports `createApp()` (Express instance, JSON body parsing, the `/api/v1/auth` router, the error-envelope middleware) but deliberately has **no `app.listen()`** and no global rate-limit/OpenAPI/X-Request-Id — those stay Unit 04's job. Tests exercise `createApp()` directly via supertest, no running server needed.
  - **Guards** (`src/core/guards/`): `authGuard` (verifies the access JWT, attaches `req.auth`), `tenantContext` (asserts tenant presence — deliberately thin until Unit 04/05 add `TENANT_SUSPENDED`/`MODULE_DISABLED` checks, which need Tenant/ModuleToggle lookups those units introduce), `requireBranch` (OWNER/SUPERADMIN span every branch; others need a matching `BranchMembership`), `requirePermission` (checks the DB fresh on every request — permissions are never cached in the JWT, so an owner editing a role's grid takes effect immediately, not after the access token's ~15 min expiry), `requireSelf` (PARENT/STUDENT bypass the grid per `rbac.md` rule 5; only covers direct self-userId for now — richer child-linking self-scope needs Guardian/StudentGuardian, which don't exist until Unit 08).
  - **Auth core** (`src/core/auth/`): argon2id password hashing; JWT via `jose` (access ~15 min, refresh ~30 days) with claims per `api-conventions.md`; refresh tokens stored as a SHA-256 hash (not argon2 — the token is already high-entropy random, so a fast hash is the correct choice, argon2 is for low-entropy secrets like passwords); rotation revokes the old row and links `replacedById` in one `withTenant` transaction; OTP and staff 2FA both reuse the same Redis-backed 6-digit-code mechanism (`SET`+TTL, `INCR`+`EXPIRE` throttling — 3 requests / 10 min); SMS delivery is a `console.log` stub behind a `sendOtpSms`/`sendTwoFaCode` seam, exactly where Unit 04's BullMQ job enqueue will go once MSG91/Gupshup is chosen.
  - **Real gap found and resolved while implementing:** `api-conventions.md`'s `POST /auth/otp/request { phone }` doesn't say how the tenant is resolved, but phone is only unique per `(tenantId, phone)` — the same number can legitimately exist under two different schools — and `User` is RLS-scoped, so an unscoped lookup returns nothing by design. Added a required `tenantSlug` field to the OTP and staff-login request schemas (`packages/validation/src/auth.ts`) as an **internal placeholder, not the permanent shape** — founder decision: a distinct, short, human-friendly `schoolCode` (separate from the URL `slug`) will resolve to a tenant for the mobile shared-app login (architecture-context.md Part 2 §3A), replacing `tenantSlug` in these endpoints when that lookup exists. Tracked below.
  - **Enumeration masking (founder decision — added after initial implementation):** the first pass returned a distinct `404 NOT_FOUND` for an unknown tenant slug or unregistered phone/email, which would let a caller enumerate valid school slugs and registered contacts. `requestOtp`/`verifyOtp`/`staffLogin` (`apps/api/src/modules/auth/service.ts`) now fold "no such tenant," "no such user," and "wrong password/code" into the same generic failure in each endpoint (`otp/request` always returns `{ phone }` with no `devCode` unless a real user existed to notify; `otp/verify` and `login` always fail with the same `UNAUTHENTICATED` code+message regardless of which part of the lookup failed). Covered by 2 new tests.
  - Redis added to `docker-compose.yml` (`redis:7-alpine`) for OTP/2FA/throttling state.
  - Role seeding extended in `packages/db/prisma/seed.ts`: all 7 tenant-assignable `RoleKey`s seeded as system roles for the demo tenant, `RolePermission` rows populated from `DEFAULT_ROLE_PERMISSIONS` for the 5 staff roles, plus a demo OWNER login (`owner@demo-school.test` / `Owner@12345`) and a demo PARENT (`+919999999999`, OTP-only) — idempotent, re-run safely.
  - Tests: `apps/api/test/auth-flows.test.ts` (9, supertest against `createApp()` — OTP request/verify incl. wrong-code rejection and enumeration masking, staff login with and without 2FA incl. wrong-password rejection and enumeration masking, refresh rotation + old-token-reuse rejection, logout revocation), `rbac.test.ts` (3 — allowed role passes, disallowed role gets `403 FORBIDDEN`, no token gets `401 UNAUTHENTICATED`), `branch-scope.test.ts` (3 — PRINCIPAL on Branch A denied Branch B, allowed on their own branch, OWNER spans both). All 15 pass. Extended `packages/db/test/tenant-isolation.test.ts` with a 4th case covering the new `User` table (now 4 tests, all pass). Root `pnpm typecheck`/`pnpm build` clean; no lint script yet for `apps/api`/`packages/types`/`packages/validation` (same backend-lint gap noted in Unit 02, still deferred to Unit 04).
  - **Unit 04 scope confirmed with founder:** Unit 04 adds `app.listen()`/the server entrypoint, the full rate-limit→auth→tenant→RBAC→Zod pipeline formalized, OpenAPI docs, and `X-Request-Id` on top of what's here — nothing from Unit 03 gets undone or rebuilt.

- **Unit 04 — API Skeleton + Jobs Infra.** Formalized the pipeline Unit 03 left partial. `apps/api/src/app.ts` now assembles: `X-Request-Id` (generated if absent, on every response) → `helmet`+CORS allow-list (`CORS_ALLOW_ORIGINS` env, empty = allow-all for local dev) → body parsing → unauthenticated/unlimited `/health` + `/ready` + `/api/v1/docs` → a Redis-backed rate limiter (`express-rate-limit` + `rate-limit-redis`, 300 req/min default, `429 RATE_LIMITED` + `Retry-After`) → the auth/jobs/sample routers → the error-envelope middleware. `src/server.ts` adds the actual `app.listen()` entrypoint (still separate from `app.ts`/`createApp()`, so tests never need a running server).
  - **Response helpers** (`core/envelope.ts`): `ok`/`created`/`list`/`noContent`, per `api-conventions.md` — `list` computes `totalPages`. Unit 03's `sendData` renamed to `ok` throughout.
  - **OpenAPI**: `@asteasolutions/zod-to-openapi` registry built directly from `packages/validation`'s existing Zod schemas (no hand-duplicated shapes), served at `/api/v1/docs` (Swagger UI) and `/api/v1/openapi.json`. `pnpm --filter @vidyut/api openapi:export` writes the document to `packages/api-client/openapi.json`.
  - **`packages/api-client`** (new): `openapi-typescript` generates `src/schema.ts` from that JSON; a 6-line `createApiClient(baseUrl)` wraps `openapi-fetch` for a fully-typed client. Both the generated JSON and the generated TS are committed (not gitignored) so a fresh clone typechecks without running codegen first.
  - **Jobs infra**: `QUEUE_NAME`/`JOB_NAMES`/payload types live in `packages/types` (shared, so apps/api and apps/worker never import each other). `apps/api/src/core/jobs.ts` is the producer-side `enqueue(name, payload, opts)` / `getJobStatus(id)` interface — callers never touch the BullMQ `Queue` directly, per `code-standards.md`. **`apps/worker`** (new app) is the consumer: a `Worker` dispatching by job name to a processor map, currently just `demo.ping` (echoes its payload — proves the round trip, not a real feature). `POST /api/v1/jobs/demo` returns `202 { jobId }`; `GET /api/v1/jobs/:id` returns `{ id, name, state, returnValue, failedReason }` (`404` if unknown).
  - **Object storage**: `core/storage.ts` wraps `@aws-sdk/client-s3` + the presigner for `getUploadUrl`/`getDownloadUrl` — signed-URL-only, the API process never streams object bodies (invariant #4). MinIO added to `docker-compose.yml` for local dev, with a one-shot `minio-init` container (`minio/mc`) creating and locking down the `vidyut-dev` bucket, since the `minio/minio` image doesn't support bucket auto-creation via env vars.
  - **Sample protected route** (`GET /api/v1/sample/protected`, `modules/sample/`): exists solely to exercise the *entire* pipeline end to end for the DoD (`authGuard` → `tenantContext` → `requireBranch` → `requirePermission("student.view")` → `validateQuery`) — not a domain feature; domain modules start at Unit 06. Its Zod schema deliberately stays local to `apps/api` rather than `packages/validation`, since no real client will ever call it.
  - Tests (all in `apps/api/test/`, 25 total, all passing): `pipeline.test.ts` (5 — 200/400/401/403 through the sample route, 429+`Retry-After` via a dedicated low-limit test instance so the suite doesn't need 300 real requests), `health.test.ts` (2), `jobs.test.ts` (2 — real enqueue→process→status round trip using `apps/worker`'s actual `startWorker()`, not a mock), `storage.test.ts` (1 — genuine signed-URL PUT+GET round trip against local MinIO, not mocked), plus the 15 carried over from Unit 03. `packages/db`'s 4 tenant-isolation tests still pass unchanged. Root `pnpm typecheck`/`pnpm build` clean.
  - **Known-flaky fixed during this unit:** the job round-trip test initially failed intermittently — BullMQ can report a job's `state` as `"completed"` a tick before `returnvalue` is readable back from Redis. Fixed by waiting for both in the poll condition; confirmed stable across 5 repeated runs after the fix.
  - **Dockerfiles added** (`apps/api/Dockerfile`, `apps/worker/Dockerfile`, both root-context multi-stage-free builds: `pnpm install` + `prisma generate` + `pnpm start`, with a BuildKit cache mount for the pnpm store) and `docker-compose.yml` extended with `api`+`worker` services. **Could not verify a full `docker-compose up --build` in this sandbox**: two attempts each resolved the lockfile (989 entries) and fetched 845+/859 packages before pnpm's own supply-chain-policy check timed out — package downloads inside Docker's network namespace ran at a small fraction of host speed in this environment (confirmed: the same install completes in seconds on the host). This reads as a sandbox network constraint, not a defect in the Dockerfiles or lockfile; recommend the user run `docker compose up --build` themselves to confirm on a normal network before relying on it for onboarding/CI.
  - **Real pnpm CLI quirk found, unrelated to this unit's code:** in this pnpm version (11.17.0), plain `pnpm lint` / `pnpm run lint` at the repo root intermittently fails with `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL: Command "eslint" not found`, while `pnpm --filter vidyut run lint` and `pnpm exec turbo run lint` reliably succeed (same root `"lint": "turbo run lint"` script, just invoked more explicitly). Only `lint` is affected — `typecheck`/`build` were unaffected throughout. Worth knowing if `pnpm lint` misbehaves in a real terminal; use one of the two reliable forms.

- **Unit 05 — Super-Admin: Tenants & Plans.** `packages/db` gained `platform.prisma`: `Plan` (global catalog, no `tenantId`, no RLS), `Subscription`/`ModuleToggle`/`SmsWallet`/`AppBuild` (carry `tenantId` for filtering but **no RLS** — `data-model.md` §13 labels this whole section "NOT tenant-scoped" even though most rows have the column; they're platform-managed config consulted by internal guards and the super-admin surface, not a tenant-facing CRUD resource), and a new `PlatformUser` model (founder decision — see below) + `AuditLog` (§12, RLS'd like every other domain table, in a follow-up migration).
  - **Founder decision — SUPERADMIN identity:** the existing `User` table requires `tenantId` and is RLS-scoped, but the spec requires a super-admin auth context that's "separate from tenant JWT" and "never uses withTenant." Neither `data-model.md` §13 nor `rbac.md` says where these identities live — a real gap, flagged and resolved before coding: a dedicated `PlatformUser` table (own JWT type `platform_access`, own secret `JWT_PLATFORM_SECRET`, own guard `platformAuthGuard`), keeping the tenant-scoping invariant on `User` untouched rather than special-casing a nullable `tenantId`.
  - **Founder decision — Enterprise pricing:** `plans-entitlements.md` gives concrete launch prices for Starter/Standard/Pro but Enterprise is explicitly "custom." Seeded a clearly-placeholder ₹1,00,000/yr + ₹20,000 setup fee, never charged as-is — real Enterprise deals are quoted manually. Confirm all four again before Unit 30 (billing), per the doc's own instruction.
  - **Shared seeding helpers moved to `packages/db`** (`seedDefaultRoles`, `seedModuleToggles`) so the demo-tenant seed script and real tenant provisioning (the platform service) can never drift apart — same code seeds roles/permissions and module toggles either way. `seedModuleToggles` always resets every `ModuleKey` to the plan's defaults (called at creation and on plan change — "re-seeds," not merges); a manual per-tenant override survives any *other*, unrelated action, verified by a dedicated test.
  - **Platform module** (`apps/api/src/modules/platform/`): `POST /platform/auth/login`, `POST /platform/tenants` (creates tenant + branch + current academic session + owner user + roles + module toggles + `SmsWallet`; `appType` comes from the plan, never client-supplied; `DEDICATED` plans also get an `AppBuild` row + an `appbuild.stub` job — real EAS pipeline is Unit 31), `GET/PATCH /platform/tenants/:id`, `GET /platform/tenants`, `GET /platform/tenants/:id/usage`, `POST /platform/tenants/:id/impersonate` (access-token-only, no refresh — impersonation is time-boxed by design, audited via `AuditLog`).
  - **`tenantContext` guard extended** (Unit 03/04 left it thin, exactly as flagged): now looks up the `Tenant` row and returns `403 TENANT_SUSPENDED` for `SUSPENDED`/`CANCELLED` tenants — the lookup this needed didn't exist until this unit.
  - **Entitlement hooks** (`core/entitlements.ts`): `isModuleEnabled`/`requireModuleEnabled` and `assertWithinLimit` (404 `MODULE_DISABLED`/`LIMIT_EXCEEDED` — reads the tenant's current plan via `Tenant.planId`, `null` limit = unlimited/no plan, never enforced). Both are plain `prisma` calls (no `withTenant`), consistent with the no-RLS decision above.
  - **Usage endpoint is honest about what it can't measure yet:** `students` and `storageGb` always report `used: 0` with a comment, since neither the `Student` model (Unit 07) nor a `Document`/storage-accounting model exist yet — `users` and `branches` are real counts.
  - **Super-admin web** (`apps/web-app/app/super-admin/`): login, tenant list, create-tenant form, tenant detail (plan/status/app-type/usage, suspend↔activate) — all client components against a new `lib/platform-client.ts` fetch wrapper, no generated `@vidyut/api-client` usage yet (kept simple given "minimal" scope). **Added `@tanstack/react-query`** (root `QueryProvider` in the layout) — the project's own `code-standards.md` already specifies TanStack Query for web data-fetching, and a new, strict `react-hooks/set-state-in-effect` lint rule confirmed that plain `useEffect`+`setState` for data fetching is exactly the anti-pattern it's designed to catch; the one legitimate `useEffect`+`setState` left (the client-only auth-gate reveal in `super-admin/layout.tsx`, which isn't data-fetching) is lint-suppressed with an inline justification rather than restructured.
  - **Full flow verified in-browser, not just tests:** logged in as the seeded demo super-admin, created a real tenant end-to-end through the UI (correct plan/limits/usage shown), and suspended it live — status flipped and the button relabeled correctly. Cleaned up the test tenant afterward.
  - Tests: `apps/api/test/platform.test.ts` (9, all passing) — login, unauthenticated rejection, full provisioning (owner can actually log in afterward, proving roles/permissions were seeded correctly), dedicated-plan `AppBuild`+job, duplicate-slug `409`, suspension blocking tenant API access, plan-change re-seed + override persistence, usage accuracy, direct `assertWithinLimit` unit test. `apps/api` suite now 34 tests total, `packages/db` still 4 — all pass together. Root typecheck/lint/build clean.

- **Unit 06 — Academic Structure.** `packages/db` gained `academic.prisma`: `Class`/`Section`/`Subject`/`ClassSubject`/`TeacherAssignment` + `SubjectType` enum (`data-model.md` §4), all branch-scoped and RLS'd in a follow-up migration (`20260726051500_enable_rls_academic`), same pattern as every prior tenant-owned table.
  - **Staff-dependency deferral (per this unit's own spec Open Question):** `Section.classTeacherId` and `TeacherAssignment.staffId` ship now as plain columns with no FK — `Staff` doesn't exist until Unit 09, which adds the relation *and* the write endpoints (`PATCH /academic/sections/:id`, `POST/GET/DELETE /academic/teacher-assignments`). `TeacherAssignment` itself is schema-only this unit — no route touches it.
  - **Real tenant-facing session CRUD** (`POST/GET/PATCH /api/v1/academic/sessions`) — Unit 02 only ever created a session internally during provisioning. Setting `isCurrent: true` unsets the branch's previous current session in the same `withTenant()` transaction (`academicSession.updateMany` + `create`/`update` inside one `$transaction`), verified atomic by a dedicated test.
  - **Classes/Sections/Subjects/ClassSubject CRUD** (`modules/academic/`): soft-deleted (`deletedAt`) except the `ClassSubject` join table (hard delete). Sections and class-subject links nest under `:classId` and have no `branchId` in the URL — branch access is checked in the *service* layer (`assertBranchAccess`, calling a small helper extracted from the `requireBranch` middleware, `branchAccessAllowed`) after fetching the parent `Class`, not via route middleware, since the branch is only knowable once the class is loaded.
  - **RBAC:** `session.manage`/`class.manage`/`subject.manage` gate all mutations (already existed in `rbac.md`/`packages/types` from Unit 01 — no new permission strings needed); reads have no permission check (broad, any authenticated tenant user), matching the spec's "reads are broad" call.
  - Tests (`apps/api/test/academic.test.ts`, 6, all passing, via the real `createApp()` + full route pipeline): OWNER CRUD + TEACHER read-only enforcement, cross-branch PRINCIPAL denial on nested section creation (proves the fetch-then-check branch scope works), class-subject assignment round trip, cross-tenant isolation (both RLS-scoped and a deliberately unscoped `prisma` query), current-session flip atomicity, TEACHER denied session management. `apps/api` suite now 46 tests total, all passing together with the 40 carried over from Units 01–05. Root `pnpm typecheck` clean across all 9 packages; `pnpm test` (apps/api) green.
  - **Not done this unit (explicitly out of scope, per spec):** OpenAPI registry (`core/openapi.ts`) wasn't extended for the new endpoints — not part of this unit's Definition of Done, and the existing registrations are all hand-added per-route; left for whoever next touches API docs coverage as a whole, not invented here.

- **Unit 07 — Students + Bulk Import.** `packages/db` gained `students.prisma`: `Student`/`Enrollment` + `StudentStatus`/`EnrollmentStatus` enums (`data-model.md` §5), branch-scoped and RLS'd (`20260726053500_enable_rls_students`). `Enrollment` preserves full history across sessions (`@@unique([studentId, sessionId])`, never overwritten) so Unit 33's rollover has a trail to work from.
  - **Three decisions confirmed with the user before coding (per this session's "ask, don't assume" instruction), all folded into the spec's Decisions section:** (1) `admissionNo` — simple sequential, zero-padded per branch, no configurable prefix yet (`packages/db/src/generate-admission-no.ts`, a `ponytail:`-flagged count-then-pad approach — two concurrent creates in the same branch can race on the unique constraint; noted as a known ceiling, not silently hidden). (2) Bulk import accepts **both `.xlsx` and `.csv`** (feature-catalog's actual stated scope, not the narrower "Excel only" the draft spec had), via one library (`xlsx`/SheetJS) that reads both formats through the same API. (3) `POST /students` and the import job **reject loudly** (400/job failure) if the target branch has no `AcademicSession` with `isCurrent = true` — no silent fallback.
  - **A fourth decision surfaced and confirmed mid-implementation:** the spec's "same Zod schema as single-create" implied import rows carry raw `classId`/`sectionId`, which staff filling in a spreadsheet can't know. Resolved: import rows reference class/section **by name** (`className`/`sectionName`), resolved to IDs by the worker per row (unmatched name = a row-level error); single-create keeps taking real IDs, since a UI form supplies those from a dropdown.
  - **`modules/students/`** (`apps/api`): `POST/GET/PATCH/DELETE /api/v1/students` (+ `GET /:id`) — student CRUD creates its initial `Enrollment` atomically in the same `withTenant()` transaction; soft-deleted except through `student.delete` (OWNER only, per `rbac.md`). List supports `classId`/`sectionId`/`status`/`search` filters (class/section filter via `enrollments: { some: {...} }`, since class/section live on `Enrollment`, not `Student` itself).
  - **Bulk import plumbing:** `POST /students/import/upload-url` (signed PUT URL, reusing Unit 04's `core/storage.ts`) → client uploads directly to object storage → `POST /students/import { fileKey }` enqueues `students.import` and returns `202 { jobId }`. **`apps/worker`** gained `processors/students-import.ts`: parses the sheet via `xlsx` (`cellDates: true`), resolves class/section by name, generates `admissionNo` per row if omitted, and creates Student+Enrollment **per row in its own `withTenant()` transaction** (so one bad row can't abort the rest of the file) — returns a `{ total, succeeded, failed, rows[] }` summary readable via the existing `GET /api/v1/jobs/:id`. A new `getCurrentSessionId` helper (`packages/db`) is shared between the API's single-create path and the worker's import path so "no current session" is checked identically in both places.
  - **New dependency:** `xlsx` (SheetJS, MIT) added to `apps/worker` (parsing) and as an `apps/api` devDependency (test-only, to build a real `.xlsx` fixture) — no existing dependency covered Excel/CSV parsing, and it was the smallest single library covering both formats.
  - Tests (`apps/api/test/students.test.ts`, 5, all passing, via the real `createApp()` + `startWorker()` — no mocks): CRUD round trip incl. the `"0001"` sequential admissionNo, current-session rejection (`400 VALIDATION_ERROR`), TEACHER denied mutation + cross-branch read denial, cross-tenant isolation (RLS-scoped + unscoped-query), and a genuine bulk-import round trip — builds a real 3-row `.xlsx` buffer (1 valid, 1 unknown-class, 1 missing-required-field), PUTs it to local MinIO via a real signed URL, enqueues, and polls `GET /jobs/:id` until the worker reports `succeeded: 1, failed: 2`. `apps/api` suite now 45 tests total, all passing together with the 40 carried over from Units 01–06. Root `pnpm typecheck` clean across all 9 packages.

- **Unit 08 — Parents/Guardians.** `packages/db` gained `guardians.prisma`: `Guardian`/`StudentGuardian` + `GuardianRelation` enum (`data-model.md` §5), tenant-scoped and RLS'd (`20260726055500_enable_rls_guardians`). `Guardian` deliberately carries no `branchId` (a guardian's children can span branches within a tenant group) — branch-scoping happens transitively through the linked `Student` in the service layer, same `branchAccessAllowed` helper Unit 06/07 already use.
  - **No blocking Open Questions this unit** (unlike 06/07/10) — the spec's endpoint shapes, models, and RBAC were already concrete. One implementation call made without asking, consistent with established precedent (Unit 07's worker/storage.ts duplication): the invite's SMS "send" step runs in `apps/worker` via a small duplicated stub (`processors/guardian-invite.ts`), not a cross-app import — OTP generation/storage (needs Redis + Unit 03's throttle logic) still happens synchronously in `apps/api`; only the notification send is deferred to the `guardian.invite` job, per invariant #2.
  - **`modules/guardians/`** (`apps/api`): `POST/GET/PATCH /api/v1/guardians` (no `DELETE` — the spec's own endpoint list never lists one, so none was invented), `POST /api/v1/students/:id/guardians` + `DELETE /api/v1/students/:id/guardians/:guardianId` (mounted as a second router at the `/api/v1/students` prefix, since linking is about the `Guardian` module even though the URL lives under `/students`), `POST /api/v1/guardians/:id/invite` (idempotent — reuses the existing `User` if the phone already resolves to one, in case an invite is re-sent).
  - **Self-scope resolver** (`core/guards/require-self.ts`, alongside Unit 03's `requireSelf`): `resolveGuardianStudentIds(tenantId, userId)` walks `Guardian.userId -> StudentGuardian` — the piece Unit 03 explicitly deferred. `GET /guardians/me/children` is its first real consumer, registered *before* `/:id` in the router (otherwise Express would swallow `/me/children` as `GET /:id`), and returns each child's current-session class/section (resolved via `Enrollment.session.isCurrent`, joined per student — `null` if no current enrollment).
  - Tests (`apps/api/test/guardians.test.ts`, 5, all passing, via the real `createApp()` + full pipeline): guardian CRUD + RBAC (`guardian.manage` gated, TEACHER denied), link/unlink incl. cross-branch link denial for a PRINCIPAL scoped to a different branch, cross-tenant isolation (RLS-scoped + unscoped-query), invite→real-OTP-verify round trip (not mocked — actually calls `/auth/otp/verify` with the invite's `devCode`), and `/guardians/me/children` returning exactly one parent's linked child while a second parent's stays invisible (plus an empty result for an unlinked parent token). `apps/api` suite now 50 tests total, all passing together with the 45 carried over from Units 01–07. Root `pnpm typecheck` clean across all 9 packages.

- **Unit 09 — Staff/HR + Leave.** `packages/db` gained `staff.prisma`: `Staff`/`LeaveRequest` + `StaffType`/`LeaveStatus` enums (`data-model.md` §7), branch-scoped and RLS'd (`20260726062900_enable_rls_staff`). Also closes Unit 06's deferred loop: `Section.classTeacherId` and `TeacherAssignment.staffId` now carry real FK relations to `Staff` (added via a schema-only migration — `ON DELETE SET NULL` for the class-teacher link, `RESTRICT` for the assignment link).
  - **One decision confirmed with the user before coding:** `LeaveRequest.type` had no enum in `data-model.md` (unlike `StaffType`/`LeaveStatus`, which are explicit) — confirmed as a fixed `LeaveType{CASUAL, SICK, EARNED, OTHER}` baseline rather than free text; a configurable per-tenant leave-type catalog is deferred to a later HR/settings unit.
  - **One spec/implementation mismatch resolved without asking (documented in the spec's Decisions):** the spec's own scope line writes class-teacher assignment as a flat `PATCH /academic/sections/:id`, but Unit 06 already built `Section` under the nested `/academic/classes/:classId/sections/:id` route — extended that existing endpoint's schema with `classTeacherId` rather than adding a redundant flat route.
  - **`modules/staff/`**: `POST /api/v1/staff` creates `User`(email+password)+`Staff`+`UserRole`+`BranchMembership` atomically, gated by `staff.manage` (OWNER/PRINCIPAL only — ADMIN explicitly denied, matching `rbac.md`, narrower than most mutations in this codebase). `GET/PATCH /api/v1/staff` — reads broad (any authenticated user, no `staff.view` string invented), mutations `staff.manage`-gated. No `DELETE` — the spec's own endpoint list never listed one.
  - **`modules/academic/` extended**: `POST/GET/DELETE /academic/teacher-assignments` (staffId/subjectId/sectionId cross-checked to share the same branch before insert) — `class.manage`-gated, reusing the existing permission rather than inventing `teacherassignment.manage` (`rbac.md` has none).
  - **`modules/leave/`**: `POST /leave-requests` takes `staffId` in the body and rejects (`403`) if it doesn't resolve to the caller's own `Staff` row via `userId` — "no applying on a colleague's behalf." `PATCH /leave-requests/:id` (approve/reject) gated by `leave.approve` + branch-scope (`branchAccessAllowed` against the request's `branchId`).
  - Tests (`apps/api/test/staff.test.ts`, 5, all passing, via the real `createApp()` + full pipeline): staff CRUD with a genuinely working linked login, `staff.manage` restricted to OWNER/PRINCIPAL (ADMIN denied), cross-tenant isolation (RLS-scoped + unscoped-query), class-teacher assignment + teacher-subject-section mapping now functional end-to-end, leave apply with ownership enforcement (spoofing a colleague's `staffId` denied), `leave.approve` denied to TEACHER, and cross-branch approval denial for a PRINCIPAL scoped to a different branch. `apps/api` suite now 55 tests total, all passing together with the 50 carried over from Units 01–08. Root `pnpm typecheck` clean across all 9 packages.

- **Unit 10 — Admissions/Enquiry.** `packages/db` gained `admissions.prisma`: `Enquiry`/`Application` + `EnquiryStage`/`ApplicationStatus` enums (`data-model.md` §6), branch-scoped and RLS'd (`20260726064000_enable_rls_admissions`). **Completes Milestone 1 (Academic Core).**
  - **Two rounds of user confirmation before coding this unit** (per this session's "ask, don't assume" instruction): (1) scope stays **backend + tests only** — no `apps/web-app` UI screens, matching Units 06–09 (none of those domain modules have a UI yet either; a front-desk admin UI is deferred to a dedicated later pass across all of them, not built piecemeal here) — the spec's DoD line "through the staff admin UI" is read as "through the API." (2) The three Open Questions drafted with the spec itself (public self-apply form deferred to Unit 29 since `apps/web-site` doesn't exist yet; a nullable `Application.studentId` added beyond `data-model.md`'s sketch, set on conversion; `Application.regFeeInvoiceId` omitted until Unit 12 introduces `Invoice`) — all confirmed as originally recommended.
  - **One small implementation gap resolved without a third round of questions:** `data-model.md`'s `Application` has no `sectionId` (only `classAppliedId`), but converting to a `Student` needs one (Unit 07's `createStudent` requires it). Resolved the same way as the `studentId` question already answered: the front desk supplies `sectionId` in the `POST /applications/:id/convert` request body itself (not stored on `Application`), the same way a plain student create takes it directly.
  - **`modules/admissions/`**: `POST/GET/PATCH /api/v1/enquiries`, `POST/GET/PATCH /api/v1/applications` (fixed v1 `formData` shape: childName/dob/guardianName/guardianPhone/priorSchool — school-configurable form fields are a later config-over-code unit, not this one), `POST /applications/:id/convert` (reuses Unit 07's `createStudent` service function directly — not a duplicate implementation — then sets `status = CONFIRMED` + `studentId`; `409 CONFLICT` on a second convert attempt). `CONFIRMED` is excluded from the plain `PATCH /applications/:id` status enum so it can only ever be set by `convert`, keeping `status`/`studentId` from drifting out of sync.
  - Tests (`apps/api/test/admissions.test.ts`, 4, all passing, via the real `createApp()` + full pipeline): the full enquiry → application → convert pipeline (verifying a real `Enrollment` row, not just a `Student`), double-convert rejected with `409`, cross-tenant isolation (RLS-scoped + unscoped-query), `admission.manage` RBAC (ADMIN passes, TEACHER/ACCOUNTANT denied), and branch-scope denial for an ADMIN on Branch A targeting Branch B. `apps/api` suite now 59 tests total, all passing together with the 55 carried over from Units 01–09. Root `pnpm typecheck` clean across all 9 packages.
  - **Known gap flagged, not fixed this unit:** the fixed `formData` shape has no `address` field, but `Student.address` is required (non-optional, per `data-model.md` §5) — `convertApplication` currently passes an empty string, relying on front-desk staff to complete the profile via the existing `PATCH /students/:id` after conversion. Worth revisiting if this proves annoying in practice; not blocking since the DoD only requires "a real, correctly-enrolled Student," which this satisfies.

- **Unit 11 — Fee Setup.** `packages/db` gained `fees.prisma`: `FeeHead`, `FeeStructure`+`FeeStructureItem`, `FeeAssignment`, `Concession` + `FineRule` (addition — see Decisions) + enums `FeeType`, `FeeFrequency`, `ConcessionType`, `ConcessionStatus` (addition), all branch-scoped and RLS'd (`20260726065200_enable_rls_fees`). **Begins Milestone 2 (Fees).** No `Invoice`/`Payment`/`Receipt` yet — that's Unit 12.
  - **Spec drafted this turn** (`feature-specs/11-fee-setup.md` didn't exist before this unit) — read `data-model.md` §10, `rbac.md`, `feature-catalog.md` §B1 first, then **three decisions confirmed with the user before coding**: (1) `Concession.status` — `data-model.md` left it unenumerated (same gap as Unit 09's `LeaveRequest.type`) — confirmed a fixed `ConcessionStatus{PENDING, APPROVED, REJECTED}` so `rbac.md`'s separate `fee.concession.approve` permission has an actual decision to gate. (2) Fine/late-fee rules — `feature-catalog.md` lists this as P1 and Unit 11's own scope line mentions "fines," but `data-model.md`'s Fees section has no `FineRule` entity at all (fines only appear as a field on Unit 12's `InvoiceItem`) — confirmed adding a small `FineRule` model now (config only: `graceDays`/`isPercent`/`value` per `FeeStructureItem`), which Unit 12 will *read* when generating invoices, not apply anything itself this unit. (3) Bulk fee assignment — confirmed `POST /fee-structures/:id/assign { classId }` fans out to every currently-enrolled student in that class (current session), via `createMany({ skipDuplicates: true })` so re-running it is a no-op, not a duplicate-row bug.
  - **`modules/fees/`** (`apps/api`): `fee-heads` CRUD (soft-deleted, same pattern as `Subject`), `fee-structures` CRUD + nested `items` (feeHeadId cross-checked to share the structure's branch) + nested `fine-rule` (one per item, `@unique` on `feeStructureItemId`), `fee-assignments` (bulk-by-class + direct individual assign/unassign, `409 CONFLICT` on a duplicate individual assign), `concessions` (apply → `PENDING`, then `PATCH /:id/decide` gated by `fee.concession.approve`; `409 CONFLICT` on redeciding a non-`PENDING` concession, so it can't be approved-then-approved-again or flipped after rejection).
  - **RBAC:** `fee.setup` gates every mutation (OWNER/ACCOUNTANT per `rbac.md` — PRINCIPAL/ADMIN/TEACHER denied, narrower than most mutations in this codebase, same shape as Unit 09's `staff.manage`); `fee.view` gates reads (OWNER/PRINCIPAL/ADMIN/ACCOUNTANT, TEACHER excluded); `fee.concession.approve` (OWNER/ACCOUNTANT) is a separate permission from `fee.setup` even though the same two roles hold both today — kept distinct because `rbac.md` defines them as distinct strings.
  - Tests (`apps/api/test/fees.test.ts`, 5, all passing, via the real `createApp()` + full pipeline): full head→structure→item→fine-rule creation with PRINCIPAL denied mutation but allowed read, bulk-assign creating exactly one `FeeAssignment` per enrolled student and a verified no-op on re-run, cross-tenant isolation (RLS-scoped + unscoped-query), branch-scope denial for an ACCOUNTANT on Branch A targeting Branch B, and the full concession apply→approve workflow (TEACHER denied `decide`, re-deciding an already-decided concession rejected with `409`). `apps/api` suite now 64 tests total, all passing together with the 59 carried over from Units 01–10. Root `pnpm typecheck` clean across all 9 packages.

- **Unit 12 — Fee Collection + Ledger.** `packages/db` gained `payments.prisma`: `Invoice`+`InvoiceItem`, `Payment`, `Receipt` + enums `InvoiceStatus`, `PaymentMode`, `PaymentStatus` (`data-model.md` §10), branch-scoped and RLS'd (`20260726071200_enable_rls_payments`). `Invoice` gained `@@unique([studentId, sessionId, periodLabel])` (an addition beyond the sketch, same category as Unit 10's `Application.studentId`) — the mechanism that makes invoice generation idempotent.
  - **Spec drafted this turn** (`feature-specs/12-fee-collection-ledger.md` didn't exist before this unit) — read `data-model.md` §10, `rbac.md`, `api-conventions.md`'s idempotency section, `architecture-context.md`'s PDF/jobs notes first, then **two decisions confirmed with the user before coding**: (1) invoices are created via **automated generation** — `POST /fee-structures/:id/generate-invoices` expands every linked `FeeAssignment`'s `FeeStructureItem`s (respecting `frequency`) into due-dated invoices for the structure's session, not manual ad-hoc creation. Period expansion is a documented simplification (`apps/api/src/modules/payments/periods.ts`): `QUARTERLY`/`TERM` are evenly-spaced splits of the session date range, since `data-model.md` has no `Term` entity to anchor real boundaries. (2) Receipt PDF generation is **stubbed** (`receipt.generate` job, same pattern as Unit 05's `appbuild.stub`) — no PDF infrastructure or tenant-configurable template system exists yet; `Receipt.pdfUrl` stays `null` this unit.
  - **A third, lower-stakes decision made without a second question round** (flagged in the spec's Decisions rather than asked, since it's reversible and lower-impact): `Concession`/`FineRule` (Unit 11) are **not** automatically applied to generated invoices — `InvoiceItem.discount`/`.fine` exist per the schema but the generator leaves them at `0`, since nothing in `data-model.md` specifies an allocation scheme for a flat-amount concession split across multiple invoice items. Deferred to a later pass; fine application in particular pairs naturally with Unit 14's reminder cron, which already needs a scheduled per-invoice sweep. The same reasoning applies to `InvoiceStatus.OVERDUE`: nothing transitions an invoice into it without a scheduled sweep, so the dues/defaulter reports compute "logically overdue" directly (`dueDate < now` and not `PAID`/`CANCELLED`) rather than relying on the stored enum value.
  - **`modules/payments/`** (`apps/api`): invoice generation (idempotent via the new unique constraint, combining same-period items from different `FeeStructureItem`s into one invoice), `GET /invoices` + `:id`, `POST /payments` (requires an `Idempotency-Key` header per `api-conventions.md`, mapped to `Payment.idempotencyKey` — a replayed key returns the original `Payment` with `200` instead of creating a duplicate, verified by both a same-Payment-row check and an explicit id-equality assertion), invoice status transitions (`PARTIAL`/`PAID` based on cumulative successful payments vs. invoice total), a `Receipt` row + `AuditLog` entry per payment (`AGENTS.md` invariant #6 — the first unit in this build to actually write `AuditLog` for a domain mutation, not just platform actions), `GET /students/:id/fee-ledger` (a **derived** merge of invoices+payments, not a stored table, per `data-model.md`'s own note), `GET /fees/reports/{dues,defaulters}`, and `POST /students/:id/opening-balance` (seeds a legacy due as a `PENDING` invoice against an auto-created "Opening Balance" `FeeHead`; a second call for the same student/session is rejected `409` rather than silently duplicating).
  - **New generators** (`packages/db`, mirroring Unit 07's `nextAdmissionNo`): `nextInvoiceNumber`/`nextReceiptNumber` — same documented concurrency ceiling (count-then-pad, race-prone under concurrent generation in the same branch, retry-the-transaction is the upgrade path).
  - Tests (`apps/api/test/payments.test.ts`, 7, all passing, via the real `createApp()` + `startWorker()` — no mocks): invoice generation producing exactly 12 monthly invoices for a full session and a verified no-op on re-run, payment idempotency (replayed key → same `Payment` id, exactly one row in the DB) plus the `PARTIAL`→`PAID` transition across two real payments, a genuine `receipt.generate` enqueue→process round trip (observed via worker log output), the fee ledger merging both entry types, dues/defaulter reports validated against a deliberately-constructed overdue-vs-future invoice scenario, opening-balance creation + duplicate rejection, cross-tenant isolation (RLS-scoped + unscoped-query), and branch-scope denial for an ACCOUNTANT on Branch A targeting Branch B. `apps/api` suite now 71 tests total, all passing together with the 64 carried over from Units 01–11. Root `pnpm typecheck` clean across all 9 packages.

- **Unit 13 — Online Payment + Reconciliation.** `packages/db` gained `RefundRequest` + `RefundStatus` enum (additions beyond `data-model.md`'s sketch, same category as Unit 12's `Invoice` unique constraint) and `Payment.platformFeeAmount`, plus a new `AuditActorType.SYSTEM` value (webhook-driven mutations have no human actor). Branch-scoped; RLS per the established pattern.
  - **Spec drafted this turn** (`feature-specs/13-online-payment-reconciliation.md` didn't exist before this unit) — read `data-model.md` §10, `rbac.md`, `architecture-context.md`'s Razorpay/payments notes, `plans-entitlements.md`'s `online_payment` module first, then **four decisions confirmed with the user before coding**: (1) **gateway integration is stubbed** for order creation (no live Razorpay credentials or local emulator exist in this sandbox, unlike MinIO for S3) — but the **webhook's HMAC-SHA256 signature verification is real**, tested with a genuinely-computed signature against a test secret, not mocked. (2) **Platform fee tracking exists but the rate defaults to 0** (`PAYMENT_PLATFORM_FEE_BPS` env var) — no document anywhere gives an actual business rate. (3) **Reconciliation reporting is skipped entirely** this unit — no bank-statement import and no live gateway to poll against; deferred until a real gateway connection exists. (4) **Refunds are implemented now** (request + approval), resolving a direct conflict between Unit 12's own spec (which deferred `fee.refund`) and this unit's progress-tracker scope line (which explicitly lists "refunds") — the user confirmed implementing now, gated by the single `fee.refund` permission `rbac.md` defines (no separate request/approve permissions like Unit 11's `fee.setup`/`fee.concession.approve`).
  - **Refactored, not duplicated:** Unit 12's payment-completion logic (invoice status recompute, `Receipt` + `AuditLog` + `receipt.generate` enqueue) was extracted into a shared `finalizePaymentSuccess()` (`modules/payments/complete-payment.ts`) so the counter-payment path and the new webhook path can never drift apart.
  - **`modules/online-payments/`**: `POST /payments/online/initiate` — gated by the `online_payment` module being enabled (`requireModuleEnabled`, the first tenant-facing use of that Unit 05 helper) **and** either a `PARENT` paying for their own linked child (`resolveGuardianStudentIds`) or staff with `fees.collect`; discovered mid-implementation that `requireBranch` middleware is incompatible with `PARENT` callers (parents have no `BranchMembership`, so the middleware would 403 them before the self-scope check ever ran) — branch-scope for the staff path moved into the service instead of route middleware. `POST /webhooks/razorpay` (public, no `authGuard`) — verifies `X-Razorpay-Signature` via a raw-body capture added to `express.json()`'s `verify` hook (needed because a re-serialized JSON body doesn't reproduce the exact bytes the signature was computed over); resolves tenant context from the (stubbed) order's `notes` payload rather than an RLS-bypassing lookup. Refund `POST /payments/:id/refund-request` + `PATCH /refund-requests/:id/decide` (approval subtracts the refunded amount from the invoice's paid total and recomputes its status).
  - **New reusable primitive:** `userHasPermission()` extracted from `requirePermission`'s middleware body (`core/guards/require-permission.ts`) — needed for the initiate endpoint's "either self-scope or this permission" check, which doesn't fit a single `requirePermission()` gate.
  - Tests (`apps/api/test/online-payments.test.ts`, 7, all passing, via the real `createApp()` + `startWorker()` — no mocks): the `online_payment` module gate actually blocking (`403 MODULE_DISABLED`), `PARENT` self-scope (own child allowed, another child denied), a **genuinely HMAC-signed** `payment.captured` webhook completing the payment end-to-end (Invoice/Receipt/AuditLog) plus an invalid-signature rejection that touches no data, `payment.failed` setting `FAILED`, the refund approve→`REFUNDED`+invoice-recompute flow with `fee.refund` RBAC and branch-scope denial, and cross-tenant isolation (RLS-scoped + unscoped-query). `apps/api` suite now 78 tests total, all passing together with the 71 carried over from Units 01–12. Root `pnpm typecheck` clean across all 9 packages.

- **Unit 14 — Fee Reminders.** `packages/db` gained `notifications.prisma`: `NotificationLog` + `NotifChannel`/`NotifStatus` enums (RLS'd, first built here — Unit 20 reuses it for general announcements) and `WalletTxn` + `WalletTxnType` (no RLS, same as `SmsWallet` — `data-model.md` §13 gave this model's fields literally elided, so the shape here is a Unit 14 addition). **Completes Milestone 2 (Fees).**
  - **Spec drafted alongside Units 15/15b/16/17 in a single batch this session** (`feature-specs/14-fee-reminders.md` didn't exist before), reviewed by the user, then implemented. Key recommendations proceeded on as drafted: `SMS_COST_PAISE` (default 20) and the 3-day-before / 7-day-repeat-overdue cadence are both configurable placeholders, not real business figures.
  - **Cron + fan-out**: a BullMQ *repeatable* job (`fees.reminderScan`, daily `0 9 * * *`, registered idempotently in `apps/worker`'s real-process startup path only — not when tests import `startWorker()` directly) scans every `ACTIVE` tenant with the `fees` module enabled, finds due-soon/overdue `Invoice`s lacking a recent `fee.reminder` `NotificationLog` entry, and enqueues one `fees.reminderSend` per (invoice, eligible guardian). `apps/worker` gained its own tiny `enqueue()` (`src/enqueue.ts`) — the first case of a processor needing to enqueue a follow-up job itself, and `apps/worker` doesn't depend on `apps/api`'s `core/jobs.ts`, so it's a small duplicated producer, not a cross-app import.
  - **`fees.reminderSend`** checks `SmsWallet.balancePaise` first: sufficient → stub-sends, decrements the wallet + writes a `WalletTxn`, logs `NotificationLog(SENT)`; insufficient → logs `NotificationLog(FAILED)` without touching the wallet (never goes negative).
  - **`modules/notifications/`** (`apps/api`): `POST /fees/reminders/run` (manual trigger, scoped to the caller's own tenant, gated by `notification.send`) and `GET /notifications` (gated by `fee.view`).
  - Tests (`apps/api/test/notifications.test.ts`, 4, all passing, via the real `createApp()` + `startWorker()` — no mocks): a full scan→send round trip with real wallet deduction, the insufficient-balance path logging `FAILED` without deducting, cross-tenant `NotificationLog` isolation (RLS-scoped + unscoped-query), and RBAC (`notification.send` / `fee.view`). `apps/api` suite now 82 tests total, all passing together with the 78 carried over from Units 01–13. Root `pnpm typecheck` clean across all 9 packages.

- **Unit 15 — Attendance (API + Web).** `packages/db` gained `attendance.prisma`: `AttendanceRecord` + `AttendanceStatus`/`AttendanceSource` enums (`data-model.md` §8, fully specified with no gaps), branch-scoped and RLS'd. `regularizedById`/`regularizeReason` are a small addition beyond the bare sketch, needed to audit the correction path.
  - **Spec drafted alongside Units 14/15b/16/17 in a single batch**, reviewed by the user, then implemented as drafted: stayed backend + tests only (the unit's own title said "API + Web," flagged and confirmed to match every backend-only unit since 06 rather than building `apps/web-app` screens); `POST /attendance` accepts an optional client-supplied `id` per record now, so Unit 16's offline mobile sync won't need to retrofit it later.
  - **`modules/attendance/`**: `POST /attendance` (bulk, **upserts** by `(studentId, date)` — re-marking the same day overwrites, no error) gated by `attendance.mark` (`rbac.md`: PRINCIPAL/ADMIN/TEACHER — **not** OWNER); a `TEACHER` caller is additionally checked against `TeacherAssignment`/`Section.classTeacherId` for that specific section, PRINCIPAL/ADMIN aren't section-restricted. `GET /attendance` (`attendance.view` — OWNER/PRINCIPAL/ADMIN/TEACHER, not ACCOUNTANT). `PATCH /attendance/:id` — the audited correction path for a past day, gated by the narrower `attendance.regularize` (OWNER/PRINCIPAL/ADMIN only), writes an `AuditLog` entry. `GET /attendance/reports/register` (day-by-day grid) and `GET /attendance/reports/defaulters` (below-threshold %) — both `attendance.view`.
  - Tests (`apps/api/test/attendance.test.ts`, 5, all passing, via the real `createApp()` + full pipeline): bulk mark + same-day upsert (verified via record count, not just status), an unassigned `TEACHER` denied a section while an actually-`TeacherAssignment`-linked one is allowed, regularize restricted to `attendance.regularize` roles with a verified `AuditLog` row, register/defaulter reports validated against a deliberately-constructed good-student/poor-student scenario, cross-tenant isolation (RLS-scoped + unscoped-query), and branch-scope denial. `apps/api` suite now 87 tests total, all passing together with the 82 carried over from Units 01–14. Root `pnpm typecheck` clean across all 9 packages.

- **Unit 15b — Mobile App Shell** (pulled forward from Unit 24). `apps/mobile` is new: Expo (TS) + React Navigation, OTP login, i18next (Hindi/English), a WatermelonDB offline-storage foundation (schema-only, no synced tables yet), and `app.config.ts`/shared-mode-only EAS scaffolding.
  - **`Tenant.schoolCode`** (nullable, unique) — the follow-through on the founder decision recorded since Unit 03. `generateSchoolCode()` (`packages/db`) does a random-then-check-uniqueness loop (tenant creation is low-frequency, so this is simpler than a shared sequential counter); Unit 05's `createTenant` and the demo-tenant seed script both now assign one. `GET /api/v1/tenants/resolve/:schoolCode` (public, case-insensitive, `404`s an unknown or suspended/cancelled code) resolves it to the `tenantSlug` the existing OTP endpoints already accept — `tenantSlug` itself is unchanged, just no longer typed by hand.
  - **A real cross-package typecheck regression, found and fixed, not hidden:** adding `apps/mobile` (which depends on `react-native` + `react-i18next`) changed pnpm's peer-dependency resolution for `apps/web-app`'s own `react-i18next` instance, resolving a second, divergent `@types/react` copy for it — `components/i18n-provider.tsx` and `components/query-provider.tsx` started failing typecheck as a result (confirmed via a `pnpm-lock.yaml` diff, not assumed). Fixed at the root: both files re-type the third-party provider component against the file's own `ReactNode`, a standard, explained workaround for two non-identical `@types/react` instances in one monorepo — not a workaround for anything in this session's own code.
  - **Honest verification gap:** this environment has no Expo Go / simulator with a JS bundler attached, so **on-device/Expo Go verification was not performed** — only `tsc --noEmit` (clean across all 10 workspace packages) and the OTP-flow's *backend* half (existing, already-tested `/auth/otp/*` endpoints, unchanged) were verified. The user should run `pnpm --filter @vidyut/mobile start` themselves and confirm the school-code → phone → OTP → role-home flow end-to-end on a device/simulator before relying on it.
  - Tests (`apps/api/test/tenants.test.ts`, 5, all passing): schoolCode → tenantSlug resolution, case-insensitivity, `404` for an unknown code and for a suspended tenant's code, and tenant-isolation (each tenant's code resolves only to itself). `apps/api` suite now 92 tests total (Unit 15 didn't change this count; Unit 15b added the 5 new ones on top of 87). Root `pnpm typecheck` clean across all **10** packages (mobile is new).

- **Unit 16 — Teacher Mobile Attendance + Parent Alerts** (Milestone 3's final unit). Builds directly on Unit 15's `POST /attendance`, no new tables beyond the offline mirror on-device.
  - **Backend absence alert**: `markAttendance()` (`apps/api/src/modules/attendance/service.ts`) now records each record's pre-upsert status inside the `withTenant` transaction and, after it commits, enqueues one `students.absenceAlert` job per record that **genuinely transitioned into ABSENT** (`existing?.status !== "ABSENT"`) — a re-mark that was already `ABSENT` (e.g. a retried offline sync) does not re-fire. `apps/worker/src/processors/students-absence-alert.ts` resolves the student's `isPrimary`/`canPay` guardians and writes one `NotificationLog(channel: SMS, templateKey: "attendance.absence", status: SENT)` per guardian with a phone — stub-sent, and **not** billed against `SmsWallet` (unlike Unit 14's fee reminders, which are).
  - **Mobile offline attendance** (`apps/mobile`): `src/lib/database.ts` gained a real WatermelonDB `attendance_records` table (replacing the prior schema-only stub) with `syncedAt` tracking. `src/screens/TeacherAttendanceScreen.tsx` is the teacher's roster: loads a section's students via the existing `GET /students?sectionId=` (Unit 07, reused — no new endpoint needed), lets the teacher tap a student to cycle PRESENT → ABSENT → LATE (written to WatermelonDB immediately, works offline), and a "Sync" button pushes every unsynced local record for that section+day to `POST /attendance` in one bulk call using the client-generated WatermelonDB row `id` (Unit 15 already supports this) — a retried sync is a no-op upsert, never a duplicate. `RootNavigator` now routes any session with the `TEACHER` role straight to this screen instead of the generic `HomeScreen` placeholder.
  - **Known gap, not resolved this unit**: no endpoint lets a teacher look up "my own assigned sections" (only `GET /academic/teacher-assignments?staffId=`, which isn't self-deriving from the logged-in user's JWT). The screen has the teacher type in `branchId`/`sectionId` directly rather than picking from a list. Small, isolated addition for whenever a real teacher-facing section picker is wanted — doesn't block the offline-mark → sync → alert flow this unit is about.
  - **Honest verification gap** (same posture as Unit 15b): no Expo Go/simulator with a bundler is attached in this environment, so the on-device tap-to-mark → offline-write → sync-push flow was not visually verified — only `tsc --noEmit` (clean across all 10 packages) and the backend absence-alert logic (fully tested below) were verified. The user should run `pnpm --filter @vidyut/mobile start` and confirm the roster/sync UI end-to-end on a device/simulator.
  - Tests (`apps/api/test/mobile-attendance.test.ts`, 2, all passing): a client-generated `id` upserts cleanly on a retried sync (record count stays 1), and marking a student `ABSENT` enqueues exactly one `NotificationLog` while a same-day re-mark does not duplicate it. `apps/api` suite now **94 tests total** (up from 92), all passing together with everything carried over from Units 01–15b. Root `pnpm typecheck` clean across all 10 packages. **Completes Milestone 3 (Attendance).**

- **Unit 17 — Exam Setup & Grading.** `packages/db` gained `exam.prisma`: `Exam` + `ExamSubject` + `ExamType`/`GradingScheme` enums (`data-model.md` §9, fully specified), branch-scoped and RLS'd.
  - **Open question resolved as drafted**: no new "board config" entity — an exam's effective board is just its branch's existing `Board` field (Unit 02); a real grade-boundary/conversion table for `CCE`/`CGPA` is deferred to whichever unit first computes a grade from a mark (likely Unit 18), matching the spec's own recommendation.
  - **`modules/exams/`**: `POST/GET/PATCH/DELETE /api/v1/exams` (soft-deleted) and `POST/GET/DELETE /api/v1/exams/:examId/subjects`, both gated by `exam.manage` (`rbac.md`: OWNER/PRINCIPAL/ADMIN) on mutations; reads are open to any authenticated staff role, matching Unit 06's structural-data pattern. `ExamSubject` cross-checks its `classId`/`subjectId` belong to the exam's own branch, and `createExamSubjectSchema` guards `passMarks ≤ maxMarks` at the validation layer.
  - **Lock semantics**: `isLocked` is a plain field on the normal `PATCH` (no separate lock endpoint). Once `true`, any other field change on that `Exam` and any subject-mutation (`POST`/`DELETE` on `/subjects`) is rejected with `409`. Unlocking is still possible but only as its own deliberate `PATCH` carrying `{ isLocked: false }` alone — never bundled with another field change, so a lock can't be silently reopened as a side effect of an unrelated edit.
  - Tests (`apps/api/test/exams.test.ts`, 6, all passing, via the real `createApp()` + full pipeline): exam + exam-subject CRUD end to end, the `passMarks > maxMarks` validation guard, the lock → blocked-mutation (`409`) → deliberate-unlock → allowed-again round trip, RBAC (`exam.manage` roles pass, TEACHER/ACCOUNTANT denied on mutations, reads open to TEACHER), branch-scope denial (ADMIN restricted to their own branch), and tenant-isolation (RLS-scoped zero rows + a deliberately unscoped plain-`prisma` query also blocked by `FORCE ROW LEVEL SECURITY`). `apps/api` suite now **100 tests total** (up from 94), all passing. Root `pnpm typecheck` clean across all 10 packages.

- **Unit 18 — Marks Entry + Moderation.** `packages/db` gained `marks.prisma`: `MarksEntry{ id, tenantId, branchId, examSubjectId, studentId, marks?, grade?, isAbsent, enteredById, lockedAt? }`, unique `(examSubjectId, studentId)`, branch-scoped and RLS'd. `enteredById` is a plain `User.id` with no FK relation, same convention as `AttendanceRecord.markedById`.
  - **Open question resolved as drafted**: no per-tenant grade-boundary config exists anywhere, so `modules/marks/grading.ts`'s `computeGrade()` hardcodes CBSE's own 9-band CCE table (A1 91–100 … E2 0–20) for both `GRADE` and `CCE` schemes, plain formatted percentage for `PERCENTAGE`, and leaves `grade` `null` for `MARKS`/`CGPA` (no marks→CGPA formula is uncontroversial enough to hardcode).
  - **`modules/marks/`**: `POST /api/v1/marks` (bulk upsert by `(examSubjectId, studentId)`, same upsert-not-error shape as Unit 15's attendance) gated `marks.enter` (`rbac.md`: PRINCIPAL/TEACHER — **not** OWNER/ADMIN); a TEACHER caller is additionally checked against `TeacherAssignment` for that `ExamSubject`'s class+subject (mirrors Unit 15's `assertCanMarkSection`). Guards `marks > maxMarks` with a `400`. `GET /api/v1/marks?examSubjectId=` — broad read. `PATCH /api/v1/marks/:id/lock` — the per-row moderation step (narrower than Unit 17's whole-exam `isLocked`), gated `marks.moderate` (OWNER/PRINCIPAL); once locked, further upserts touching that row are rejected `409`, other rows for the same `ExamSubject` stay editable.
  - Tests (`apps/api/test/marks.test.ts`, 6, all passing, via the real `createApp()` + full pipeline): bulk entry + upsert-by-(examSubjectId,studentId) (verified via record count), CBSE grade-band computation for a `GRADE`-scheme exam, the `marks > maxMarks` validation guard, the per-row lock → blocked-edit (`409`) round trip with an unaffected sibling row, RBAC (`marks.enter` PRINCIPAL passes/OWNER denied, `marks.moderate` PRINCIPAL passes/TEACHER denied), and tenant-isolation (RLS-scoped zero rows + a deliberately unscoped plain-`prisma` query also blocked by RLS). `apps/api` suite now **106 tests total** (up from 100), all passing. Root `pnpm typecheck` clean across all 10 packages.

- **Unit 19 — Report Cards.** `packages/db` gained `reportcard.prisma`: `ReportCardTemplate{ id, tenantId, branchId?, name, board, layout(Json) }` (branchId nullable = tenant-wide default template) and `ReportCard{ id, tenantId, branchId, sessionId, studentId, examId, templateId, pdfUrl?, publishedAt? }`, unique `(examId, studentId)`. Both branch-scoped and RLS'd.
  - **Open questions resolved as drafted**: `layout` is an opaque `Json` blob and generation is a stub job (`reportcard.generate`, `console.log`, no real Puppeteer render, `pdfUrl` stays null) — identical posture to Unit 12's `receipt.generate`; no separate `Term` model — `ReportCard.examId` is the only "term" reference, `data-model.md`'s "termId" read as a stale alternate name; a parent-facing read endpoint is deferred to whichever unit builds the real parent app surface.
  - **`modules/reportcards/`**: `POST/GET/PATCH/DELETE /api/v1/report-card-templates` (soft-deleted, gated `reportcard.generate` for mutations, broad reads including tenant-wide `branchId: null` templates alongside branch-specific ones). `POST /api/v1/report-cards/generate` (`{ examId, templateId, studentIds? }` — omitting `studentIds` resolves every student enrolled in the exam's `ExamSubject.classId`s for that session) upserts by `(examId, studentId)` and enqueues one `reportcard.generate` job per card (never inline), gated `reportcard.generate`. `GET /api/v1/report-cards?examId=` — list with `publishedAt`/`pdfUrl` status. `PATCH /api/v1/report-cards/:id/publish` — sets `publishedAt`, gated by the narrower `reportcard.publish` (OWNER/PRINCIPAL only — an ADMIN who can generate cannot publish).
  - Tests (`apps/api/test/reportcards.test.ts`, 5, all passing, via the real `createApp()` + full pipeline): template CRUD + generate + publish end to end, regenerating for the same exam upserts rather than duplicating (verified via row count), RBAC (`reportcard.generate` ADMIN passes on template CRUD + generate, `reportcard.publish` denies that same ADMIN), branch-scope denial (an ADMIN on Branch A denied creating a Branch B template), and tenant-isolation (RLS-scoped zero rows + a deliberately unscoped plain-`prisma` query also blocked by RLS). `apps/api` suite now **111 tests total** (up from 106), all passing. Root `pnpm typecheck` clean across all 10 packages. **Completes Milestone 4 (Exams & Report Cards).**

- **Unit 20 — Notifications & Announcements.** `packages/db`'s `notifications.prisma` (Unit 14's file) gained `Announcement{ id, tenantId, branchId, title, body, audience(Json)?, attachmentUrl?, publishedAt, createdById }`, branch-scoped and RLS'd. `createdById` is a small addition beyond the bare doc sketch (the issuer, needed for the register), same convention as Unit 15's `regularizedById`.
  - **Open questions resolved as drafted**: `audience` is a simple rule object `{ roles?: RoleKey[], classIds?: string[] }`, not a query engine — empty/omitted means every staff member in the branch sees it (staff reads are never audience-filtered); "events/calendar" stayed fully out of scope (no model exists, a genuinely different feature from a notice board); publishing happens immediately on create (no draft/review step) since a "circular" is fire-and-forget, not a workflow.
  - **`modules/announcements/`**: `POST /api/v1/announcements` creates and sets `publishedAt = now()` in one step, gated `announcement.send` (OWNER/PRINCIPAL/ADMIN), and enqueues an `announcement.fanout` job. `GET /api/v1/announcements?branchId=` — broad read (any authenticated staff role sees every announcement in the branch, unfiltered). `DELETE /api/v1/announcements/:id` — hard delete (a notice-board correction, not an audited financial record), gated `announcement.send`.
  - **`apps/worker/src/processors/announcement-fanout.ts`**: resolves `audience.roles` via `UserRole` (branch + role-key match) and `audience.classIds` via `Enrollment` → `StudentGuardian` → `Guardian.userId` (parents), writing one `NotificationLog(channel: PUSH, templateKey: "announcement.published")` per matched user (stub-sent, same pattern as Units 14/16's fan-out jobs).
  - Tests (`apps/api/test/announcements.test.ts`, 5, all passing, via the real `createApp()` + `startWorker()` — no mocks): create → immediate `publishedAt` → real fan-out verified against a `{ roles: ["TEACHER"] }` audience matching a real `TEACHER` `User`, list/delete CRUD, RBAC (`announcement.send` roles pass, TEACHER/ACCOUNTANT denied, reads open to TEACHER), branch-scope denial (an ADMIN on Branch A denied Branch B), and tenant-isolation (RLS-scoped zero rows + a deliberately unscoped plain-`prisma` query also blocked by RLS). `apps/api` suite now **116 tests total** (up from 111), all passing. Root `pnpm typecheck` clean across all 10 packages.

- **Unit 21 — Certificates & IDs.** `packages/db` gained `certificates.prisma`: `Certificate{ id, tenantId, branchId, studentId, type(CertificateType), customTitle?, number, pdfUrl?, issuedAt, issuedById }`, unique `(branchId, type, number)`, branch-scoped and RLS'd. `issuedById` is a small addition beyond the bare doc sketch (the issuer, needed for the register), same convention as Unit 20's `createdById`.
  - **Open question resolved as drafted**: `CertificateType` extends `data-model.md`'s bare `{TC, BONAFIDE, CHARACTER, CONDUCT, CUSTOM}` sketch with `ID_CARD`/`ADMIT_CARD` — this unit's own title and `build-approach.md`'s Milestone 5 scope line both name ID/admit cards as in-scope, and structurally they're identical to any other certificate (a numbered, issued, per-student document), just a different `type`.
  - **`packages/db/src/generate-certificate-number.ts`**: `nextCertificateNumber(tx, branchId, type)` — sequential, zero-padded, **per `(branchId, type)`** (a TC register and a bonafide register are numbered independently, matching real school registers), same count-based scheme and documented race-condition ceiling as `nextInvoiceNumber`/`nextReceiptNumber`.
  - **`modules/certificates/`**: `POST /api/v1/certificates` (`{ studentId, type, customTitle? }` — `customTitle` required when `type = CUSTOM`, validated at the Zod layer) resolves the student's branch, assigns the next number, and enqueues a stub `certificate.generate` job (no real PDF rendering yet, same posture as Units 12/19). `GET /api/v1/certificates?branchId=&studentId=&type=` — the issue register/log. Both gated `certificate.issue` (OWNER/PRINCIPAL/ADMIN) — a register is itself sensitive, not open to every staff role like Unit 17/06's structural data.
  - Tests (`apps/api/test/certificates.test.ts`, 6, all passing, via the real `createApp()` + full pipeline): issuing a TC then a BONAFIDE for the same student proves independent per-type sequences (`TC-000001`, `BONAFIDE-000001`, then `TC-000002`), the `CUSTOM`-without-`customTitle` validation guard, an `ID_CARD` issues cleanly (the enum extension), RBAC (`certificate.issue` ADMIN passes on issue + register read, TEACHER denied on both), branch-scope denial (an ADMIN on Branch A denied issuing against a Branch B student), and tenant-isolation (RLS-scoped zero rows + a deliberately unscoped plain-`prisma` query also blocked by RLS). `apps/api` suite now **122 tests total** (up from 116), all passing. Root `pnpm typecheck` clean across all 10 packages. **Completes Milestone 5 (Communication & Certificates).**

- **Unit 22 — Timetable.** `packages/db` gained `timetable.prisma`: `TimetablePeriod{ id, tenantId, branchId, sessionId, sectionId, dayOfWeek, periodNo, subjectId, staffId, room? }`, unique `(sectionId, dayOfWeek, periodNo)`, branch-scoped and RLS'd. `dayOfWeek` is a plain `Int` 0–6, no enum — validated at the Zod layer (`min(0).max(6)`).
  - **Open questions resolved as drafted**: only two conflict guards, both cheap and high-value — slot uniqueness (DB-level `@@unique`, upserted like Unit 15's attendance) and a cross-section staff double-booking check (same `staffId`+`sessionId`+`dayOfWeek`+`periodNo` in a *different* section → `409`); auto/smart generation and room-conflict detection stayed explicitly out of scope (`feature-catalog.md` marks auto-generation `[P3]`). Self-scoped parent/student/teacher "app views" are deferred to whichever unit builds those real screens (same posture as Units 19/20's Open Questions) — this unit ships plain `sectionId`/`staffId`-filtered reads open to any authenticated user in the branch.
  - **`modules/timetable/`**: `POST /api/v1/timetable` (`{ branchId, sessionId, sectionId, periods: [...] }`, bulk upsert by `(sectionId, dayOfWeek, periodNo)`) gated `timetable.manage` (OWNER/PRINCIPAL/ADMIN); cross-checks the whole batch for a staff double-booking **before** writing any row (atomic — a partially-conflicting batch fails entirely, not partially). `GET /api/v1/timetable?sectionId=`/`?staffId=` — resolves the target's branch and checks access, broad read otherwise. `DELETE /api/v1/timetable/:id` — single-slot removal, gated `timetable.manage`.
  - Tests (`apps/api/test/timetable.test.ts`, 5, all passing, via the real `createApp()` + full pipeline): bulk upsert + same-slot overwrite (verified via row count, not just response), the cross-section staff-double-booking `409`, RBAC (`timetable.manage` ADMIN passes, TEACHER denied on mutation, reads open to TEACHER), branch-scope denial (an ADMIN on Branch A denied writing Branch B's grid), and tenant-isolation (RLS-scoped zero rows + a deliberately unscoped plain-`prisma` query also blocked by RLS). `apps/api` suite now **127 tests total** (up from 122), all passing. Root `pnpm typecheck` clean across all 10 packages.

- **Unit 23 — Homework/Assignments.** `packages/db` gained `homework.prisma`: `Homework{ id, tenantId, branchId, sectionId, subjectId, title, description, attachmentUrl?, dueDate, createdById }` (`data-model.md` §11, fully specified), branch-scoped and RLS'd.
  - **Open questions resolved as drafted**: no calendar model — a parent/student "calendar" is just `GET /homework` filtered by due date, no new entity (matches Unit 20's Open Question 2 precedent); self-scoped parent/student reads stay deferred to Unit 24, which is exactly the unit built to resolve this recurring gap.
  - **`modules/homework/`**: `POST/PATCH/DELETE /api/v1/homework` gated `homework.manage` (`rbac.md`: PRINCIPAL/TEACHER only — not OWNER/ADMIN, same asymmetry as Unit 18's `marks.enter`); a TEACHER caller is additionally checked against `TeacherAssignment`/`Section.classTeacherId` for that section (reuses the exact pattern from Unit 15's `assertCanMarkSection`), PRINCIPAL isn't section-restricted. `GET /api/v1/homework?sectionId=&subjectId=&dueBefore=&dueAfter=` — broad read (any authenticated staff role).
  - Tests (`apps/api/test/homework.test.ts`, 5, all passing, via the real `createApp()` + full pipeline): full CRUD by a section-assigned TEACHER, an unassigned TEACHER denied while a PRINCIPAL is allowed regardless of assignment, RBAC (OWNER/ADMIN/ACCOUNTANT denied on mutation, reads open to OWNER), branch-scope denial (a TEACHER on Branch A denied Branch B), and tenant-isolation (RLS-scoped zero rows + a deliberately unscoped plain-`prisma` query also blocked by RLS). `apps/api` suite now **132 tests total** (up from 127), all passing. Root `pnpm typecheck` clean across all 10 packages. **Completes Milestone 6 (Scheduling & Homework) — the v1 "Complete Core" backend-domain build (Units 06–23) is now fully implemented.**

- **Unit 24 — Mobile: Parent/Student Layers.** First unit of Milestone 7. Builds the self-scope layer every unit since 19 deferred to "later" — resolved here.
  - **Open questions resolved as drafted**: `Student.userId String? @unique` added (same nullable-unique pattern as `Guardian.userId`) — optional, opt-in direct student login via the existing OTP flow (no auth-service changes needed at all: `POST /auth/otp/*` already resolves purely by `User.phone` + `UserRole`, so a `Student`-linked `User` with a `STUDENT` role logs in through the exact same path parents already use). One shared resolver, not five bespoke checks: `resolveSelfStudentIds(auth)` (`apps/api/src/core/guards/require-self.ts`) extends Unit 08's existing `resolveGuardianStudentIds` — `STUDENT` tokens resolve to their own linked `Student.id`, `PARENT` tokens delegate to the existing guardian resolver, everything else returns empty.
  - **`modules/me/`**: `GET /api/v1/me/students` (new — the caller's own resolved student(s), themselves or linked children, replacing the mobile app's prior use of the guardian-specific `/guardians/me/children` since that endpoint doesn't work for a `STUDENT` token), `GET /api/v1/me/attendance?studentId=&month=&year=`, `GET /api/v1/me/report-cards?studentId=` (only `publishedAt`-set rows — Unit 19's whole reason that field exists), `GET /api/v1/me/homework?studentId=`, `GET /api/v1/me/timetable?studentId=` (the latter two resolve the student's current-session section via `getCurrentSessionId` + `Enrollment`, then read Unit 23/22's tables). Every endpoint re-checks `studentId` against `resolveSelfStudentIds()` on every call, never trusting a client-supplied id alone.
  - **Mobile**: `ParentStudentHomeScreen` (`apps/mobile`) replaces the generic `HomeScreen` placeholder for `PARENT`/`STUDENT` roles — a multi-child switcher (chips, only shown when >1 child) plus four tabs (attendance/report cards/homework/timetable) reading the endpoints above.
  - **Honest verification gap** (same posture as every prior mobile unit): no Expo Go/simulator with a bundler is attached in this environment — only `tsc --noEmit` (clean across all 10 packages) and the backend self-scope logic (fully tested below) were verified. The user should run `pnpm --filter @vidyut/mobile start` and confirm the child-switcher/tab UI end-to-end on a device/simulator.
  - Tests (`apps/api/test/me.test.ts`, 3, all passing, via the real `createApp()` + full pipeline): a PARENT fetches their own child's `/me/students`, attendance, and homework but is `403`'d on an unrelated child in the same tenant; a `STUDENT` with a direct login (`Student.userId` set) fetches their own timetable; `/me/report-cards` never returns an unpublished `ReportCard` even when one exists alongside a published one for the same student. `apps/api` suite now **135 tests total** (up from 132), all passing. Root `pnpm typecheck` clean across all 10 packages.

- **Unit 25 — Parent App.** Fills out Unit 24's minimal proof screen into the real parent-facing experience: fees + pay, attendance, results, notices, homework, timetable.
  - **Open questions resolved as drafted**: multi-child switching stays the same chip-row client-side state from Unit 24 (no re-login, `?studentId=` already threaded through every `/me` read); a real Razorpay Checkout SDK/WebView was explicitly **not** built — the backend's own order creation (`createStubOrder`, Unit 13) is still a stub, so a real checkout screen against it would be verification theatre, not a working payment flow. The mobile "Pay Now" button proves the `initiate` round trip only (shows the returned `gatewayOrderId`), with this deferral documented inline in both the api-client and the screen; offline caching was skipped entirely — this is a read-heavy surface, WatermelonDB's sync engine (built for Unit 16's offline writes) isn't the right tool here.
  - **New self-scoped endpoints** (`modules/me/`, alongside Unit 24's): `GET /api/v1/me/fees/ledger?studentId=` — required extracting `buildStudentFeeLedgerEntries()` out of Unit 12's staff-facing `getStudentFeeLedger` (which was branch/permission-gated in a way that hard-fails for a PARENT token with no `BranchMembership`) so both the staff and self-scope reads compute the exact same ledger, just authorized differently. `GET /api/v1/me/announcements?studentId=` — same audience-matching rule as Unit 20's `announcement.fanout` worker job (roles/classIds), just a read instead of a `NotificationLog` write.
  - **Mobile**: `ParentStudentHomeScreen` extended with `fees`/`notices` tabs alongside Unit 24's `attendance`/`reportCards`/`homework`/`timetable` — a fee ledger view with a "Pay Now" action per unpaid invoice, and a notices feed.
  - **Honest verification gap** (same posture as every prior mobile unit): no simulator/device attached in this environment — only `tsc --noEmit` (clean across all 10 packages) and the backend self-scope logic (fully tested below) were verified.
  - Tests (`apps/api/test/me.test.ts`, 2 new, all passing, on top of Unit 24's 3): `GET /me/fees/ledger` returns a self-scoped student's real invoice/payment history; `GET /me/announcements` returns only a global notice and a class-matched one, correctly excluding a role-targeted-elsewhere one. `apps/api` suite now **137 tests total** (up from 135), all passing. Root `pnpm typecheck` clean across all 10 packages.

- **Unit 26 — Teacher App.** Extends Unit 16's attendance screen rather than building a new surface from scratch; resolves the "my sections" gap Unit 16's own tracker entry flagged as unresolved.
  - **`GET /api/v1/academic/teacher-assignments/me`** (new, self-scoped — no `staffId` param, resolves the caller's own `Staff` row via `getStaffByUserId` then their `TeacherAssignment` rows, `include`-ing `section.class` and `subject` for display). Closes the gap: previously only `GET /academic/teacher-assignments?staffId=` existed, which doesn't self-derive from the logged-in user's JWT.
  - **Mobile**: a new shared `SectionPicker` component (chips, fed by the endpoint above) replaces the manual branch/section text-entry `TeacherAttendanceScreen` used since Unit 16. `MarksEntryScreen` (new) — one-student-at-a-time entry (context/feature-specs/26's Open Question 1: a phone-sized flow, not Unit 18's spreadsheet-style grid) against a picked exam + the resolved `ExamSubject` for that class/subject, using Unit 18's `POST /marks` with a single-entry batch per student. `HomeworkPostScreen` (new) — a simple form against Unit 23's `POST /homework`, scoped to the teacher's own picked assignment. `TeacherHomeScreen` (new) wraps all three behind one tab row, replacing the direct `TeacherAttendanceScreen` route for the `TEACHER` role.
  - **Honest verification gap** (same posture as every prior mobile unit): no simulator/device attached — only `tsc --noEmit` (clean across all 10 packages) and the backend self-scope logic (tested below) were verified.
  - Tests (`apps/api/test/teacher-assignments-me.test.ts`, 1, passing): a teacher's own assignments are returned, correctly excluding a second teacher's assignment for the same section+subject in the same tenant. `apps/api` suite now **138 tests total** (up from 137), all passing. Root `pnpm typecheck` clean across all 10 packages.

- **Unit 27 — Web Admin Panel.** `apps/web-app` gained the school-staff-facing admin surface — the biggest gap in the build (Units 06–23 had API + tests only, no web UI). Per the spec's own Open Question 1, deliberately scoped to the shell + three reference modules, not all 15+ backend modules.
  - **Shell**: a new `app/(school)/` route group with its own `layout.tsx` (client-side auth guard, same posture as the super-admin layout), `SchoolSidebar` (role-filtered nav — by role, not permission, since no `GET /auth/me` endpoint exists yet to fetch effective permissions; a deliberate simplification, documented inline), and a `BranchSelector` (manual branch-ID entry — no `GET /branches` endpoint exists for tenant staff yet, same "manual entry, documented gap" precedent Unit 16 used before Unit 26 built a real picker).
  - **`lib/admin-client.ts`**: token storage + typed fetch wrapper, mirroring Unit 05's `lib/platform-client.ts` pattern exactly (same shape, separate localStorage key, separate JWT) — proves the pattern is truly reusable across the super-admin and school-admin surfaces.
  - **Reference modules**: students (list/search/create/edit, using the existing shadcn `Table` + `useQuery` pattern already established by Unit 05's tenant list — no new "data table" abstraction needed, it already existed), fees (invoice list + a `Dialog`-based collect-payment form), attendance (register grid + defaulter list).
  - **Verified live in a browser** (not just typecheck, per this repo's own UI-change rule): logged in as the demo tenant's seeded OWNER, created a real `Class`/`Section` via direct API calls, created a real student through the UI end to end (visible back in the list afterward), and confirmed the fees/attendance pages render cleanly against empty real data with no console errors.
  - **Known gap surfaced during verification**: the branch selector's value isn't reactive across components (changing it requires a page reload for list queries to pick it up, since each page reads `getAdminBranchId()` once at render) — acceptable for this reference implementation, a real shared branch-context provider is fast-follow work alongside the remaining modules.
  - **Remaining modules explicitly deferred** (fast-follow, not blocking this unit): guardians, staff, admissions, exams, marks, report cards, announcements, certificates, timetable, homework — each is a mechanical repeat of the same shell/table/form pattern established here, not a new design problem.
  - Lint + typecheck pass (all 10 packages clean); `progress-tracker.md` updated (27 → done, 28 current).

- **Unit 28 — Owner/Principal Dashboard.** A thin aggregator, per the spec's own Open Question 1 — every number reuses an existing, already-tested calculation, no new business logic.
  - **`GET /api/v1/dashboard/summary?branchId=`**: this month's collection % (`Unit 12`'s `invoiceTotal` helper, now exported and reused rather than duplicated), total dues (all non-cancelled invoices, any period), today's attendance % (Unit 15's `PRESENT`/`LATE`/`HALF_DAY` convention), and this month's admissions funnel (Unit 10's `Enquiry` count → `Application` count → `Application.studentId`-set count as "converted"). Gated by either `dashboard.owner` or `dashboard.principal` (`rbac.md`: OWNER-only vs OWNER+PRINCIPAL — the endpoint itself just requires either, no per-role field differences).
  - **Web**: a `Dashboard` nav item (OWNER/PRINCIPAL only) + four stat cards (`apps/web-app/app/(school)/dashboard`), reusing the exact `useQuery` + typed-client pattern from Units 05/27 — no new charting library.
  - **Verified live in a browser**: logged in as the demo tenant's OWNER, confirmed the dashboard renders all four cards with real (zero, since freshly seeded) data and no console errors.
  - Tests (`apps/api/test/dashboard.test.ts`, 3, all passing): every number checked against a direct recomputation from seeded rows (a real invoice+payment+attendance+enquiry fixture, not just "the endpoint responds") — 40% collection on a ₹1000 invoice with a ₹400 payment, ₹600 total dues, 100% attendance, 1 enquiry; RBAC (`dashboard.owner`/`dashboard.principal` pass, ADMIN/TEACHER denied); branch-scope denial (a PRINCIPAL on Branch A denied Branch B). `apps/api` suite now **141 tests total** (up from 138), all passing. Root `pnpm typecheck` clean across all 10 packages.

- **Unit 29 — Public Site + Online Admission + Branded PWA.** Completes Milestone 7 (Apps & Dashboards). New workspace package `apps/web-site` (port 3001) — a genuinely separate app from `apps/web-app`, not a route group on it.
  - **`GET /api/v1/public/schools/:schoolCode`** and **`POST /api/v1/public/admissions/:schoolCode`** — unauthenticated (no `authGuard`/`tenantContext`, same precedent as Unit 15b's `tenantsRouter`), resolving the tenant by `schoolCode` (404 for unknown/suspended/cancelled). The enquiry endpoint defaults to the tenant's oldest active branch rather than building a branch-picker UI, since multi-branch public admission is explicitly deferred. Rate-limited with a new stricter `createRateLimiter({ windowMs: 60_000, max: 5, keyPrefix: "public-admission" })`, the first route-specific (non-default) use of Unit 24's rate-limit helper.
  - **`apps/web-site`**: root `/` is a schoolCode-entry redirect into `/[schoolCode]` (Open Question 1: no shared marketing site, per-tenant public pages only, reusing the same `schoolCode` mechanism the mobile app already resolves). `/[schoolCode]` is an async server component rendering school info + an `AdmissionForm` client component posting to the public enquiry endpoint.
  - **PWA**: Next's built-in `app/manifest.ts` metadata route + a hand-written minimal `public/sw.js` (network-first) + a `register-sw.tsx` client component — no `next-pwa` dependency added, per stdlib/native-first.
  - **Real bug found and fixed**: `packages/db/prisma/seed.ts`'s `tenant.upsert({ update: {} })` silently discarded a freshly-computed `schoolCode` on every re-run after first creation, leaving the demo tenant's `schoolCode` permanently `null`. Fixed by hoisting `schoolCode` to a `const` passed into both `create` and a non-empty `update: { schoolCode }`; re-seeded, confirmed demo tenant now has `schoolCode: "4UZC3S"`.
  - **Verified live in a browser**: started the API + `web-site` preview servers, navigated to `/4UZC3S`, confirmed the page renders real live data ("Demo School", "School code: 4UZC3S", "Demo School — Main Branch", "CBSE", the admission form) with no console errors. The form-submission round trip itself is covered by the automated test below rather than re-verified manually a second time.
  - Tests (`apps/api/test/public.test.ts`, 4, all passing): resolve real school info, 404 for an unknown school code, a real `Enquiry` row created and visible via `withTenant`, and rate-limiting (8 parallel requests, asserting at least one `429`). `apps/api` suite now **145 tests total** (up from 141), all passing. Root `pnpm typecheck` clean across all now-**11** workspace packages (new `@vidyut/web-site` added).

## In Progress

- (none)

- **Unit 30 — Billing & Subscriptions.** First unit of Milestone 8.
  - **`PlatformInvoice` model** (per its own Open Question 1) — `Platform*`-prefixed to avoid confusion with the tenant-facing `Invoice` (Unit 12). `POST/GET /platform/tenants/:id/invoices`, `PATCH .../invoices/:invoiceId` (mark PAID/OVERDUE) — manual create/reconcile for v1, no automated recurring billing, matching the spec's own reasoning.
  - **Real bug found and fixed**: no `Subscription` row was ever created anywhere in the codebase — `Tenant.planId` tracked the plan but the `Subscription` model (status/currentPeriodEnd) sat completely unused since Unit 05. Fixed by creating one ACTIVE `Subscription` (1-year period) in `createTenant`, and syncing its `planId` in `patchTenant` on a plan change. `PlatformInvoice.subscriptionId` would have had nothing to reference otherwise.
  - **`POST /platform/tenants/:id/wallet/recharge`** — credits `SmsWallet.balancePaise` + writes a `WalletTxn(CREDIT)`, reusing Unit 14's existing wallet/txn models; verified the existing debit path still works against the same wallet afterward.
  - **`GET /platform/revenue/summary`** — subscription revenue (sum of PAID `PlatformInvoice`s) + platform-fee revenue. The latter sums `Payment.platformFeeAmount`, which is tenant-RLS'd (Unit 13) — no cross-tenant bypass exists, so it's one `withTenant()` aggregate per tenant, not a single unscoped query.
  - **Second real bug found and fixed**: `GET /platform/tenants/:id/usage` had hardcoded `students: { used: 0 }` behind a comment saying "Student model doesn't exist until Unit 07" — stale for 20+ units. Fixed to a real `tx.student.count({ where: { status: "ACTIVE" } })`; also added `smsWalletBalancePaise` to the response.
  - **Web**: tenant detail page (`apps/web-app/app/super-admin/tenants/[id]`) gained an invoice list/create/mark-paid section and a wallet recharge form; new `app/super-admin/revenue` page for the summary dashboard.
  - Tests (`apps/api/test/billing.test.ts`, 5, all passing): invoice creation against a real Subscription, mark-paid reflected in revenue summary, wallet recharge credits + existing debit path still correct, platform-JWT-required check, and the usage-endpoint student-count fix. `apps/api` suite now **150 tests total** (up from 145), all passing. Root `pnpm typecheck` clean across all 11 packages.

- **Unit 31 — White-Label App Pipeline.** Completes Milestone 8.
  - **Honest scope-down per this unit's own Open Question 1**: `context/prerequisites.md` shows the Google Play developer account still pending verification and the Apple account not started — so per the spec's own instruction ("gated behind confirming the account/credential checklist is done... before writing code against real Apple/Google APIs"), no fake "real build succeeded" result was produced. `appbuild.stub` renamed to `appbuild.generate` (job name + payload type renamed throughout): it calls the real EAS Build REST API **only** when `EAS_ROBOT_ACCESS_TOKEN`/`EAS_PROJECT_ID` are configured (never — those credentials don't exist yet); otherwise it marks the `AppBuild` clearly `FAILED` with a logged reason, instead of a silent no-op that could be mistaken for "done." The EAS-configured code path itself has never run against a real token — flagged inline as unverified.
  - **Real bug found and fixed**: `apps/mobile/app.config.ts` had a hardcoded `applicationId`/`bundleIdentifier`/name (`com.vidyut.app` / "Vidyut") for every build — a direct violation of `architecture-context.md` §4/§5.1's non-negotiable Google Play rule that every white-label build needs a **unique** applicationId and app name per school (a shared id is a Repetitive Content policy violation). Fixed to read `VIDYUT_TENANT_SLUG`/`VIDYUT_APP_NAME` env vars (set per dedicated EAS build profile), falling back to the original shared identity when unset — shared-mode builds are unaffected.
  - **OTA channel wiring** (Open Question 3): `expo-updates` installed (`~0.25.28`, SDK 51-compatible), `app.config.ts` sets `updates.requestHeaders["expo-channel-name"]` to `"shared"` for shared-mode or `"dedicated-<tenantId>"` for dedicated-mode, driven by `VIDYUT_APP_MODE`/`VIDYUT_TENANT_ID`. New `eas.json` with `shared`/`dedicated` build profiles.
  - **Google Play compliance checklist** (`architecture-context.md` §4) — reviewed against the actual current pipeline, not assumed: unique applicationId now ✅ (fixed above); unique icon/screenshots/description generation and the store-listing-assets pipeline itself are **not built** (no automated listing generator exists) — explicitly out of scope per this unit's own "not a fully automated pipeline" allowance, but a real gap for whenever a dedicated build actually ships.
  - Tests (`apps/api/test/appbuild.test.ts`, 1, passing): starts a real worker, provisions a DEDICATED-plan tenant, and proves the resulting `AppBuild` settles on `FAILED` (not a silent stub-success) when EAS credentials aren't configured — matching this environment's real state. `apps/api` suite now **151 tests total** (up from 150), all passing. Root `pnpm typecheck` clean across all 11 packages.
  - **Correction to `feature-catalog.md`**: an earlier pass of this session's feature-status audit wrongly claimed no offline mobile store exists at all. It does — `apps/mobile/src/lib/database.ts` (Unit 16) is a real WatermelonDB local queue for attendance. Corrected in both Part I and the E3 section; the real gap is narrower than first stated — marks entry and homework posting aren't offline, attendance is.

- **Unit 32 — Offline Sync Hardening.** First unit of Milestone 9.
  - **Idempotent retry** (Open Question 1a): verified `POST /attendance`'s existing upsert-on-`(studentId,date)` semantics already make a full-batch retry safe — `withTenant()` wraps the whole batch in one Prisma `$transaction`, so a "partial commit" scenario can't actually happen at the DB layer; the real-world failure mode is the client never seeing a successful response and retrying the identical batch, which the existing unique constraint already handles correctly. New test proves it: submitting the same multi-student batch twice produces exactly one row per student, not duplicates.
  - **`?since=` delta param** (Open Question 2) added to `GET /attendance`, `GET /marks`, `GET /homework` — filters on `updatedAt >= since`, omitting it keeps full-fetch behavior. Tested against real before/after mutations.
  - **PUSH-then-SMS-fallback** (Open Question 3) in `students-absence-alert.ts` and `announcement-fanout.ts`: a guardian with a linked `User` (`Guardian.userId` set) gets PUSH; one without falls back to SMS via Unit 14's existing `SmsWallet`-gated debit path (reused, not duplicated). **Real gap fixed in `announcement-fanout.ts`**: phone-only guardians in a classId-audience were previously silently skipped entirely (never notified by any channel) — now they get SMS.
  - **Mobile "wire to `?since=`" scope note**: the teacher app's current architecture has no natural integration point for this — `TeacherAttendanceScreen` restores state from its local WatermelonDB store (Unit 16), not a server re-fetch, and `MarksEntryScreen`/`HomeworkPostScreen` are write-only screens with no repeated GET loop to wire a delta param into. The backend capability is real and tested; there's honestly nothing on the client side to hook it into yet, disclosed rather than forced.
  - **Correction to the audit performed after Unit 29**: this session's own earlier feature-status pass wrongly claimed the mobile app had no offline store at all — corrected in `feature-catalog.md` (see below).
  - **Real infra bug found and fixed**: the shared, Redis-backed `defaultRateLimiter` (300 req/min, IP-keyed) was tripping as a flaky 429 partway through a full `apps/api` test run once the growing suite's total request count crossed it within the run's window — a false failure, not a real rate-limit bug (confirmed by re-running in isolation). Fixed by raising the limit only under `NODE_ENV=test` (Vitest sets this automatically); production behavior unchanged, and the dedicated small-limiter test (`pipeline.test.ts`) uses its own isolated keyPrefix so it's unaffected.
  - Tests (`apps/api/test/offline-sync.test.ts`, 4, all passing: idempotent retry, `since=` on attendance, `since=` on homework, SMS fallback with wallet debit for a phone-only guardian) + updated `mobile-attendance.test.ts` (seeded a real `SmsWallet` since the absence-alert SMS fallback is now wallet-gated, matching Unit 14's existing pattern). `apps/api` suite now **155 tests total** (up from 151), all passing. Root `pnpm typecheck` clean across all 11 packages.

- **Unit 33 — Academic-Year Rollover.**
  - **`POST /academic/rollover/preview`** — proposes each currently-`ACTIVE`-enrolled student's default next class by `Class.order + 1` within the branch (`null` if already in the highest-order class — no default, staff must decide). Writes nothing, verified by a direct row-count check.
  - **`POST /academic/rollover/commit`** — `{ branchId, fromSessionId, toSessionId, decisions: [{studentId, action, targetClassId?, targetSectionId?}] }`. PROMOTE/REPEAT create a new `Enrollment` in `toSessionId`; WITHDRAW writes nothing; old session's rows are never touched. Flips `fromSessionId.isCurrent = false` / `toSessionId.isCurrent = true` only after every decision is processed.
  - **Real bug found and fixed during testing**: the original idempotency design (create, catch a `P2002` unique-constraint error on `Enrollment`'s existing `@@unique([studentId, sessionId])`, treat as `ALREADY_MIGRATED`) failed with a 500 on the second commit — `withTenant()` runs the whole commit in one Postgres transaction, and a failed statement poisons that transaction for every subsequent statement even when the JS `catch` handles it. Fixed to check-then-create (`findUnique` before `create`) instead of catch-after-fail — a real, non-obvious Postgres/Prisma interaction, caught by this unit's own idempotency test rather than shipped broken.
  - Gated `session.manage` (OWNER/PRINCIPAL/ADMIN per `rbac.md`), branch-scoped like every prior unit.
  - Tests (`apps/api/test/rollover.test.ts`, 5, all passing): preview writes nothing, commit creates exactly the decided rows for a mixed promote/repeat/withdraw fixture and flips `isCurrent`, old session's enrollments provably untouched, re-running commit is a clean no-op not a duplicate, RBAC (missing `session.manage` denied) + branch-scope (wrong-branch PRINCIPAL denied) as separate tenants (avoids a `Role` unique-constraint collision from creating the same role key twice in one tenant). `apps/api` suite now **160 tests total** (up from 155), all passing. Root `pnpm typecheck` clean across all 11 packages.

- **Unit 34 — Security & Isolation Pass.** An audit unit — verifies the whole system's isolation/RBAC invariants together, closes two genuinely new gaps.
  - **`withTenant()` static check** (`apps/api/src/core/checks/tenant-scope-check.ts` + `test/isolation-static.test.ts`): walks every `apps/api/src/modules/*/service.ts`, flags any direct `prisma.<model>.` call whose model isn't in a small platform-managed allow-list (`tenant`, `platformUser`, `plan`, `subscription`, `moduleToggle`, `smsWallet`, `appBuild`, `platformInvoice`, `walletTxn`). Passes clean on the real codebase; a fixture test proves it actually flags a deliberately-introduced unscoped query.
  - **RBAC coverage cross-check** (`apps/api/src/core/checks/rbac-coverage-check.ts`): cross-references every permission in `rbac.md`'s matrix against real `requirePermission("...")` route calls *and* inline `userHasPermission(auth, "...")` service checks (needed after finding Unit 28's dashboard gate uses the latter, not the former).
  - **Real gap found and fixed**: `subscription.view` (OWNER-only per `rbac.md`) was documented but enforced nowhere — no tenant-facing "view my subscription" endpoint existed at all. Added `GET /tenants/me/subscription` (gated `subscription.view`), returning the tenant's own active `Subscription` + `Plan`.
  - **Four confirmed real gaps left deliberately unbuilt**: `branch.manage`, `settings.manage`, `user.manage`, `role.manage` are documented in `rbac.md`'s matrix but **no branch-management, settings-management, or user/role-management API exists anywhere in the codebase** to enforce them against — not a checker bug, verified by direct grep. Building four new management modules is out of proportion for an audit unit ("an audit unit, not a new-feature unit" per this unit's own intro); tracked here as a real, load-bearing gap for a future unit rather than silently built now. The isolation-static test explicitly excludes these four with a named, commented allowlist rather than silently passing.
  - **Backup/restore drill** (`scripts/backup-restore-drill.sh`) — actually run once against the real local dev Postgres (via `docker exec` into the `schoolerp-postgres-1` container, not simulated): `pg_dump` the real `vidyut` DB, restore into a scratch `vidyut_restore_drill` DB, confirm 44/44 RLS policies and `FORCE ROW LEVEL SECURITY` settings survived the restore, and confirm an unscoped query as the non-superuser `vidyut_app` role returns **0 rows** on real data (1 row exists, confirmed via a superuser bypass query) — proving RLS isn't silently reopened by a restore. First attempt used `pg_restore --no-owner` and got a misleading "permission denied" (an ownership artifact, not an RLS finding); fixed by preserving ownership on restore, matching how a real recovery would run as the same `vidyut_app`-owning role.
  - **`GET /me/data-export`** (DPDP compliance) — self-scoped via the existing `resolveSelfStudentIds` resolver (Unit 24): a user's own profile + their own/linked children's student record, attendance, invoices, and payments. Verified a PARENT's export includes only their linked child, not an unrelated student in the same tenant.
  - Tests: `test/isolation-static.test.ts` (5), `test/security-pass.test.ts` (3, data-export scope + subscription-view RBAC). `apps/api` suite now **168 tests total** (up from 160), all passing. Root `pnpm typecheck` clean across all 11 packages.

- **Unit 35 — Launch Readiness.** Final unit of the v1 "Complete Core" build plan. An operational-checklist unit, not new product code — every item below has a concrete artifact, and unverifiable items say so explicitly rather than being marked done on faith (per this unit's own Open Question 1).
  - **Rich demo seed** (`packages/db/prisma/seed.ts`) — extended from a bare tenant/branch/session into a lived-in demo tenant: 2 classes, section 9-A, 4 subjects, 1 teacher (class teacher + a real login), 3 students (one linked to the existing demo PARENT login, two phone-only guardians to exercise Unit 32's SMS fallback on real data), a week of real attendance (with one deliberate ABSENT), an exam with marks for all 3 students, one published report card, a fee structure with a PAID invoice+receipt and a PENDING invoice (a realistic mixed state), one announcement, one BONAFIDE certificate, one timetable period, one homework item, one admission enquiry. Every write is upsert-or-find-then-create; **actually ran the seed script twice** and confirmed via a direct RLS-scoped query that row counts didn't double (3 students, 15 attendance records, 2 invoices, etc. — exactly once each).
  - **Sentry** (`apps/api/src/core/sentry.ts`, `apps/worker/src/sentry.ts`, `apps/web-app/sentry.{client,server}.config.ts`) — gated on `SENTRY_DSN`/`NEXT_PUBLIC_SENTRY_DSN` being set (a no-op otherwise, same optional-integration posture as this codebase's other external services), tenant-tagged (never leaks student/guardian PII into a shared Sentry project). **Honest gap**: no real Sentry project/DSN exists in this environment, so the "captures a deliberately-triggered error and it's visible in the Sentry project" DoD item could not be verified end-to-end. What *was* verified: `apps/api/test/sentry.test.ts` (2 tests) proves `captureError` is a true no-op with no DSN, and — with a mocked `@sentry/node` — correctly calls `captureException` with the right `requestId`/`tenantId` tags when a DSN *is* configured. The wiring is real and tested at the unit level; live capture into an actual Sentry dashboard is not.
  - **Structured logging** (`apps/api/src/core/logger.ts`) — one JSON line per request (`requestId`, `tenantId`, `method`, `route`, `statusCode`, `durationMs`), reusing Unit 04's `X-Request-Id` middleware. Worker logs equivalent fields per job via the same Sentry-tag pattern.
  - **CI** (`.github/workflows/ci.yml`) — lint, typecheck, test (including Unit 34's isolation/RBAC checks), build, against real Postgres+Redis services with the `vidyut_app` role created to mirror `docker/postgres-init`. **Honest gap**: never run on real GitHub Actions (no push access exercised this session) — the underlying commands were verified locally instead: `npx turbo run lint` (2/2 packages clean, two pre-existing `react-hooks/set-state-in-effect` errors fixed along the way), `npx turbo run typecheck` (9/9 clean), `npx turbo run build` (2/2 Next.js apps build clean), and `apps/api`'s test suite (170/170). Note: `pnpm run lint` specifically failed with a spurious "eslint not found" in this session's own shell (a local tool-wrapper artifact, not a real repo issue) — `npx turbo run lint` directly proved the underlying command is fine, so the CI workflow still calls `pnpm run lint` since a real GitHub Actions runner won't have that wrapper.
  - **`context/deployment-runbook.md`** (new) — a real, followable staging→prod path (build/push images, `prisma migrate deploy` against RDS, ECS rolling deploy, rollback). **Explicitly marked "written, NOT yet executed"** at the top of the file — `prerequisites.md` §3's AWS checklist is entirely unstarted (no AWS account exists), so there is no real staging environment to deploy to yet. This is the single largest honestly-disclosed gap in this unit.
  - Tests: `test/sentry.test.ts` (2). `apps/api` suite now **170 tests total** (up from 168), all passing. Root typecheck/lint/build clean across all packages (verified via `turbo` directly, see CI note above).

## Unit 35 — Final Launch-Readiness Checklist (`build-approach.md` §5, evidence-based)

- [x] **Fees**: structure → collect → receipt → dues → reminder → online payment ✅ all built and tested (Units 11–14). **Reconciliation ❌ not built** (confirmed absent in the feature-catalog audit) — this item is genuinely **partial**, not done.
- [x] Teachers mark **attendance offline** (real WatermelonDB local queue, Unit 16), parents get absence alerts (Unit 16, SMS-fallback-hardened in Unit 32) — done.
- [x] A full **term of exams → report cards** generated and published — done and now present in the rich demo seed as real, walkable data (Units 17–19).
- [x] **Admissions → student → fees** — `convertApplication` (Unit 10) creates the Student; fee assignment/invoicing are the existing Unit 11/12 flows against that Student. Works end to end via the API; not a single unified button.
- [x] **Academic-year rollover** tested (Unit 33, mixed promote/repeat/withdraw fixture, idempotency proven).
- [ ] **Mobile app live on Play; dedicated-app pipeline passes review** — ❌ **not done**. No Google Play developer account exists yet (`prerequisites.md`), no real EAS build has ever run (Unit 31's own honest disclosure). This is a real blocker to actual launch, not a documentation gap.
- [x] Super-admin can **onboard a tenant, set plan/modules, bill, provision the app** — done and tested (Units 05, 30; Unit 31's app-build trigger is real plumbing, gated on the same missing Play account above for the dedicated-mode path specifically).
- [~] **Data import** works on a real school's messy Excel — the import pipeline exists and is tested (Unit 07, synthetic xlsx/csv fixtures) but has never been run against an actual messy real-world spreadsheet — **unverified, not failed**. **Data export (DPDP)** ✅ done and tested (Unit 34).
- [x] **Tenant-isolation tests** pass (every unit) + **backup/restore drill done** — actually run once against real local Postgres (Unit 34), not simulated.
- [ ] **Hindi/Hinglish UI complete across core flows; SMS/WhatsApp templates DLT-approved** — ❌ **not done**. i18n infrastructure exists (Unit 01, English+Hindi locale files) and is used throughout, but coverage has not been audited flow-by-flow for completeness, and **no real SMS/WhatsApp provider is integrated at all** — every notification send in this codebase is a stubbed `console.log` (documented honestly since Unit 14). DLT template approval requires a real MSG91/Gupshup account and real template submissions — entirely unstarted.
- [x] **Owner dashboard** shows collection %, dues, attendance, admissions — done and tested (Unit 28).

**Verdict: the v1 "Complete Core" is code-complete and internally consistent (170/170 tests, clean lint/typecheck/build) but is NOT yet sales-ready.** Three items are real, unmet blockers to an actual launch — not documentation gaps, not things this session could have built without external accounts: (1) no Google Play developer account → no live app, no dedicated-build pipeline proof, (2) no real SMS/WhatsApp provider → every parent-facing notification is currently a stub, (3) no AWS account → the deployment runbook has never been executed against a real environment. All three require the user to complete real-world account setup (`prerequisites.md`) before they're buildable/verifiable further — flagged explicitly here rather than claimed done.

## Milestone 11 — Implementing the Remaining Feature Specs (Units 36–69)

Started 2026-07-28, one unit at a time per `AGENTS.md` §7's workflow, against the specs drafted in Milestone 10+ above.

- **Unit 36 — Platform Configuration (Settings, Branch, User & Role Management).** Closes Unit 34's 3 confirmed core RBAC gaps (`settings.manage`, `branch.manage`, `user.manage`/`role.manage`).
  - **Schema**: `Tenant.address/logoUrl/admissionNoPrefix/invoiceNoPrefix` (group-wide profile — per-branch address/board/logo already existed on `Branch`). `nextAdmissionNo`/`nextInvoiceNumber` (`packages/db/src/generate-*.ts`) now read the tenant's prefix (falling back to no prefix / `INV-` respectively, so existing behavior is unchanged when unset).
  - **`PATCH /tenants/me/profile`** (`settings.manage`) — added alongside the existing `GET /tenants/me/subscription` in the `tenants` module; `Tenant` is a platform-managed no-RLS table (data-model.md §13), scoped explicitly by `auth.tenantId`, same posture as that existing endpoint.
  - **`POST/PATCH /academic/branches`, `GET /academic/branches`** (`branch.manage`) — added to the existing `academic` module (Branch is tenant-owned/RLS-scoped, unlike Tenant). No `requireBranch` on create (no existing branchId to check yet); `branch.manage` is OWNER-only per `rbac.md`'s default grid, so `requirePermission` alone is the correct gate. Branches are never deleted, only deactivated (`isActive`), matching every other soft-delete convention in this codebase.
  - **`POST /users/invite`, `GET /users`, `PATCH /users/:id`** (`user.manage`) — new `users` module, deliberately thinner than Unit 09's `createStaff` (no Staff HR profile): just `User` + `UserRole` + `BranchMembership`. Mirrors Unit 08's guardian-invite pattern — a temp password is generated, hashed, and the "send" step is a real background job (`staff.invite`, new `JobName` + worker processor `apps/worker/src/processors/staff-invite.ts`), honestly stubbed (clearly logged, not a faked send) since no real email provider exists yet (same gap as Unit 40).
  - **`POST /roles`, `GET /roles`, `PATCH /roles/:id/permissions`** (`role.manage`) — new `roles` module for **custom, non-system roles only**; `Role.isSystem` rows reject a permission-edit attempt (tested). **Real architectural constraint found and disclosed, not papered over**: `Role.key` is a closed `RoleKey` enum, and `RequestAuth.roles`/JWT claims/`requirePermission` are all built around matching that enum — supporting arbitrarily-named custom roles would mean re-keying `Role` on a free string and touching the JWT shape across ~5 modules, out of proportion for one line item in this unit. **Scoped-down, honest fix**: added one new enum value `RoleKey.CUSTOM` (`ponytail:` comment on `packages/types/src/roles.ts` naming the limit + upgrade path) — every tenant gets **at most one** custom role today, created/edited entirely through the existing enum-matching permission-check code with zero changes to auth/JWT. A second `POST /roles` for the same tenant is rejected `CONFLICT`. `seedDefaultRoles` explicitly excludes `CUSTOM` from auto-seeding (created on demand by the tenant itself).
  - **Web**: new `/settings` page (`apps/web-app/app/(school)/settings/page.tsx`) — School Profile / Branches / Staff & Roles tabs (shadcn `Tabs`), reusing Unit 27's shell/table/form pattern; sidebar nav gated to OWNER/PRINCIPAL. Custom-role permission checkboxes render the same `PERMISSIONS` catalog as the backend (`@vidyut/types`, added as a real dependency — not a duplicated hardcoded list).
  - Tests (`apps/api/test/platform-configuration.test.ts`, 8, all passing): profile edit + RBAC-denied, branch create/list/deactivate + RBAC-denied, staff invite + duplicate-email conflict + deactivate, custom-role create/edit-permissions/second-custom-rejected/system-role-immutable/RBAC-denied. `apps/api` suite now **178 tests total** (up from 170), all passing. Root `pnpm typecheck`/lint/build clean across all 11 packages (incl. the new `apps/web-app` build with the `/settings` route).

- **Unit 37 — Global Search.** A cross-cutting, low-risk unit — no new business logic, one aggregate read path over models with their own existing list/filter endpoints.
  - **Migration** (`20260728000000_search_pg_trgm`, raw SQL — not modeled in `schema.prisma`, same pattern as the RLS migrations): `CREATE EXTENSION pg_trgm` + GIN trigram indexes on `Student.firstName/lastName/admissionNo`, `User.name`, `Invoice.number`. Applied to both the dev and test databases.
  - **`GET /search?branchId=&q=`** (new `search` module) — three parallel branch-scoped queries (`contains` + `mode: "insensitive"`, i.e. `ILIKE`, which the new GIN indexes accelerate), each capped at 5 results. **Reuses existing permissions, no new one** per this unit's own Scope: student results gated `student.view`, invoice results gated `fee.view`, staff results ungated (matching `staff/routes.ts`'s own already-ungated directory-read posture). A category the caller lacks permission for comes back as an empty array rather than a 403 — lets one endpoint serve every role without the frontend needing to know who can see what.
  - **Web**: `apps/web-app/components/global-search.tsx` — a header search input + results dropdown (Unit 27's shell), wired into `AppTopbar`. Student results link to the existing student detail page; staff results render as plain (non-clickable) rows and invoice results link to the fees list, since no staff-detail or per-invoice-detail page exists yet in the web admin (Unit 27's own disclosed gap, not something this unit invents a page to cover).
  - Tests (`apps/api/test/search.test.ts`, 4, all passing): finds a student/staff/invoice by partial match, a missing-permission category comes back empty rather than erroring, tenant-isolation (same admission number in two tenants, only the caller's own tenant's student returned), branch-scope (PRINCIPAL on a different branch denied). `apps/api` suite now **182 tests total** (up from 178), all passing. Root `pnpm typecheck`/lint/build clean across all 11 packages.

- **Unit 38 — Fee Reconciliation & Receipt Corrections.** Closes two confirmed B1 gaps: matching gateway/bank settlements to receipts, and an audited receipt cancellation/correction flow.
  - **`GET /fees/reconciliation?branchId=&date=`** (new `reconciliationRouter`, mounted at `/api/v1/fees/reconciliation`, gated `fee.reports`) — splits a day's `SUCCESS` payments into `online` (CARD/UPI/NETBANKING/WALLET) vs `counter` (CASH/CHEQUE/DD/BANK), per this unit's own Open Question 1 scope-down (no bank-statement-import engine — real fintech infra this stage doesn't justify). Each online payment is flagged `needsReview: true` when it has no `gatewayOrderId` — a genuine data-quality signal (it means the payment was recorded manually in an online-capable mode without ever going through Unit 13's real gateway/webhook flow), not a placeholder always-true flag.
  - **Real bug found and fixed by this unit's own test**: the date-range filter originally used `Date.setHours(0,0,0,0)`/`setDate()`, which mutate in the **server's local timezone** — on this dev machine (Asia/Calcutta, UTC+5:30), that silently shifted the UTC query window back by 5.5 hours, missing same-day payments. Fixed to pure epoch-millisecond arithmetic (`query.date` is already UTC midnight from `z.coerce.date()` on a plain `"YYYY-MM-DD"` string) — never call a local-time mutator on a value meant to represent a UTC day boundary.
  - **`PATCH /receipts/:id/cancel`** (new `receiptsRouter`, gated `fee.refund` — reused, no new permission per Open Question 2) — sets `Receipt.cancelledAt`/`cancelReason` (columns Unit 12 added but nothing ever wrote to), requires a reason, writes an `AuditLog` row, and **never** touches `Payment`/`Invoice` status (a cancelled receipt with a still-successful payment is deliberately left for a human to resolve via the existing refund flow, matching `RefundRequest`'s own human-approval-gated pattern). Idempotent: cancelling an already-cancelled receipt is a clean no-op (keeps the original reason, writes no second `AuditLog` row) rather than erroring.
  - **Web**: `apps/web-app/app/(school)/fees/page.tsx` gained an Invoices/Reconciliation tab split (shadcn `Tabs`, matching Unit 36's `/settings` pattern); the Reconciliation tab shows a date picker + online/counter payment tables with a "Cancel receipt" action per row. No dedicated receipt-detail page exists in the web admin yet (Unit 27's own disclosed gap), so the cancel action lives directly on the reconciliation row rather than a page this unit doesn't otherwise need.
  - Tests (`apps/api/test/fee-reconciliation.test.ts`, 3, all passing): online/counter split + needs-review flag (the test that caught the timezone bug above), cancel requires a reason + is audited + is idempotent + never touches `Payment.status`, RBAC (`fee.reports` denied) + branch-scope (wrong-branch `ACCOUNTANT` denied). `apps/api` suite now **185 tests total** (up from 182), all passing. Root `pnpm typecheck`/lint/build clean across all 11 packages.

- **Unit 39 — DPDP Consent, Retention & Delete-on-Request.** The other three DPDP pillars (export was already done, Unit 34). **Explicitly not a legal sign-off** — this unit builds the mechanism; `context/dpdp-policy.md` (new) states the retention approach and lists exactly what still needs real legal review before this is DPDP-compliant in practice (consent text, confirmed retention windows, whether anonymize-in-place satisfies the erasure requirement).
  - **Schema**: `Guardian.consentedAt` (set only from a real confirmed invite-time checkbox, never backfilled); `DataDeletionRequest` (`requestedById`, `status: PENDING|REJECTED|EXECUTED`, `reviewedById/At`, `reviewNote`, `executedById/At`).
  - **`POST /guardians/:id/invite`** now requires `consent: true` in the body (`inviteGuardianSchema`, Zod-refined so `false` or a missing field both 400) before creating the guardian's login; sets `consentedAt`.
  - **`POST /me/data-delete-request`** (self-scoped, `me` module) creates a `DataDeletionRequest` — never deletes anything itself. **`GET/PATCH/POST /data-deletion-requests`** (new `dpdp` module, gated `settings.manage`, reused rather than inventing a new permission per Open Question 3) — OWNER lists pending requests, rejects with a required note, or executes. Execution is deliberately narrow: it anonymizes only the requester's own `User`/linked `Guardian` identity fields (name/phone/email/password) and **never touches** the student's attendance/marks/fee/payment history — those remain the school's own business records, matching `dpdp-policy.md`'s retention reasoning. `execute` additionally requires the caller to actually hold the `OWNER` role (checked in the service, not just the permission grid) since it's the one destructive action in the pipeline.
  - **Real cross-tenant data leak found and fixed by this unit's own tenant-isolation test**: `DataDeletionRequest` was added to `schema.prisma` and given `tenantId`, but no RLS-enabling migration was written for it — `withTenant()` alone does nothing without a Postgres RLS policy on the table, so an OWNER in tenant A could read every tenant's deletion requests. Caught immediately by the isolation test (expected 0 rows, got 3), fixed with a new migration (`enable_rls_data_deletion_request`, same `ENABLE/FORCE ROW LEVEL SECURITY` + `CREATE POLICY tenant_isolation` pattern as every other tenant-owned table) applied to both the dev and test databases. A reminder that adding a new Prisma model is two steps in this codebase — the model *and* its RLS migration — not one.
  - **Web**: a fourth "Data requests" tab on `/settings` (reuses Unit 36's tab shell) — OWNER sees pending requests with Reject (prompts for a note) / Execute (confirms, since it's irreversible) actions.
  - Tests (`apps/api/test/dpdp.test.ts`, 4, all passing): consent required + sets `consentedAt`, full self-request → OWNER-reject-with-note and separately self-request → OWNER-execute-anonymizes-identity-fields-only (+ a non-OWNER is denied, + re-deciding an already-decided request is rejected `CONFLICT`), reject requires a note and never touches the user, tenant-isolation (the bug above). Also updated the pre-existing `guardians.test.ts` invite test to send `consent: true` (a real behavior change, not a broken test) and assert `consentedAt` is set. `apps/api` suite now **189 tests total** (up from 185), all passing. Root `pnpm typecheck`/lint/build clean across all 11 packages.

- **Unit 40 — Real Notification Providers (SMS/WhatsApp/Push/Email) + Inbox + Templates/DLT.** The single most-flagged gap across every prior unit's honest disclosures, finally closed at the code level (still not verifiable end-to-end — no real provider account exists in this environment, per this unit's own Open Question 1, same posture as Unit 31's EAS gap).
  - **Schema**: `NotificationTemplate` (`templateKey`, `channel`, `dltId?`, `body`) — the lookup real sends consult instead of hardcoded strings; `dltId` stays null until the user has actually registered a DLT-approved template (a legal/telecom process, Open Question 2, this repo can't self-certify). `NotificationLog.readAt` (inbox read state). `Announcement.scheduledFor?` (Open Question 4). `User.pushToken?` — a genuinely necessary addition found while building this unit: without a device-token field, a "real" push send has nothing to target regardless of how the FCM call is wired, so `sendPush` treats a missing token as a real "can't send" signal, not just "not configured."
  - **Provider adapters** (`apps/worker/src/providers/{sms,whatsapp,push,email}.ts`) — MSG91 (SMS), Gupshup (WhatsApp template messages), FCM legacy HTTP API (push, keyed off `pushToken`), SES v2 (`@aws-sdk/client-sesv2`, new dependency, same posture as the existing S3 wrapper). Each gated on its own env var(s) (`MSG91_API_KEY`, `GUPSHUP_API_KEY`, `FCM_SERVER_KEY`, `SES_FROM_ADDRESS`), falling back to the same clearly-logged stub every prior unit used — never a faked success. A shared `resolveTemplateBody()` looks up the tenant's `NotificationTemplate`, falling back to a hardcoded default string when none is configured, so behavior never breaks for a tenant with no templates set up.
  - **Wired into the three existing stub processors** (`fees-reminder-send.ts`, `students-absence-alert.ts`, `announcement-fanout.ts`) in place of `console.log`. **Real, pre-existing bug fixed along the way**: PUSH sends were unconditionally logged `status: "SENT"` for any guardian/staff member with a linked `User` account — with no device-token concept in the schema at all, this was always a lie regardless of provider wiring. Now gated on `pushToken` presence; no token means an honest, immediate stub-fallback (still logged `SENT` when unconfigured, preserving this unit's own DoD: "with no provider env vars set, every send path behaves exactly as today").
  - **`Announcement.scheduledFor`** (Open Question 4) — `createAnnouncement` now computes a BullMQ `delay` from `scheduledFor - now()` and passes it to the existing `enqueue()` call; the fan-out processor itself is unchanged. Verified with a real delayed job in the test suite (not mocked), including the row genuinely not existing yet immediately after creation.
  - **`NotificationTemplate` CRUD** (`POST/GET/PATCH /notifications/templates`, gated `notification.send`) and **in-app inbox** (`GET/PATCH /me/notifications[/:id/read]`, self-scoped by `toUserId`) and **push-token registration** (`PATCH /me/push-token`, self-scoped) — new endpoints in the `notifications` and `me` modules.
  - **Web**: `/notifications` inbox page (bell icon in `AppTopbar`, linking there) listing the caller's own notifications with a mark-read action.
  - Tests (`apps/api/test/notifications-providers.test.ts`, 4, all passing): template create/list/patch/duplicate-conflict/RBAC-denied, inbox self-scope (a second user gets 404 trying to mark someone else's notification read) + idempotent mark-read, push-token registration, and a genuinely delayed scheduled-announcement fan-out (asserted empty immediately after creation, non-empty after the real delay elapses — not simulated). Also updated `test/helpers.ts`'s shared `cleanupTenant()` to delete `NotificationTemplate` rows (a second real FK-constraint gap found the same way as Unit 39's — every new tenant-owned model needs both an RLS migration *and* a cleanup-helper entry, not just the model itself). `apps/api` suite now **193 tests total** (up from 189), all passing. Root `pnpm typecheck`/lint/build clean across all 11 packages.

- **Unit 42 — Staff HR Depth.** Closes A4's remaining gaps: document storage, staff attendance, staff ID cards. Recruitment/onboarding and appraisal explicitly deferred (Open Question 2) — real HR-process features with no source-of-truth process document; feature-catalog.md already tags them P3.
  - **Schema**: `Certificate.studentId` made nullable + `Certificate.staffId` added (Open Question 1) — exactly one of the two is enforced at the app layer (Zod `.refine`), not a DB constraint Postgres can express cleanly for an XOR across two nullable FKs. `StaffAttendanceRecord` mirrors `AttendanceRecord`'s shape (no `sectionId`/`sessionId` — staff attendance isn't tied to a class section), same `@@unique([staffId, date])` upsert-on-remark pattern.
  - **`POST /staff/:id/documents`** — presigned upload via Unit 04's existing S3 wrapper (`getUploadUrl`), same two-step pattern as Unit 07's bulk-import upload: generates a `fileKey`, returns the signed URL, and appends `{key, label}` to `Staff.docs` JSONB immediately (the client uploads directly to the bucket).
  - **`POST/GET /staff/attendance`** — reuses the exact same upsert-on-`(staffId,date)` pattern as student attendance (Unit 15), gated by the existing `attendance.mark`/`attendance.view` permissions (no new permission). **Route-ordering note**: `/staff/attendance` had to be registered *before* the existing `/staff/:id` routes, since Express matches routes in registration order and `/:id` would otherwise swallow `/attendance` as a param value — a real, easy-to-miss ordering bug caught before it shipped, not found by a test.
  - **Staff ID cards** — `POST /certificates` now accepts either `studentId` or `staffId`, reusing the exact same register/numbering machinery (`nextCertificateNumber`) rather than a parallel `StaffCertificate` model; `CertificateType.ID_CARD` already existed in the enum from Unit 21, so no enum change was needed, only the FK.
  - Tests (`apps/api/test/staff-hr-depth.test.ts`, 3, all passing): document upload returns a real presigned URL and appends (not overwrites) `Staff.docs`, staff attendance mark/upsert-on-remark/list/RBAC-denied (parity with student attendance), staff ID card issuance sharing the register with student certificates but never colliding on certificate number, and the exactly-one-of-studentId/staffId validation (both or neither rejected `400`). Updated `test/helpers.ts`'s `cleanupTenant()` to delete `StaffAttendanceRecord` rows before `Staff` (a third instance of the same FK-constraint-on-cleanup gap as Units 39/40 — now a well-established checklist item for any new tenant-owned model: RLS migration + cleanup-helper entry, not just the Prisma model). `apps/api` suite now **196 tests total** (up from 193), all passing. Root `pnpm typecheck`/lint/build clean across all 11 packages.

- **Unit 43 — Academic Structure Depth (Streams, Electives, Houses).** Streams (Open Question 1) confirmed directly with the user before writing any code: a separate `Class` row per stream (e.g. "Class 11 Science"/"Class 11 Commerce"), already representable today — **zero new schema**, no `Stream` model added, matching the recommendation exactly.
  - **Schema**: `ClassSubject.electiveGroupId` (nullable — an elective subject optionally belongs to one named basket), `ElectiveGroup` (`branchId`, `classId`, `name`), `StudentElectiveChoice` (`@@unique([studentId, electiveGroupId])` — one pick per basket, upsert on re-pick). `House` (`branchId`, `name`, `color?`) + `Student.houseId` (nullable) — simple tagging, no scoring engine (Open Question 2 — that's D6 Discipline territory, separately deferred).
  - **`POST /academic/elective-groups`**, **`POST /elective-groups/:id/options`** (attaches an existing `ClassSubject` to the basket), **`POST /elective-groups/:id/choice`** (a student's pick — validated to actually belong to that group, rejected `400` otherwise) — all in the existing `academic` module, gated by the already-existing `class.manage`/`subject.manage` permissions, no new permission. **`POST/GET /academic/houses`**, **`GET /houses/:id/roster`** — same pattern. House assignment reuses the existing `PATCH /students/:id` endpoint (`houseId` added to `patchStudentSchema`) rather than a separate endpoint.
  - Tests (`apps/api/test/academic-structure-depth.test.ts`, 3, all passing): full elective flow (create basket → attach two options → student picks one → re-pick upserts, never duplicates → picking an option not in the group is rejected `400`), house tagging + roster listing, tenant-isolation (a house created in tenant A is invisible to tenant B). Updated `test/helpers.ts`'s `cleanupTenant()` for the three new models, careful about FK order: `StudentElectiveChoice` before `ClassSubject`/`Student`, `ElectiveGroup` after `ClassSubject` (since `ClassSubject.electiveGroupId` references it), `House` after `Student` (since `Student.houseId` references it) but before `Branch`. `apps/api` suite now **199 tests total** (up from 196), all passing (one unrelated pre-existing flake in `auth-flows.test.ts`'s 2FA test reproduced once under full-suite load, passed cleanly in isolation and on rerun — a timing sensitivity, not a regression from this unit). Root `pnpm typecheck`/lint/build clean across all 11 packages.

- **Unit 44 — Attendance Depth (period-wise, device-scan, analytics).** Both Open Questions resolved by direct engineering judgment (squarely within the spec's own stated reasoning, not a business-process ambiguity like Unit 41's): a generic vendor-agnostic device-scan endpoint (Open Question 1) and reusing `AttendanceRecord` with a nullable `periodId` rather than a parallel model (Open Question 2).
  - **Schema**: `AttendanceRecord.periodId?` (→ `TimetablePeriod`) — daily attendance keeps it `null`, period-wise attendance sets it. Its old `@@unique([studentId, date])` doesn't survive adding a nullable dimension cleanly in Prisma's schema DSL (Postgres treats `NULL` as distinct per row, so a plain compound unique would silently stop deduplicating daily marks), so it's replaced by **two hand-written partial unique indexes** in a raw-SQL migration: one on `(studentId, date) WHERE periodId IS NULL` (daily), one on `(studentId, date, periodId) WHERE periodId IS NOT NULL` (period-wise). The application layer does its own find-then-create/update instead of relying on Prisma's `.upsert()`, since Prisma can't target a partial index as a `where` clause. `AttendanceRecord.markedById` made nullable (device-scan records have no authenticated user behind them). `Branch.attendanceDeviceToken?` (`@unique`) — a device authenticates with this instead of a user JWT.
  - **`POST /attendance/device-token/:branchId`** (`branch.manage`, same admin tier as Unit 36's other branch endpoints) — (re)generates the token, returned once like an API key. **Real design snag found and fixed while building this**: `Branch` is RLS-scoped, so a bare `prisma.branch.findUnique()` with no tenant context set (which is exactly the situation the device-scan endpoint is in — it doesn't know the tenant yet, that's what it's trying to resolve) silently returns zero rows rather than erroring, since RLS's `USING` clause treats a missing `current_setting('app.tenant_id', true)` as "match nothing." Fixed by embedding the tenantId as a plaintext, colon-free prefix on the token itself (`${tenantId}.${randomSecret}`) — the request tells us which tenant's RLS context to open before we look anything up, then the actual secret suffix is verified inside that tenant's own scope. Same shape as Unit 13's Razorpay webhook, which resolves tenantId from its own `notes.tenantId` payload field for the identical reason.
  - **`POST /attendance/device-scan`** — public, no `authGuard` (mounted at `/api/v1/attendance/device-scan`, registered *before* `attendanceRouter`'s `/api/v1/attendance` mount in `app.ts`, since Express matches `app.use` prefixes in registration order and both share the same path prefix). Resolves the student by `admissionNo` + branch, the section from their current enrollment, and upserts a `BIOMETRIC`-sourced daily (`periodId: null`) record — vendor-agnostic per the spec's own recommendation; no ESSL/Mantra SDK wired, since there's no real device to test against.
  - **`GET /attendance/analytics?branchId=&classId?&from=&to=&minAbsences=`** — a real day-by-day attendance-percent trend plus a chronic-absentee list (`>= minAbsences` in the window), both computed from the same `PRESENT_STATUSES` constant `getDefaulters` already used (factored out so the "present" definition lives in exactly one place, per the spec's own "reuse Unit 15's percent logic" instruction).
  - Existing `POST/GET /attendance` gained an optional `periodId` (mark and filter respectively) — no new endpoints needed for period-wise marking, satisfying the spec's "period-aware mark/list variant" by extending the existing ones.
  - Tests (`apps/api/test/attendance-depth.test.ts`, 3, all passing): period-wise and daily records coexist for the same student/day without colliding, re-marking a period upserts rather than duplicating, filtering by `periodId` returns only that row; device-scan creates then updates (not duplicates) a daily record across two scans the same day, and rejects a malformed/wrong token with `401` before touching the database; analytics returns a genuine 3-day trend (50% each day, verified numerically) and correctly isolates the one chronic absentee from the one regular student. `packages/db/prisma/seed.ts`'s attendance-seeding loop rewritten from `.upsert()` (which used the now-removed `studentId_date` compound key) to the same manual find-then-create pattern as the service. `apps/api` suite now **202 tests total** (up from 199), all passing. Root `pnpm typecheck`/lint clean across all 11 packages.

- **Unit 45 — Homework Depth (Submission, Grading, Calendar).** Both Open Questions were already answered by the spec itself (reuse Unit 04's S3 wrapper; keep grading separate from `MarksEntry` since exam marks are a different concept) — nothing to resolve before coding.
  - **Schema**: `HomeworkSubmission` (`homeworkId`, `studentId`, `fileUrl`, `submittedAt`, `grade?`, `feedback?`, `gradedById?`, `gradedAt?`), `@@unique([homeworkId, studentId])` — one submission per student per homework, upsert on resubmission (same shape as every other per-day/per-choice table this session: `AttendanceRecord`, `StaffAttendanceRecord`, `StudentElectiveChoice`).
  - **`POST /homework/:id/submissions`** — no `requirePermission` gate at all; a PARENT/STUDENT caller is authorized entirely by `assertOwnStudent()` inside the service (rbac.md rule 5: self-scoped roles bypass the permission grid), reusing Unit 24's `resolveSelfStudentIds`. Same two-step presigned-upload pattern as Unit 42's staff documents: returns `{ uploadUrl, ...submission }`, client uploads directly to the bucket. Resubmitting the same homework upserts (clears any prior grade — a new file means the old grade no longer applies to what's actually there).
  - **`GET /homework/:id/submissions`** and **`PATCH /homework/submissions/:id`** (grading) — both gated `homework.manage`, reusing the exact same `assertCanManageSection()` a TEACHER/PRINCIPAL already had to pass to create the homework in the first place (Unit 23). **Test-writing note**: `homework.manage` is PRINCIPAL/TEACHER-only per Unit 23's own RBAC test — OWNER is deliberately denied, so this unit's tests use a PRINCIPAL token for grading/listing, not OWNER (would have been a false-negative 403 if reused blindly from other units' owner-centric test style).
  - **`GET /me/homework/calendar?studentId=&month=&year=`** — same underlying query as the existing `GET /me/homework`, grouped by due-date day (`Record<string, Homework[]>` keyed by day-of-month), mirroring the exact bucketing pattern Unit 15's attendance register already used for its day-keyed grid.
  - Tests (`apps/api/test/homework-depth.test.ts`, 2, all passing): a parent submits against their own child only (a wrong `studentId` is rejected `403` before any storage call), resubmission upserts rather than duplicating (verified by row count and updated `fileUrl`), a PRINCIPAL lists and grades the submission, and a PARENT attempting to grade is denied `403` (permission-gated, not self-scoped — the two are deliberately different authorization mechanisms on sibling endpoints); calendar groups two same-day submissions together and excludes a third due the following month. Updated `test/helpers.ts`'s `cleanupTenant()` to delete `HomeworkSubmission` rows before `Homework`/`Student`. `apps/api` suite now **204 tests total** (up from 202), all passing. Root `pnpm typecheck`/lint clean across all 11 packages.

- **Unit 46 — Exams & Report Cards Depth.** The largest remaining depth unit (A8 had the most unbuilt rows of any P0 module). Open Question 1 (online exam scope) taken exactly per the spec's own recommendation — MCQ-only, a genuinely separate `OnlineExam`/`OnlineExamQuestion`/`OnlineExamSubmission` model, not a repurposing of `Exam`/`MarksEntry`. Open Question 3 (board-specific report-card layout) is the one piece **deliberately deferred**, same posture as Unit 41 — it explicitly needs a real board report-card sample confirmed with the user before locking a JSON schema for `ReportCardTemplate.layout`, not an engineering judgment call.
  - **Schema** (new file `exam-depth.prisma`, kept separate from the Unit 17 `exam.prisma` it builds on): `ExamTimetable` (`examId`, `subjectId`, `date`, `startTime`, `room?`, `@@unique([examId, subjectId])`) — an exam-specific datesheet, distinct from Unit 22's recurring weekly `TimetablePeriod`. `CoScholasticGrade` (`examId`, `studentId`, `activity`, `grade`, `@@unique([examId, studentId, activity])`, upsert-on-remark) — discipline/activity grades alongside `MarksEntry`, never conflated with it (no `maxMarks`/scholastic concept here). `QuestionBankItem` (`classId`, `subjectId`, `questionText`, `options: Json`, `correctOptionIndex`) — reusable MCQs, sequenced after `OnlineExamQuestion` existed first per the spec's own dependency note. `OnlineExam`/`OnlineExamQuestion`/`OnlineExamSubmission` — MCQ-only, `OnlineExamSubmission.answers` is a JSON array of selected option indices positionally matched to question `order`; `@@unique([onlineExamId, studentId])` — one shot per student, enforced at the DB level too, not just app logic.
  - **`GET /exams/:id/results/rank`** — computed entirely from existing `MarksEntry`/`ExamSubject` rows (no new input data, per the spec's own scope #3): sums each student's obtained/max marks across every `ExamSubject` in the exam, sorts by percent, assigns rank by sort position.
  - **`GET /students/:id/transcript`** — a multi-session `ReportCard` rollup, filtered to `publishedAt IS NOT NULL` only (an unpublished/draft report card must never appear on a transcript), ordered by session then exam start date.
  - **Online exams** (`apps/api/src/modules/online-exams/`, new module): `POST /online-exams` (create, unpublished by default) → `POST /online-exams/:id/questions` (direct) or `POST /online-exams/:id/questions/from-bank` (**copies** the bank item's content into a new row rather than referencing it, so a later bank edit can never silently change a question already in a published exam) → `PATCH /online-exams/:id/publish` (locks question-adding — `addOnlineExamQuestion*` both reject once `isPublished`) → student-facing `GET /online-exams/:id/take?studentId=` (self-scoped, answer key stripped — a separate code path from the staff-facing `GET /online-exams/:id/questions`, which does include it) → `POST /online-exams/:id/submit` (self-scoped via the same `assertOwnStudent`/`resolveSelfStudentIds` pattern Unit 45's homework submission established, plus an enrollment check confirming the student is actually in the exam's class — auto-grades by summing per-question marks where the submitted index matches `correctOptionIndex`, rejects a second submission with `409`).
  - **Question bank** (`apps/api/src/modules/question-bank/`, new module): `POST/GET /question-bank`, filterable by class/subject, gated `exam.manage`.
  - **Test-writing note (reused from Unit 45's lesson)**: `exam.manage` and `marks.enter` both allow OWNER when explicitly granted in a test's role (unlike `homework.manage`, which only bypasses for PRINCIPAL) — this unit's tests use OWNER throughout, matching Unit 17/18's own existing test convention.
  - Tests (`apps/api/test/exam-report-depth.test.ts`, 3, all passing): exam datesheet create+list, rank correctly orders a topper above an average student with the right percent/rank numbers, co-scholastic grade upsert-on-remark (re-entering the same activity updates, doesn't duplicate); transcript returns only the published report card of two (one deliberately left unpublished) across the rollup; the full online-exam lifecycle — create → add a direct question → add a second from the question bank (verifying the bank's answer key was copied correctly) → submission rejected before publish (`400`) → publish → self-scoped `take` view hides the answer key → an unenrolled student's submission is rejected (`403`) → a correct/incorrect mixed submission auto-grades to the exact expected score → a second submission attempt is rejected (`409`) → the teacher's submissions list shows the graded score. Updated `test/helpers.ts`'s `cleanupTenant()` for all six new models in FK-safe order (submissions/questions before their parent `OnlineExam`, before `Class`/`Subject`; co-scholastic/exam-timetable before `Exam`/`Student`). `apps/api` suite now **207 tests total** (up from 204), all passing. Root `pnpm typecheck`/lint clean across all 11 packages.

- **Web UI catch-up for Units 42/43/44/46.** User feedback partway through this milestone: Units 42-46 had all shipped API + schema + tests with **no corresponding web screen**, an inconsistency versus Units 36-40 (which did get screens). Rather than let the gap keep growing through the rest of the 47-69 run, went back and built web UI for the four units that needed one (Unit 45/Homework was correctly API-only — it's mobile-first, no admin web screen exists or is warranted for it, matching the project's own web=admin/mobile=parent-teacher-student split).
  - **`/staff` (Unit 42)** — new nav entry + page, tabs: Staff List (real data from `listStaff`), Documents (presigned-upload flow: request URL → `fetch(uploadUrl, {method:"PUT"})` direct-to-bucket, mirroring Unit 07's bulk-import upload pattern), Attendance (date + staffId/status rows, mirroring the existing student-attendance page), ID Card (issue + show the returned certificate number).
  - **`/academic-structure` (Unit 43)** — new nav entry + page, tabs: Electives (create basket → add option → student choice, three thin forms since there's no picker UI anywhere in this codebase yet — every ID field is a plain text input, consistent with the existing attendance/students pages' own style) and Houses (create/list + roster viewer).
  - **`/attendance` (Unit 44)** — existing page restructured from two stacked sections into four tabs (Register, Defaulters — both pre-existing — plus new Analytics and Device tabs). Analytics tab: from/to date range → trend table + chronic-absentee table. Device tab: one rotate button, shows the raw token once (API-key convention, matches the backend's own "shown once" design).
  - **`/exams` (Unit 46)** — the largest of the four, and a bigger lift than it looked: the *base* exam/marks/report-card modules (Units 17-19) had never had a web screen either (Unit 27's web admin panel only ever covered students/fees/attendance/dashboard), so this page had to be built from scratch, not just extended. Four tabs: Exams (create + list, each row expands via "Manage" into nested Subjects/Timetable/Co-Scholastic/Rank tabs), Transcript (studentId → published report cards), Online Exams (create + list, "Manage" expands into publish/add-question/add-from-bank/questions-table/submissions-table), Question Bank (create + list, filterable by class).
  - **`admin-client.ts`** grew ~150 lines of new types + fetch wrappers for all four units' endpoints (staff, elective-groups, houses, attendance analytics/device-token, exams/exam-subjects/exam-timetable/co-scholastic/rank, students-transcript, online-exams/questions/submissions, question-bank) — no new patterns, same `adminFetch<T>()` wrapper every existing call already used.
  - **Locale keys** — ~140 new `school.staff.*`/`school.academicStructure.*`/`school.attendance.*`/`school.exams.*` keys added to both `en/common.json` and `hi/common.json` via a small Python script (safer than hand-editing two large JSON files in lockstep).
  - **Real bug caught and fixed during manual verification** (not by a test — no test suite covers the web-app): the Online Exams "Publish" button's mutation had no `onSuccess` query invalidation, so the "Published" badge stayed stuck on "No" until a manual page reload even though the publish had actually succeeded server-side (confirmed via network tab: `PATCH .../publish` → `200`, `isPublished: true`). Fixed by invalidating the `["online-exams"]` query key (prefix match, no branchId in scope at that call site) alongside the existing toast.
  - **Verified live**, not just typechecked: started the real `apps/api` server + `web-app` dev server, logged in as the seeded demo owner, and exercised every one of the 4 new/changed pages against real seeded data — created a house end-to-end (visible in the list immediately), issued a staff ID card (real certificate number returned), viewed the Rank tab against real seeded `MarksEntry` rows (correctly ranked Rohan Singh 91% > Aarav Sharma 82% > Diya Verma 45%), and ran the full online-exam lifecycle (create → add question → add-from-bank → publish → verified "Published: Yes" appears without a reload after the fix). One 403 was hit and confirmed to be correct pre-existing RBAC (OWNER lacks `attendance.mark`, matching rbac.md — not a defect); one 401 was hit and confirmed to be ordinary access-token expiry from the long manual-testing session (re-login resolved it, not a defect either).
  - No new automated tests — this was UI work with no server-side behavior change (other than the one query-invalidation bug, which is a client-only fix). Root `pnpm typecheck`/lint clean across all 11 packages (re-verified after the fix).

- **Mobile catch-up for Units 44/45/46.** Same conversation, same root cause as the web catch-up above, one layer deeper: `apps/mobile` hadn't been touched at all since Unit 26 — every screen file traced back to a single "phase 1 completed mvp" commit, confirmed via `git log` before starting so the claim wasn't a guess. Units 42/43 (Staff HR, Academic Structure) correctly need **no** mobile screen — they're office/admin work, not parent/teacher/student-facing, matching the web=admin/mobile=parent-teacher-student split. Only 44/45/46 have a real mobile-facing capability.
  - **Real backend gap found and fixed while building this, not before**: `GET /online-exams?branchId=` requires branch access, which a PARENT/STUDENT JWT never carries (their `branchIds` claim is empty by design — Unit 24's self-scope model routes them through `/me/*` and studentId, not branch). Without a fix, a student would have no way to discover which online exams exist for their own class at all. Added `GET /online-exams/mine?studentId=` (self-scoped via the same `assertOwnStudent`/enrollment-lookup pattern every other `/me`-style endpoint uses), returning each published exam for the student's current-session class plus their own submission status (`submitted`, `score`, `maxScore`) so the app can show "Take" vs. "Score: X/Y" without a second round trip. Covered by 2 new assertions in the existing `exam-report-depth.test.ts` (before-submit shows `submitted: false`, after-submit shows the real score) — `apps/api` suite still **207 tests total** (assertions added to an existing test, not a new file).
  - **`apps/mobile/src/lib/database.ts`** — WatermelonDB schema bumped to v2 with a real migration (`addColumns`), not just a version bump: `attendance_records.period_id` (nullable — null means daily, mirroring the server's own `AttendanceRecord.periodId`). A migration was written even though there are no live installs yet, since a bare version bump with no migration path would have broken the *next* schema change's assumptions for no reason.
  - **`TeacherAttendanceScreen.tsx` (Unit 44)** — added a period-chip row (Daily + each of today's periods, resolved from `GET /timetable?sectionId=` filtered to the server's own `dayOfWeek` convention — 0=Monday, requiring a JS-`Date.getDay()`-to-server conversion since JS treats Sunday as 0) above the existing roster. Every local WatermelonDB query and the `pushAttendance` call now key on `(sectionId, date, periodId)` instead of just `(sectionId, date)`, so a period mark and a daily mark for the same student/day genuinely coexist offline, exactly like they do server-side.
  - **`HomeworkGradingScreen.tsx` (Unit 45, new)** — a fourth tab on `TeacherHomeScreen`: pick a homework posted for the active section, see its submissions, enter a grade + optional feedback per submission, save. Reuses the existing `SectionPicker` and homework-list/submissions/grade endpoints Unit 45's API already exposed.
  - **`ParentStudentHomeScreen.tsx` (Unit 45 + 46)** — three additions to the existing tab row: a real **Submit** button on each homework row (installed `expo-document-picker@12.0.2`, matched to the existing Expo SDK 51 — picks a file, requests a presigned URL via the same two-step pattern the web staff-documents screen uses, `fetch(uploadUrl, {method:"PUT"})`s the blob directly to the bucket); a **Calendar** tab (`GET /me/homework/calendar`, grouped by day); an **Online Exams** tab (`GET /online-exams/mine`, "Take" for anything unsubmitted, real score for anything done).
  - **`OnlineExamTaker.tsx` (Unit 46, new)** — the MCQ-answering screen itself, rendered inline in place of the tab list while a student is mid-exam (not a separate navigator route — this app has no stack for role-home screens beyond the top-level one, so a local `takingExamId` state swap matches the existing style better than adding navigation plumbing for one screen). Fetches questions with the answer key already stripped (the backend's own `/take` route), lets the student pick one option per question, submits once, returns the real score to the parent screen's state on completion.
  - **Locale keys** — new `attendance.daily`/`attendance.period`, `homework.grading*`, `me.tabs.calendar`/`me.tabs.onlineExams`, `me.submit*`, and a new `onlineExams.*` namespace, added to both `apps/mobile/src/locales/{en,hi}/common.json` the same way as the web catch-up (small Python script, not hand-edited).
  - Root `pnpm typecheck` clean across all 11 packages; full `apps/api` suite (207 tests) re-run and passing. Not manually verified in a live simulator/device this time (no Expo dev client was started this session) — verified via typecheck + the 2 new backend assertions + careful reading against the existing screens' own established patterns, unlike the web catch-up which was click-tested end to end. Flagged here explicitly as a real difference in verification depth, not glossed over.

- **Unit 47 — Timetable Depth (Substitution, Room Allocation). ✅ Implemented, incl. web UI.** First unit built under the new standing rule (web/mobile paired with the API in the same pass, not deferred). Spec updated before coding, not just after: recorded the Open Question 2 decision (room stays free text, no new `Room` model — no school has asked for capacity-aware room *management*, only a same-slot clash check, which a string-equality check on the existing field answers just as well) and an explicit "UI scope" section (web-only — a principal/office view, matching the web=admin/mobile=parent-teacher-student split; the full weekly timetable grid editor remains Unit 22's own pre-existing gap, out of scope here).
  - **Schema** (`packages/db/prisma/schema/timetable.prisma`): `Substitution` (`timetablePeriodId`, `date`, `substituteStaffId`, `room?` override, `reason?`, `@@unique([timetablePeriodId, date])` — one same-day override per period, upsert-on-resubmit). Two migrations (schema + RLS), same two-file-per-model discipline as every prior unit.
  - **`POST /timetable/substitutions`** (gated `timetable.manage`): validates the substitution date's day-of-week actually matches the period's recurring `dayOfWeek` (reusing the same JS-`Date.getDay()`-to-server-convention conversion mobile's `TeacherAttendanceScreen` already established), then rejects on two independent conflicts before writing — the substitute teacher already covering (or already teaching) another section's period in the exact same day/slot, and the room (override or inherited from the period) already in use elsewhere that slot — both reusing Unit 22's own double-booking guard shape, just re-scoped to `Substitution` rows plus the recurring grid.
  - **`GET /timetable/substitutions/today?branchId=`** — a principal's morning view, today's date computed server-side (`UTC` midnight), includes section/subject/staff/substitute names for direct display, no client-side date math needed.
  - **Web UI**: new `/timetable` page (nav entry added) — a "Today's substitutions" table (staff/substitute names resolved via `staff.user.name`, added `include: { user: true }` to the two staff relations the service query already had) above an "Assign a substitute" form: load a section's periods by ID (existing `GET /timetable?sectionId=`, no new endpoint needed), pick one via radio, fill substitute staff ID + optional room override + reason, submit — same plain-text-ID-field convention as every prior unit's forms, same `adminFetch<T>()` client pattern.
  - Tests (`apps/api/test/timetable.test.ts`, +4, all passing, appended to the existing Unit 22 file rather than a new one): create-and-list round trip (recurring period untouched, "today" list shows it), substitute-teacher double-booking rejected, room double-booking rejected, RBAC (TEACHER denied, ADMIN with `timetable.manage` allowed). `cleanupTenant()` updated to delete `Substitution` rows before `TimetablePeriod` (new FK). `apps/api` suite now **211 tests total** (up from 207), `packages/db` isolation suite unaffected (4 tests). Root lint+typecheck clean across all 11 packages.
  - **Verified live**, not just typechecked: started the real API + web-app dev servers (added an `api` entry to `.claude/launch.json`, and wired `dotenv/config` into `apps/api`'s `dev` script since the app has no env-loading of its own — a pre-existing gap, not new to this unit), logged in as the seeded demo owner, created a second teacher and a real timetable period via the API, then drove the actual `/timetable` UI end to end: loaded the section's periods, selected one, assigned a substitute with a reason — the "Today's substitutions" table updated immediately showing the correct resolved names, room, and reason, no reload needed.

- **Unit 48 — Fee Management Depth (Cheque/PDC Tracking). ✅ Implemented, incl. web UI.** The smallest remaining fee-collection gap — most of fee management was already built (Units 11–14, 30, 38). Multi-currency (the spec's own Open Question 1) skipped entirely as pure speculation, per the spec's own recommendation.
  - **Schema** (`packages/db/prisma/schema/payments.prisma`): `ChequeStatus` enum (`PENDING`/`CLEARED`/`BOUNCED`) + `ChequePayment` (`paymentId` unique 1:1, `chequeNo`, `bankName`, `dueDate`, `status`) — attaches to an existing `Payment(mode: CHEQUE)` row rather than replacing it, per the spec's own scope #1. A cheque payment counts toward the invoice **immediately** at collection time, same as any other mode (real financial practice — the school books it as received, then tracks the bank's clearing outcome separately); only a bounce later reopens the invoice.
  - **Refactor before adding**: extracted the invoice-status recompute logic that lived inline inside `finalizePaymentSuccess` (Unit 12/13's shared payment-success tail) into a standalone `recomputeInvoiceStatus(tx, invoiceId)` in `complete-payment.ts` — it recomputes from scratch off current `SUCCESS` payments rather than only ever incrementing, so the exact same function now serves both the original "payment succeeded" path and this unit's new "a payment's success was revoked" (bounce) path, with zero logic duplicated between them. Never touches a `CANCELLED` invoice (a human decision, not derivable from totals).
  - **`POST /payments`** (existing endpoint, extended): `createPaymentSchema` gained optional `chequeNo`/`bankName`/`chequeDueDate`, required together via a `.refine()` only when `mode === "CHEQUE"`. When present, `createPayment` creates the `ChequePayment` row (status `PENDING`) inside the same transaction as the `Payment`.
  - **`PATCH /payments/:id/cheque-status`** (gated `fees.collect`, same permission as collecting the payment in the first place): `CLEARED` is a pure no-op on the invoice (the payment already counted). `BOUNCED` marks the `Payment` `FAILED` and calls `recomputeInvoiceStatus`, reopening the invoice if this was the payment keeping it `PAID`/`PARTIAL`. Idempotent on same-status re-application (matching Unit 38's receipt-cancel precedent); rejects a second *different* transition once already decided (`409` — a cheque can't un-bounce back to cleared or vice versa through this endpoint, a real decision needing a fresh manual review, not an automated flip).
  - **`GET /fees/reports/cheques?branchId=&status=`** (gated `fee.reports`) — a due-for-clearing list, filterable by status, joined to the parent `Payment` for amount/student/invoice display.
  - **Web UI**: extended the existing `/fees` page rather than adding a new one — the "Collect payment" dialog now shows cheque-specific fields (cheque no./bank name/due date) only when mode is `CHEQUE`, with the submit button disabled until they're filled; a new "Cheques" tab lists cheques by status with Mark Cleared/Mark Bounced actions, invalidating the list on decision. Web-only, no mobile screen — this is office/accounts work (fees.collect role gate), matching the web=admin/mobile=parent-teacher-student split every prior unit has followed.
  - Tests (`apps/api/test/payments.test.ts`, +4 new test cases covering 11 assertions, appended to the existing Unit 12 file): cheque creation requires all three cheque fields together (`400` if missing) and counts toward the invoice immediately; clearing is a no-op on the invoice and idempotent on re-clearing; bouncing marks the payment `FAILED` and reopens the invoice, and rejects a further transition once decided (`409`); the cheques report filters correctly by status and RBAC denies a TEACHER on the status-update endpoint. `cleanupTenant()` updated to delete `ChequePayment` rows before `Payment` (new FK). `apps/api` suite now **215 tests total** (up from 211). Root lint+typecheck clean across all 11 packages.
  - **Verified live**: reused the API + web-app dev servers from the Unit 47 session, collected a real cheque payment through the actual dialog (cheque fields correctly appeared/required only for `CHEQUE` mode), confirmed the invoice flipped `PENDING → PARTIAL` immediately, saw the pending cheque in the new Cheques tab with real data, then bounced it — the invoice correctly stayed reopened at `PARTIAL` (it already had an earlier unrelated payment from prior testing, so bouncing the cheque removed only its own contribution) and the cheque disappeared from the "Pending" filter, confirming `recomputeInvoiceStatus` handles multi-payment invoices correctly, not just the single-payment case the automated tests cover.

- **Unit 49 — Messaging & Engagement (C2 Depth). ✅ Implemented, incl. web + mobile UI.** By far the largest single unit since the 36-46 batch — seven real feature areas in one pass. User explicitly said "go ahead with async chat and skip SOS bypass for now" before this started, resolving both of the spec's own Open Questions in one line: parent-teacher chat confirmed in scope (async/REST-polled, no websocket), SOS broadcast (scope #8) deliberately not built pending a real sign-off on bypassing the SMS-wallet balance check.
  - **Schema** (new `packages/db/prisma/schema/engagement.prisma`, 11 models): `Circular`/`CircularAck` (reuses Announcement's exact `{roles?, classIds?}` audience-matching JSON shape, per the spec's own instruction — but deliberately has **no SMS/push fan-out of its own**; Announcement already owns that pipeline, a circular's actual job here is ack-tracking, which Announcement doesn't have). `PTMSlot` (`staffId`, `startTime`/`endTime`, `bookedByGuardianId?`). `CalendarEvent`. `Complaint`. `Survey`/`SurveyQuestion`/`SurveyResponse` (single-choice/text only, no branching, per spec scope #6). `GalleryAlbum`/`GalleryPhoto` (S3-backed, reuses Unit 04's presigned-upload wrapper). `Message` (`staffId`, `guardianId`, `senderId`, `body` — keyed by the `(staffId, guardianId)` pair, no thread/room concept beyond that). Two migrations (schema + RLS), generated via `prisma migrate dev --create-only` against the real dev DB rather than hand-written, then renamed to the project's own timestamp convention. New `engagement.manage` permission (OWNER/PRINCIPAL/ADMIN, same role set as `announcement.send`) added to `packages/types` and `context/rbac.md`.
  - **Seven new modules** (`apps/api/src/modules/{circulars,ptm,calendar,complaints,surveys,gallery,messages}/`), each following the exact existing service/controller/routes shape, plus self-scoped reads added to the existing `me` module rather than new modules of their own (`getMyCirculars`, `getMyCalendar`, `getMyGuardian`) — mirroring how Unit 24 already does this for homework/attendance/etc.
  - **Real self-scope design problem solved, not worked around**: PARENT/STUDENT tokens carry no `BranchMembership`, so `requireBranch` (which every staff-facing list already uses) can't gate their reads. Rather than looping every new read through that guard and breaking self-scoped access, each area got the access model that actually fits it: PTM-slot/gallery reads are open to any authenticated user (low-sensitivity — timings and photos, not personal data); Complaint-raising verifies branch ownership via the caller's *own* linked student's branch, not a client-trusted `branchId`; Circular/Calendar/Survey-response self-access goes through dedicated self-scoped endpoints (mirroring `/me/homework` etc.); Messages self-scope is enforced by resolving the caller's own Staff/Guardian record server-side and checking they're a real participant in that exact `(staffId, guardianId)` pair (staff with branch access can also read, for moderation, matching the fee-refund precedent of "staff sees everything in their branch").
  - **`GET /me/calendar`** merges three sources at read time (Open Question 2's own recommendation) — `CalendarEvent` for the branch, `ExamTimetable` dates reached via the student's current-session `ExamSubject.classId` rows, and `Homework.dueDate` for the student's section — replacing the mobile Parent/Student screen's old homework-only calendar outright rather than adding a second tab.
  - **Web UI**: new `/engagement` page (nav entry added), six tabs — Circulars (create + ack-count view), PTM Slots (create + list by staffId), Calendar, Complaints (list + resolve), Surveys (create + tally/list results), Gallery (album + presigned photo upload, inline `<img>` grid). Staff-facing only, matching the web=admin split.
  - **Mobile UI**: `MessagesScreen.tsx` (new, shared component — threads list, open-by-ID, send/receive bubbles) wired into both `TeacherHomeScreen` (own staffId resolved via `listMyTeacherAssignments`) and `ParentStudentHomeScreen` (own guardianId resolved via the new `GET /me/guardian`). `ParentStudentHomeScreen` also grew Circulars (list + ack), Complaints (raise + own list), and an upgraded merged Calendar tab. **Scope cut, stated plainly**: PTM-slot *booking* (parent side) and Survey-response/Gallery-viewing did not get mobile screens this pass — staff already have full management via the new web page, and adding three more mobile tabs on top of Messages/Circulars/Complaints/Calendar would have pushed this single unit's mobile diff past what's reviewable in one sitting. Flagged here explicitly as a real gap, not glossed over, same as every prior unit's honest verification-depth notes.
  - Tests (`apps/api/test/engagement.test.ts`, new file, 11 tests covering all seven areas + one tenant-isolation check): class-audience circular visible only to a parent in that class + ack round-trip + ack-list; PTM slot double-booking rejected + a non-staff user denied creation; the three-source calendar merge (event/exam/homework all present in one response); a parent denied raising a complaint for a branch their child isn't in; single-choice tally computed correctly (`{Good: 2, Bad: 0}`) alongside a text question's raw response list; gallery album+photo+signed-URL round trip; a full message exchange with a third party correctly denied (`403`) reading the thread. `apps/api` suite now **226 tests total** (up from 215; 2 pre-existing MinIO-dependent failures in unrelated files, `storage.test.ts`/`students.test.ts`, since MinIO wasn't running this session — not new). Root lint+typecheck clean across all 11 packages.
  - **Verified live**: reused the API + web-app dev servers, hit a real `403` on circular creation first (the seeded demo tenant's OWNER role predates this unit's new `engagement.manage` permission — existing tenants don't retroactively get new permissions, a real production-migration consideration flagged here rather than silently worked around), granted it directly via a one-off script to unblock testing, then created a real circular end-to-end (appeared in the list immediately) and confirmed the Gallery tab renders. Mobile UI was typechecked and covered by the new backend test suite but not run in a live Expo simulator this session, consistent with every prior mobile-catch-up unit's own stated verification posture.

- **Unit 50 — Certificates Depth (Custom Builder, Bulk IDs, DMS, e-Sign). ✅ Implemented, web UI only (explicit scope decision — see below).** Both of the spec's own Open Questions were adopted as recommended and recorded as resolved directly in `context/feature-specs/50-certificates-depth.md` before coding: (1) the certificate builder is token-based (`{{studentName}}` etc.), not a visual designer; (2) e-sign is a gated stub, same honest posture as Units 31/40 — no real provider account exists, so nothing is ever faked as `SIGNED`.
  - **Schema**: `CertificateTemplate` (token-based `body`, optional `branchId` for tenant-wide defaults, same convention as `ReportCardTemplate`). `Certificate` extended with `templateId`, and a new `SignatureStatus` enum (`NONE`/`REQUESTED`/`SIGNED`) plus `signatureRequestedAt`/`signedAt`/`signedPdfUrl`. New `Document`/`DocumentOwnerType` model (the actual central DMS scope item 3 calls for — Unit 07/42's ad-hoc `docs` `Json?` fields on Student/Staff were deliberately kept as-is, not migrated, per the spec's own "not required for this unit" note). Two migrations (schema + RLS), generated via `prisma migrate dev --create-only` against the live dev DB then renamed to the project's timestamp convention, applied to both the dev and test databases.
  - **`renderCertificateTemplate()`** (`apps/api/src/modules/certificates/service.ts`) — a small pure `{{token}}` → value substitution function, exported and unit-tested directly (`certificates-depth.test.ts`) so the DoD's "a template with tokens renders correctly" is provable without a real PDF pipeline (which still doesn't exist, same stub posture as Unit 21/19/12).
  - **`POST /certificates/bulk-ids?sectionId=`** — one `Certificate(ID_CARD)` per `ACTIVE` enrollment in the section's current session, each enqueuing the existing `certificate.generate` stub job with a `qrData` payload (`{studentId, admissionNo}` JSON) for a future real QR/PDF pipeline to consume — no QR image is actually rendered yet (Unit 21's Open Question 3 remains unresolved, unchanged by this unit).
  - **E-sign**: `POST /certificates/:id/request-signature` sets `signatureStatus=REQUESTED` and enqueues a new `certificate.esign-request` job; the worker processor (`apps/worker/src/processors/certificate-esign-request.ts`) checks `ESIGN_API_KEY`/`ESIGN_PROVIDER_URL` exactly like Unit 40's SMS/WhatsApp adapters — absent in this environment, so it always logs a clearly-labeled stub and leaves the certificate at `REQUESTED`, never faking `SIGNED`. Real completion is a separate `POST /webhooks/esign` route (public, unauthenticated — the provider calling us, not a user — same posture as Unit 13's Razorpay webhook), gated by a shared-secret header check (`ESIGN_WEBHOOK_SECRET`, `timingSafeEqual`) rather than HMAC since providers vary in what they sign; `tenantId` travels in the webhook payload itself, same reason Unit 13's Razorpay webhook does the same (no JWT to derive it from).
  - **Documents module** (`apps/api/src/modules/documents/`): `POST /documents` requests a presigned upload URL (reusing Unit 04's `getUploadUrl`/`getDownloadUrl` wrapper, same pattern as Unit 49's gallery photos) after verifying the given `ownerId` actually belongs to the given `branchId` — never trusts a client-supplied branch/owner pairing. `GET /documents` lists + filters by owner/tag, resolving a fresh signed download URL per row. Reused the existing `certificate.issue` permission rather than adding a new one — same OWNER/PRINCIPAL/ADMIN audience, avoids repeating Unit 49's "existing tenants don't retroactively get new permissions" gap for a same-session, same-audience feature.
  - **Web UI**: new `/certificates` page (nav entry added — Unit 27's own sidebar comment had listed "certificates" as fast-follow work since that unit; this closes it), four tabs — Register (issue + list + per-row "Request e-signature"), Templates (create + list), Bulk ID Cards (class/section pickers → generate → result table), Documents (register + tag-filtered list + signed download links). Added a small shadcn-style `Textarea` primitive (`components/ui/textarea.tsx`) that didn't exist yet in this codebase, matching `Input`'s existing styling exactly.
  - **Mobile UI: explicitly not built this pass.** Recorded as a decision in the spec itself ("UI scope — web only"), not an oversight — certificate templates, bulk ID generation, and the document register are office/admin tasks with no parent/student/teacher self-service angle, unlike every other unit that got mobile screens.
  - Tests (`apps/api/test/certificates-depth.test.ts`, new file, 7 tests + the pure-function render test): template creation + certificate issuance referencing it; bulk-ID generation producing one correctly-numbered `ID_CARD` per enrolled student + RBAC denial for TEACHER; the full e-sign loop (request → `REQUESTED`, webhook without the secret → `403`, webhook with the correct secret → `SIGNED` + `signedPdfUrl` persisted); document upload registers a tagged row retrievable by tag filter; a document request for an owner in the wrong branch is rejected (`400`). `apps/api` suite now **233 tests total** (up from 226; same 2 pre-existing MinIO-dependent failures, unrelated). Root lint+typecheck clean across all 11 packages.
  - **Verified live**: reused the API + web-app dev servers. Created a real `CertificateTemplate`, generated bulk IDs for a real Class 5-A section (one `ID_CARD-000002` correctly produced for its one enrolled student), registered a real `Document` for a student and confirmed a working presigned download link, and drove the e-sign loop from the UI (`Request e-signature` button flipped a real certificate's status to `REQUESTED` live). No console errors across all four tabs.

- **Unit 51 — Web Admin Panel: Remaining Modules. ✅ Implemented, web UI only (matches the spec's own scope — pure UI, no new backend).** Two of the original 10 items were dropped before starting, recorded in the spec itself: exam/term/`ExamSubject` setup was already built in Unit 46's Exams page (Subjects tab); certificates issue/list/register was already built in Unit 50's dedicated page. The real gap closed here was 8 areas: guardians, staff (create + leave queue), admissions, marks entry, report cards, announcements, homework, and a real timetable weekly grid.
  - **`admin-client.ts`**: added typed wrapper functions + interfaces for all 8 areas (`GuardianItem`, `EnquiryItem`, `ApplicationItem`, `MarksEntryRow`, `ReportCardTemplateItem`, `ReportCardItem`, `AnnouncementItem`, `HomeworkItem`, `LeaveRequestItem`) plus a new `listStudentsByClass` (existing `listStudents` wrapper left untouched to avoid breaking the Students page) and `bulkUpsertTimetable`.
  - **Guardians** (`/guardians`, new page): create + list + link-to-student, live-verified end to end (`POST /guardians` → `201`, `POST /students/:id/guardians` → `201`).
  - **Admissions** (`/admissions`, new page): Enquiries tab (create + stage-filtered list + inline stage-change dropdown, verified live — `PATCH /enquiries/:id` → `200`) and Applications tab (create + status-filtered list + convert-to-student action). **Scope call, recorded in the spec**: a stage-filtered list instead of a drag-and-drop kanban board — same table/form pattern as every other screen, no validated demand for the extra kanban UI work.
  - **Staff** (`apps/web-app/app/(school)/staff/page.tsx`, extended): added a "New staff member" create form to the previously read-only Staff List tab (verified live, `POST /staff` → `201`), and a new "Leave Requests" tab. **Real bug caught and fixed during live verification**: the tab was first built with an admin-facing "apply for leave" form, but `createLeaveRequest` is deliberately self-scoped server-side (Unit 09's "no applying on a colleague's behalf" invariant — only the caller's own linked `Staff` record can apply) — an OWNER/ADMIN calling it always gets `403`. Removed the misleading apply form; the tab is now purely the approve/reject queue, which is the actual admin-facing job here.
  - **Exams page** (extended): two new tabs. **Marks Entry** — load an exam → pick an `ExamSubject` → a real one-row-per-student bulk grid (roster fetched via the new `listStudentsByClass`, pre-filled with existing marks) → `POST /marks` bulk save; live-verified rendering a real 2-student roster with correct existing marks (45/91) and max-marks display. **Report Cards** — template create/list (reused `reportcard.generate`/`reportcard.publish` permissions, no new ones) + generate-for-exam + per-row publish button; live-verified, `POST /report-cards/generate` → `201` producing real rows, one already-published row correctly hid its Publish button.
  - **Homework** (`/homework`, new page): create (branch/section/subject/title/description/due-date) + section-filtered list. Reached the server correctly (`POST /homework` → expected `403` for OWNER, since `homework.manage` is PRINCIPAL/TEACHER-only per the existing rbac.md matrix — confirms correct wiring, not a bug).
  - **Announcements** (`/announcements`, new page): create with a clickable role-pill audience picker + optional class-IDs + list; live-verified, `POST /announcements` → `201`, new row appeared with its audience badge rendered correctly.
  - **Timetable weekly grid** (`apps/web-app/app/(school)/timetable/page.tsx`, extended): the one screen in this batch that's "more than a plain table" per the spec's own scope #9 — a real day×period grid (`WeeklyGridEditor`), click an empty cell to open an inline subject/staff/room mini-form, builds a local draft array, one `POST /timetable` (`bulkUpsertTimetableSchema`) commits it. Live-verified: clicked a real grid cell, set subject+staff, saved — `POST /timetable` → `201 Created`, existing periods (e.g. Tue/Period 6) rendered correctly alongside the new one.
  - **RBAC observations, not bugs**: three live 403s during verification (homework create, marks entry, leave-apply) all trace to the existing `rbac.md` matrix correctly denying OWNER for permissions scoped to PRINCIPAL/TEACHER only (`homework.manage`, `marks.enter`, self-scoped `leave.apply`) — each confirms the request reached the server and was correctly gated, not a defect in this unit's code.
  - Nav + i18n: sidebar reordered/extended with Guardians, Admissions, Homework, Announcements entries (role-gated same as existing items); ~90 new keys added to both `locales/en/common.json` and `locales/hi/common.json`, reusing existing keys (`school.engagement.fieldTitle`, `school.exams.publish`, etc.) wherever a suitable one already existed instead of duplicating.
  - No new backend code, no new migrations, no new tests — this was a pure-UI unit per its own spec, and every screen was live-verified against the existing (already-tested) API surface rather than needing new automated coverage. Root lint+typecheck clean across all 11 packages after the fix.

- **Unit 52 — Mobile App Depth. ✅ Implemented, scope resolved smaller than drafted.** Before writing anything, audited `apps/mobile/src/screens/` + `navigation/` against the spec's own scope list and found most of it already built by Units 46/49's own mobile passes: messaging (`MessagesScreen`), the unified calendar, and online-exam-taking (`OnlineExamTaker`) were already wired into `TeacherHomeScreen`/`ParentStudentHomeScreen`. Only two real gaps remained, both recorded as resolved in the spec's Open Questions: (1) leave-apply on the teacher app — Unit 09's `POST /leave-requests` was backend-only until now; (2) PTM slot booking on the parent app — Unit 49's own mobile pass explicitly scoped this out. Open Question 2 (study-material library) resolved to the smaller, already-endorsed option — online exams without a materials library; A10 remains fully deferred, untouched.
  - **One small backend addition**: `GET /me/teachers?studentId=` (new `getMyTeachers` in `apps/api/src/modules/me/service.ts`/`controller.ts`/`routes.ts`) — self-scoped (reuses `assertOwnStudent`/`resolveCurrentSectionId`, same pattern as every other `/me/*` read), resolves a student's current-session `TeacherAssignment` rows into `{staffId, staffName, subjectName}`. Needed because `GET /ptm-slots?staffId=` requires a staffId and no self-scoped way to discover one existed — the existing PTM backend (Unit 49) otherwise needed zero changes.
  - **`apps/mobile/src/lib/api-client.ts`**: added `applyLeave`, `listMyLeaveRequests`, `getMyTeachers`, `listPTMSlots`, `bookPTMSlot` + their `LeaveRequestItem`/`MyTeacherItem`/`PTMSlotItem` types, following the existing `authedRequest<T>()` wrapper convention.
  - **`LeaveScreen.tsx`** (new, teacher-facing): type-pill picker (Casual/Sick/Earned/Other) + from/to date inputs → `POST /leave-requests` (self-scoped server-side to the caller's own `Staff.id`, so no staffId is entered) + own-requests list with status. Added as a sixth tab on `TeacherHomeScreen`.
  - **`PTMScreen.tsx`** (new, parent-facing): teacher list (via `getMyTeachers`) → tap a teacher → their open/booked slots (via `listPTMSlots`) → book an open one (`PATCH /ptm-slots/:id/book`), mirroring `MessagesScreen`'s two-pane list/detail pattern. Added as a new `ptm` section on `ParentStudentHomeScreen`.
  - i18n: `leave.*` and `ptm.*` namespaces + `teacherHome.tabs.leave`/`me.tabs.ptm` keys added to both `apps/mobile/src/locales/{en,hi}/common.json`.
  - **Honest verification gap, same as every prior mobile unit**: no simulator/device attached this session — verified via `tsc --noEmit` only (clean on both `apps/api` and `apps/mobile`), not on-device. The full backend test suite could not be run this session — Docker/Redis wasn't up (`MaxRetriesPerRequestError` against `ioredis`, an environment-availability gap, not a code regression), same posture as prior units' MinIO-down notes. No new backend tests added since `getMyTeachers` reuses fully-tested self-scope helpers (`assertOwnStudent`, `resolveCurrentSectionId`) with no new branching logic of its own — low regression risk, but not proven by a green run this session.

- **Unit 53 — Owner Dashboard Depth. ✅ Implemented.** A small unit exactly as scoped — extends Unit 28's existing `getDashboardSummary` aggregator with two more metrics, no new modules, no new schema.
  - **`apps/api/src/modules/dashboard/service.ts`**: added `enrollmentTrend` (last 12 calendar months incl. the current one, oldest first, `{month: "YYYY-MM", count}[]`, zero-filled for months with no new `Enrollment` rows) and `staffMetrics` (`{headcount, onLeaveToday}` — active (`deletedAt: null`) `Staff` count for the branch, and a count of `LeaveRequest` rows `status: APPROVED` whose `[fromDate, toDate]` range covers today). Both reuse the existing `dayRange()`/`monthRange()` helpers and the same `assertDashboardAccess`/`assertBranchAccess` gate — no new permissions needed.
  - **Web**: `apps/web-app/app/(school)/dashboard/page.tsx` gained two more `StatCard`s (headcount, on-leave-today) and a new `EnrollmentTrendChart` — a plain CSS bar chart (flexbox heights, no charting library; ponytail call — a library is overkill for 12 static bars) rendered under the stat-card grid.
  - **`admin-client.ts`**: `DashboardSummary` interface extended with `enrollmentTrend`/`staffMetrics`.
  - i18n: `school.dashboard.{enrollmentTrend,staffHeadcount,staffOnLeaveToday}` added to both locale files.
  - **Tests**: extended the existing `dashboard.test.ts` recomputation fixture (same test, not a new one — matches Unit 28's own "exact-number recomputation" style) with a staff member on approved leave covering today; asserts `enrollmentTrend` has exactly 12 entries summing to 1 (the one enrollment created this month, in the last slot) and `staffMetrics` equals `{headcount: 1, onLeaveToday: 1}`.
  - **Honest verification gap**: neither Postgres nor Redis were reachable this session (Docker daemon down — `Cannot connect to the Docker daemon`), so the extended test could not actually be run; verified via `tsc --noEmit` only (clean on `apps/api` and `apps/web-app`). Flagged here rather than silently claimed as tested — same posture as Unit 52's note directly above.

- **Unit 54 — Public Site Depth. ✅ Implemented.** Open Question 1 resolved by adopting the spec's own recommendation: a small fixed set of sections, not a general CMS.
  - **Schema**: new `PublicNotice` model (`notifications.prisma` — `title`/`body`/`publishedAt`, deliberately separate from `Announcement` per the spec's own instruction, since not every internal announcement should be public). `GalleryAlbum` gained an `isPublic` boolean (default `false`) — reused rather than duplicated, per the spec's own scope #2. `Tenant` gained `contactPhone`/`contactEmail`/`mapUrl` (flat fields, same convention as Unit 36's existing profile fields — `address` already existed). Two migrations (schema + RLS for `PublicNotice`) hand-written in the project's timestamp convention — Docker wasn't running this session so `prisma migrate dev --create-only` couldn't hit a real dev DB; written by hand against the exact SQL shape of the last several real generated migrations (Unit 49/50's), then `prisma generate` run schema-only (no DB connection needed) to regenerate the Prisma Client types and confirm the schema itself is valid.
  - **New staff-facing module** (`apps/api/src/modules/public-notices/`): create/list, reusing `announcement.send` (no new permission — issuing a public notice is the same kind of admin action as an internal announcement, just aimed at the public site).
  - **`apps/api/src/modules/public/` extended** (Unit 29's existing no-auth module) with three new endpoints: `GET /public/notices/:schoolCode` (newest 50), `GET /public/gallery/:schoolCode` (only `isPublic` albums, photos resolved to signed download URLs), `GET /public/contact/:schoolCode` (tenant contact fields + active branches). All resolve the tenant via the existing `resolveTenantBySchoolCode` helper — same no-auth, schoolCode-only posture as the existing endpoints.
  - **Web admin** (`apps/web-app`): Settings page gained a "Public Notices" tab (create/list) and three new profile fields (contact phone/email, map URL) on the existing Profile tab; Engagement page's Gallery tab gained an "isPublic" checkbox at album-create time and a badge on public albums.
  - **`apps/web-site`**: three new pages — `/[schoolCode]/notices`, `/[schoolCode]/gallery`, `/[schoolCode]/contact` — server components following the existing `getPublicSchoolInfo` pattern exactly (fetch in the page, `notFound()` on unknown schoolCode). Nav links added to the main school page.
  - **Tests**: extended `apps/api/test/public.test.ts` with three new cases — a created `PublicNotice` visible on the public endpoint, only `isPublic`-flagged albums appearing in the public gallery (a private album created alongside stays hidden), and the contact endpoint returning tenant fields + branches.
  - **Honest verification gap, same as Units 52/53 above**: Docker was down the entire session (no Postgres, no Redis) — none of the new or extended tests could actually be run. Verified via `tsc --noEmit` only, clean across `apps/api`, `apps/web-app`, `apps/web-site`, `packages/validation`, `packages/db`. Migration SQL was hand-written rather than tool-generated for the same reason — a real risk worth stating plainly rather than glossing over, since hand-written SQL hasn't been proven against a live schema this session. The next session with Docker available should run `prisma migrate deploy` against both `vidyut` and `vidyut_test`, then the full suite, before trusting this unit as fully verified.

- **Unit 55 — Analytics & Reporting (Part F, non-AI). ✅ Implemented, no new schema.** Both Open Questions resolved as recorded in the spec: (1) adopted the spec's own recommendation of five fixed, hand-written parameterized reports instead of a generic report builder; (2) UDISE+/board-reporting exports remain explicitly blocked — no real government format spec was sourced, so nothing was guessed at.
  - **New `apps/api/src/modules/reports/`** module — five `GET /reports/{attendance|fees|exams|admissions|staff}?branchId=&from=&to=&format=json|csv` endpoints, each a small hand-written query over already-existing models (`AttendanceRecord`, `Invoice`/`Payment` via the existing `invoiceTotal` helper, `Exam`/`ExamSubject`/`MarksEntry`, `Enquiry`/`Application`, `Staff`/`LeaveRequest`). `GET /reports/kpi-summary` runs all five for the current calendar month and reduces each to one headline figure. Access reuses Unit 28's own `dashboard.owner`/`dashboard.principal` permissions rather than adding a new `report.*` permission — same MIS audience, just more detail.
  - **CSV export**: new `apps/api/src/core/csv.ts`, a ~10-line hand-written serializer (no dependency added — ponytail call, ratified: ".csv" is just escaped comma-joined text, not worth a library for flat rows). `format=csv` on any report endpoint switches the controller's response from the JSON envelope to a raw `text/csv` body.
  - **`POST /reports/schedule`** (`{branchId, reportType, cadence: WEEKLY|MONTHLY, recipientEmail}`) registers a BullMQ repeatable job (`reports.scheduledEmail`, cron `0 9 * * 1` / `0 9 1 * *`) — same "BullMQ's own repeat-job registry is the source of truth, no separate schedule table" posture as Unit 14's fee-reminder cron. New `apps/worker/src/processors/reports-scheduled-email.ts` recomputes the report (trailing 30 days, a stated simplification — a real per-cadence window is the upgrade if a school asks) and emails the CSV as the plain-text email body via the existing `sendEmail` adapter (Unit 40) — no attachment/MIME support was added, since the shared adapter is plain-text-only by design and a CSV is valid plain text.
  - **Web UI**: new `/reports` page (nav entry added, OWNER/PRINCIPAL-gated like Dashboard) — a KPI Summary tab, five standard-report tabs (date range + table + CSV download button), and a Schedule tab (report type + cadence + recipient email form).
  - **Tests** (`apps/api/test/reports.test.ts`, new file): one large recomputation-check test seeding real attendance/fees/exam/admission/staff fixtures and asserting exact numbers back from all five report endpoints + the CSV variant + the KPI summary (matching Unit 28/53's own test style); an RBAC-denial test; and a schedule test that calls the real `queue.getRepeatableJobs()` (a new `getRepeatableJobs` export added to `apps/api/src/core/jobs.ts` for this) to confirm the job was registered with the correct cron pattern — inspecting the actual repeat-job registration rather than waiting for a real month-long fire, per the spec's own DoD.
  - **Honest verification gap, same as Units 52–54 above**: Docker was down the entire session — the new test file was never actually run. Verified via `tsc --noEmit` only, clean across `apps/api`, `apps/worker`, `apps/web-app`, `packages/types`, `packages/validation`. No new migrations this unit (no new schema), so that specific risk from Units 52–54 doesn't apply here — but the test suite itself is still unproven this session.

- **Unit 56 — Super-Admin Console Depth. ✅ Implemented.** Open Questions 1 and 3 resolved by adopting the spec's own recommendations; Open Question 2 (reseller/partner) stays explicitly blocked — no commission-model decision was made, so `Partner`/`Commission` were never modeled.
  - **Schema** (`platform.prisma`): `SupportTicket` (`tenantId`, `subject`, `body`, `status`, `priority`, `response`, `respondedAt`) — tenant-owned and RLS-scoped, same posture as `AuditLog` (even though the platform side reads/writes it, always via `withTenant(thatTenantId, ...)` for one specific tenant, never a raw cross-tenant query). `GlobalAnnouncement` (`title`, `body`, `targetPlanKeys?`) — platform-owned, no tenantId, no RLS, same posture as `Plan`. Two migrations (schema + RLS for `SupportTicket`), hand-written (Docker down) against the exact shape of the last several real migrations; `prisma generate` run schema-only to regenerate client types.
  - **New tenant-side `support-tickets` module**: `POST/GET /support-tickets`, reusing `settings.manage` (no new permission) — raising a ticket is the same kind of account-level action as editing the school profile.
  - **Platform module extended**: `POST /platform/announcements` (creates the `GlobalAnnouncement`, then enqueues a background job — AGENTS.md invariant #2, never inline). `GET /platform/tickets?status=` — cross-tenant, one `withTenant(tenantId, ...)` read per ACTIVE tenant (same loop pattern as the existing `getRevenueSummary`'s platform-fee aggregate), and **every tenant whose ticket content is actually read gets its own `AuditLog` entry** (`support-ticket.read`, `actorType: PLATFORM_USER`) — the DoD's own "deliberate, audited exception to RLS isolation," not a silent bypass. `PATCH /platform/tenants/:id/tickets/:ticketId` responds and writes its own `support-ticket.respond` audit entry. `GET /platform/health-summary` — real DB/Redis pings (same checks as `/ready`), real BullMQ queue depth (new `getQueueCounts` export on `core/jobs.ts`), and a new in-process rolling 15-minute 5xx counter (`core/error-counter.ts`, ticked from the existing `structuredLogging` middleware) instead of a real Sentry API integration.
  - **New worker processor** (`platform-global-announcement-fanout.ts`): loads the `GlobalAnnouncement`, resolves matching ACTIVE tenants (filtered by `targetPlanKeys` against `Tenant.plan.key` when set), and for every active branch of every matching tenant creates a real Unit 20 `Announcement` row, then **reuses the existing `announcement.fanout` job unchanged** for actual per-user dispatch — no new dispatch logic invented, just Announcement creation at platform scale.
  - **Web UI**: three new super-admin pages — `/super-admin/announcements` (create + send), `/super-admin/tickets` (cross-tenant grouped list + inline respond), `/super-admin/health` (DB/Redis/queue/error-count cards, 15s auto-refresh) — all linked from the existing tenants list header. Tenant-side: a new "Support" tab on the school admin's existing Settings page (raise ticket + own-tickets list with staff-visible response).
  - **Tests** (`apps/api/test/super-admin-console.test.ts`, new file): a full ticket lifecycle (tenant creates → platform lists cross-tenant with a real `AuditLog` entry asserted → platform responds with a second audit entry asserted); an RBAC test (tenant caller without `settings.manage` denied, platform route with no token denied); a real fan-out test using `startWorker()` + a short wait, asserting a real `Announcement` row landed in the tenant's branch; and a health-summary test asserting real `db`/`redis`/`queue` shape.
  - **Honest verification gap, same as every Docker-down unit this session (52–56)**: none of the new tests were actually executed. Verified via `tsc --noEmit` only, clean across every touched package (`apps/api`, `apps/worker`, `apps/web-app`, `packages/db`, `packages/types`, `packages/validation`). Migration SQL hand-written, unproven against a live schema. The next session with Docker available should run both migrations against `vidyut`/`vidyut_test`, then the full suite, before trusting Units 52–56 as fully verified — this has now been true for five consecutive units and is worth resolving before continuing much further.

- **Unit 57 — Transport Management (D1, first fully On-Demand module). ✅ Implemented, at the user's explicit request.** `build-approach.md`'s "on-demand modules only when a paying school needs one" rule was surfaced directly before starting (both this spec's own header note and AGENTS.md golden rule #2 flag it) — the user confirmed to build anyway. No real school demand exists; recorded here for visibility, not glossed over. Both Open Questions resolved by adopting the spec's own recommendations.
  - **Schema** (new `packages/db/prisma/schema/transport.prisma`, 6 models): `Route`/`RouteStop` (ordered stops, optional lat/lng). `Vehicle` (`regNo`, `capacity`, optional `routeId`, `fitnessExpiry`/`insuranceExpiry`/`permitExpiry`). `Driver` (lightweight non-staff contact by default; optional `staffId` link per the spec's own scope #1). `StudentRouteAllocation` (`studentId`, `routeId`, `stopId`, `feeAssignmentId`). `LocationPing` (generic, vendor-agnostic — Open Question 1). All RLS-scoped like every other tenant-owned model. Two migrations (schema + RLS), hand-written (Docker down) against the shape of the last several real migrations; `prisma generate` run schema-only to confirm the schema itself is valid and regenerate client types.
  - **New `transport.manage` permission**, added to OWNER/PRINCIPAL/ADMIN's default grants (`packages/types`) and `context/rbac.md` — no existing permission fit, unlike most of this session's reuse-first units.
  - **New `apps/api/src/modules/transport/`** module: CRUD for routes/stops/vehicles/drivers (branch-scoped, `transport.manage`-gated), `POST /transport/allocations` (Open Question 2 — find-or-creates a per-route `FeeStructure` with one `TRANSPORT` `FeeStructureItem` via Unit 11's existing fee engine, then a `FeeAssignment`, all idempotent via `upsert` so re-allocating the same student/route updates in place rather than duplicating), and `POST /transport/location-ping` (Open Question 1 — generic ingestion, authenticated but not yet gated behind a per-vehicle device token, since no real device exists to design that flow against — Unit 44's `attendanceDeviceToken` is the reuse precedent once one does).
  - **Geofence crossing → real parent alert** (scope #4): a new `apps/api/src/modules/transport/geofence.ts` (plain Haversine distance, no dependency) plus a transition check in the ping handler — mirrors Unit 16's `isNewAbsence` pattern exactly (only fires when a ping newly enters a stop's 200m radius, not on every ping while idling inside it), enqueuing `transport.geofenceAlert`. New worker processor `transport-geofence-alert.ts` reuses the exact PUSH-then-SMS-fallback pipeline from `students-absence-alert.ts` (Unit 32/40) — no new dispatch logic, per the spec's own scope #4 instruction to reuse the existing notification pipeline.
  - **Document-expiry alerts** (scope #5): new `transport-expiry-scan.ts` (nightly cron, same shape as Unit 14's `fees-reminder-scan.ts` — no tenantId scans every ACTIVE tenant with the `transport` module enabled) and `transport-expiry-alert.ts` (IN_APP-only NotificationLog to every OWNER — an internal ops alert, not parent-facing, so no SMS wallet cost). Registered as a second nightly repeatable job alongside Unit 14's fee-reminder scan in `apps/worker/src/index.ts`.
  - **Web UI**: new `/transport` page (OWNER/PRINCIPAL/ADMIN-gated) — Routes (+ inline stop management), Vehicles (+ document expiry dates), Drivers, and Student Allocation tabs.
  - **Tests** (`apps/api/test/transport.test.ts`, new file): routes/stops/vehicles/drivers CRUD + RBAC-denial + a cross-tenant branch-id isolation check; an allocation test asserting the real `FeeStructureItem`/`FeeAssignment` amounts and idempotent re-allocation; and a real geofence-crossing test using `startWorker()` — two pings (one far away, one at the stop) confirming the alert fires only on the transition into range, landing as a real `NotificationLog`.
  - **Honest verification gap, same as every Docker-down unit this session (52–57)**: none of the new tests were actually executed — Docker was down the entire session. Verified via `tsc --noEmit` only, clean across every touched package. This is now six consecutive units built and typechecked but never run against a live database; strongly recommend prioritizing a Docker-up verification pass (both migrations against `vidyut`/`vidyut_test`, then the full suite) before continuing further, to catch anything a type-checker can't (e.g. the hand-written SQL, the Prisma upsert composite-key syntax, the actual geofence math against a real DB round trip).

- **Unit 58 — Library Management (D2, second fully On-Demand module). ✅ Implemented, at the user's explicit request** ("continue with unit 58", the same confirmed exception pattern as Unit 57). No real school demand exists; recorded here for visibility, not glossed over. The one Open Question (ISBN auto-fetch) resolved as **not built** — no confirmation it's wanted, manual `title`/`author`/`isbn` fields cover catalog entry without an external API dependency.
  - **Schema** (new `packages/db/prisma/schema/library.prisma`, 4 models): `Book` (`title`/`author`/`isbn`), `BookCopy` (`barcode`, `status: AVAILABLE|ISSUED|LOST`), `LibraryMember` (a thin link row — `studentId`/`staffId`, exactly one set — not a new identity, per scope #2), `BookIssue` (`copyId`, `memberId`, `issuedAt`/`dueAt`/`returnedAt`, `fineInvoiceId`). All RLS-scoped. Two migrations (schema + RLS), hand-written (Docker still down) against the shape of the last several real migrations; `prisma generate` run schema-only to confirm validity.
  - **New `library.manage` permission**, added to OWNER/PRINCIPAL/ADMIN's default grants (`packages/types`) and `context/rbac.md` — same reasoning as Unit 57's `transport.manage`, no existing permission fit.
  - **New `apps/api/src/modules/library/`** module: CRUD for books/copies/members (branch-scoped, `library.manage`-gated), `POST /library/issues` (rejects a copy that isn't `AVAILABLE`, sets it `ISSUED`), `POST /library/issues/:id/renew` (extends `dueAt`, rejects an already-returned issue), `POST /library/issues/:id/return` (Open Question 2's resolution — on an overdue return, finds-or-creates a `MISC` `FeeHead` and creates a one-off `Invoice`/`InvoiceItem` via Unit 12's existing engine, `periodLabel` keyed to the issue id so it's always unique; only bills student members, since `Invoice.studentId` is a hard FK and there's no staff fee ledger — a staff late return is recorded but not billed, a real gap flagged in the spec, not an oversight).
  - **No new worker/cron job** — the spec's scope only calls for a fine "generated on overdue return" (the return event itself), not a proactive outstanding-book reminder, so Unit 57's cron-scan pattern was deliberately not reused here. Revisit if a school wants proactive overdue alerts.
  - **Web UI**: new `/library` page (OWNER/PRINCIPAL/ADMIN-gated) — Books (+ inline copy management), Members, and Issue/Return tabs.
  - **Tests** (`apps/api/test/library.test.ts`, new file): books/copies/members CRUD + member-validation (exactly one of studentId/staffId) + RBAC-denial; an issue/return/renew lifecycle test asserting copy status transitions, a rejected double-issue on an already-ISSUED copy, a rejected renew/issue on an already-returned issue, and a real overdue-return fine (`InvoiceItem.feeHead.type === "MISC"`); a second test confirming an on-time return generates no fine.
  - **Honest verification gap, same as every Docker-down unit this session (52–58)**: none of the new tests were actually executed — Docker was down the entire session. Verified via `tsc --noEmit` only, clean across every touched package. This is now seven consecutive units built and typechecked but never run against a live database; a Docker-up verification pass remains the single highest-value next step before continuing further into the On-Demand catalog.

- **Unit 59 — Hostel/Dormitory (D3, third fully On-Demand module, lowest priority per its own P3 tag). ✅ Implemented, at the user's explicit request** ("keep going", continuing the same confirmed exception used for Units 57/58). No real school demand exists; recorded here for visibility. The one Open Question (mess/meal scope) resolved as **not built at all** — even the spec's own fallback (a simple published-text menu) wasn't added, since there's no confirmation it's wanted either.
  - **Schema** (new `packages/db/prisma/schema/hostel.prisma`, 4 models): `HostelBlock`, `Room` (`roomNo`, `capacity`), `RoomAllocation` (`studentId`, `roomId`, `fromDate`/`toDate`, `feeAssignmentId`), `HostelAttendanceRecord`. All RLS-scoped. Two migrations (schema + RLS), hand-written (Docker still down); `prisma generate` run schema-only to confirm validity.
  - **Corrected an inaccurate spec assumption**: scope #2 said hostel attendance "reuses Unit 44's attendance shape, just a different context tag" — checked the actual `AttendanceRecord` model and found it hard-wired to `sectionId`/`periodId` with no context enum to tag. The codebase's real precedent for a non-class attendance context is Unit 42's `StaffAttendanceRecord` — its own parallel model, same `AttendanceStatus`/`AttendanceSource` enums, no `sectionId`. `HostelAttendanceRecord` follows that same precedent instead of forcing a tag onto a model that doesn't have room for one.
  - **New `hostel.manage` permission** for blocks/rooms/allocations (OWNER/PRINCIPAL/ADMIN default grants, `packages/types`, `context/rbac.md`). Night roll-call attendance deliberately reuses the **existing** `attendance.mark`/`attendance.view` permissions instead — the exact precedent Unit 42's staff attendance already set (`apps/api/src/modules/staff/routes.ts`), not a new hostel-specific pair.
  - **New `apps/api/src/modules/hostel/`** module: CRUD for blocks/rooms (branch-scoped, `hostel.manage`-gated); `POST /hostel/allocations` (scope #3 — finds-or-creates a per-block MISC `FeeHead`/`FeeStructure`/`FeeStructureItem` via Unit 11's fee engine, then a `FeeAssignment`, same reuse pattern as transport/library; **enforces room capacity inside the same transaction** as the insert — counts active (`toDate: null`) allocations against `Room.capacity` and rejects over-capacity assignments — a service-layer check, not a DB constraint, since Postgres has no native "count of related rows below N" constraint); `POST/GET /hostel/attendance` (bulk upsert by `(studentId, date)`, mirroring Unit 15's `markAttendance` shape).
  - **Scope #4 (gate pass for hostellers) not built** — it explicitly depends on Unit 60's (Front Office) not-yet-built gate-pass model; there's nothing to reuse yet. Revisit once Unit 60 exists.
  - **Web UI**: new `/hostel` page (OWNER/PRINCIPAL/ADMIN-gated) — Blocks & Rooms (+ inline room management), Room Allocation, and Night Roll-Call tabs.
  - **Tests** (`apps/api/test/hostel.test.ts`, new file): blocks/rooms CRUD + RBAC-denial; a room-allocation test asserting the real `FeeStructureItem`/`FeeAssignment` amounts, a rejected over-capacity third allocation into a 2-capacity room, and an idempotent re-allocation of an already-resident student; a night roll-call test asserting upsert-on-remark behavior.
  - **Honest verification gap, same as every Docker-down unit this session (52–59)**: none of the new tests were actually executed — Docker was down the entire session. Verified via `tsc --noEmit` only, clean across every touched package. This is now eight consecutive units built and typechecked but never run against a live database — the accumulated risk here is genuinely significant at this point, and a Docker-up verification pass should be treated as a near-term priority, not an indefinitely deferred nice-to-have.

- **Unit 60 — Front Office & Gate (D4 remaining rows). ✅ Implemented, at the user's explicit request** ("keep going with unit 60 and 61", continuing the same confirmed exception used for Units 57–59). No real school demand exists; recorded here for visibility. No Open Questions — the spec itself flagged these as small, well-understood registers.
  - **Schema** (new `packages/db/prisma/schema/front-office.prisma`, 5 models): `Visitor` (check-in/out, optional `hostStaffId`), `GatePass` (`studentId`, `reason`, `approvedById`, `exitAt`), `ComplaintDeskEntry`, `CallLogEntry`, `PostalLogEntry`. All RLS-scoped. Two migrations (schema + RLS), hand-written (Docker still down).
  - **`ComplaintDeskEntry` is its own model, not Unit 49's `Complaint` reused directly** — `Complaint.raisedByUserId` is a required FK and a walk-in gate complaint often has no `User` behind it. Reuses the same `ComplaintStatus` enum (`OPEN`/`RESOLVED`), not a parallel vocabulary.
  - **New `frontoffice.manage` permission** (OWNER/PRINCIPAL/ADMIN, `packages/types`, `context/rbac.md`).
  - **New `apps/api/src/modules/front-office/`** module: visitor check-in/out, gate pass create/exit, complaint desk create/resolve, call-log and postal-log create/list — all branch-scoped, `frontoffice.manage`-gated.
  - **Gate pass approval → real parent alert** (scope #2): a new `frontoffice.gatePassAlert` job + `apps/worker/src/processors/frontoffice-gatepass-alert.ts`, reusing the exact PUSH-then-SMS-fallback pipeline from `students-absence-alert.ts`/`transport-geofence-alert.ts` (Unit 32/40) — no new dispatch logic, enqueued the moment a pass is created (approval and creation are the same action here, since the approving staffer is the one raising it).
  - **Web UI**: new `/front-office` page (OWNER/PRINCIPAL/ADMIN-gated) — Visitors, Gate Passes, Complaint Desk, and Call/Postal Log tabs.
  - **Tests** (`apps/api/test/front-office.test.ts`, new file): visitor check-in/out, complaint-desk create/resolve, call/postal log creation, RBAC-denial; a real gate-pass alert test using `startWorker()` confirming a `NotificationLog` lands.
  - **Honest verification gap, same as every Docker-down unit this session (52–60)**: none of the new tests were actually executed — Docker was down the entire session. Verified via `tsc --noEmit` only, clean across every touched package. Now nine consecutive units in this state.

- **Unit 61 — Health, Discipline & Others (D6, fourth fully On-Demand module). ✅ Implemented, at the user's explicit request** ("keep going with unit 60 and 61", same confirmed exception used for Units 57–60). No real school demand exists; recorded here for visibility. The one Open Question (merit/demerit scoring) resolved as **a plain log** — `DisciplineIncident.points`, no automatic downstream effect on house points (Unit 43) or report-card remarks, not wired into either until confirmed.
  - **Schema** (new `packages/db/prisma/schema/wellbeing.prisma`, 6 models): `HealthRecord` (one per student, `@@unique([studentId])`), `DisciplineIncident` (`type: MERIT|DEMERIT`, `points`), `Award`, `CanteenWallet` (one per student, mirrors `SmsWallet`'s shape) + `CanteenTxn`, `LostFoundEntry` (`status: UNCLAIMED|CLAIMED`). All RLS-scoped. Two migrations (schema + RLS), hand-written (Docker still down).
  - **New `wellbeing.manage` permission** (OWNER/PRINCIPAL/ADMIN, `packages/types`, `context/rbac.md`) covering all six sub-features — one lump permission, same granularity as the other on-demand units this session, not split per sub-feature.
  - **New `apps/api/src/modules/wellbeing/`** module: `upsertHealthRecord` (find-or-update, one row per student), discipline-incident create/list, award create/list, canteen-wallet credit/debit/get (find-or-create wallet on first transaction), lost-found create/claim/list.
  - **Canteen wallet never goes negative** (scope #4) — the same guard pattern as the platform's `SmsWallet` decrement checks (`transport-geofence-alert.ts`'s SMS-cost check is the precedent): a debit is rejected outright if it would overdraw, inside the same transaction as the balance check.
  - **Scope #5 (biometric/RFID device hub) — no new endpoint built.** The spec's own instruction is that this reuses Unit 44's existing generic device-scan endpoint; there was nothing additional to add.
  - **Web UI**: new `/wellbeing` page (OWNER/PRINCIPAL/ADMIN-gated) — Health/Discipline/Awards (per-student lookup), Canteen Wallet (credit/debit/balance), and Lost & Found tabs.
  - **Tests** (`apps/api/test/wellbeing.test.ts`, new file): health-record upsert-not-duplicate, discipline/award create+list, lost-found create+claim, RBAC-denial; a canteen-wallet test asserting correct credit/debit balances and a rejected overdraw attempt with the balance left unaffected.
  - **Honest verification gap, same as every Docker-down unit this session (52–61)**: none of the new tests were actually executed — Docker was down the entire session. Verified via `tsc --noEmit` only, clean across every touched package (`apps/api`, `apps/worker`, `apps/web-app`, `packages/db`, `packages/types`, `packages/validation`). This is now **ten** consecutive units built and typechecked but never run against a live database. This gap is no longer a minor caveat — it is the single largest source of unverified risk in the codebase at this point, and the next session should treat a Docker-up verification pass (both migrations against `vidyut`/`vidyut_test`, then the full suite) as blocking before any further feature work.

- **Unit 62 — Accounting & Finance (B2, export-first path). ✅ Implemented.** Unlike every other On-Demand unit this session, this one's Open Question wasn't just "build now vs. wait" — it was a genuine build-vs-buy scope decision the spec explicitly refused to default. **Asked the user directly** (a real general ledger vs. a Tally/Zoho CSV export), recommended export-first with the reasoning spelled out (a native ledger risks becoming an unaudited system of record for something with real legal/audit weight), and **the user confirmed export-first.** This is recorded as a founder call in the spec file itself, not an engineering default.
  - **Schema** (new `packages/db/prisma/schema/accounting.prisma`, 2 models): `ExpenseHead` (a plain category), `Expense` (`headId`, `amount`, `vendorName?`, `date`, `note?`) — a simple log, explicitly not a payables workflow (spec scope #1). No general-ledger models (chart of accounts, cash/bank books, GST fields) — the non-default path was not built. All RLS-scoped. Two migrations (schema + RLS), hand-written (Docker still down).
  - **New `accounting.manage` permission** — OWNER/PRINCIPAL/ADMIN default grants plus **ACCOUNTANT**, the first On-Demand-unit permission this session extended to a non-owner-tier role, since accounting is squarely ACCOUNTANT's job per `context/rbac.md`.
  - **New `apps/api/src/modules/accounting/`** module: expense-head and expense CRUD; `GET /accounting/export/tally?branchId=&from=&to=` merges existing `Payment` (income) rows and logged `Expense` rows for the period into one CSV, reusing Unit 55's `toCsv()` helper and the reports module's `respond()`/`text/csv` pattern.
  - **Export format is plain CSV, not literal Tally XML** — recorded as a deliberate scope decision in the spec: Tally's real XML import format (company/ledger master schema) is a much bigger integration surface than "export-first" implies, and both Tally and Zoho Books accept CSV. **Not verified against a real Tally/Zoho import** — no live account exists in this environment, same honest-stub posture as Unit 40's SMS/WhatsApp providers and Unit 50's e-sign integration. The spec's own Definition-of-Done bar ("a real import succeeds") is explicitly not claimed as met.
  - **GST invoice fields (scope #3) — not built.** Not confirmed that GST applies to this fee category, and the spec's own instruction is "confirm, don't assume" — nothing was added to `Invoice`.
  - **Web UI**: new `/accounting` page (OWNER/PRINCIPAL/ACCOUNTANT-gated) — Expenses (category + entry log) and Export (date-range CSV download) tabs.
  - **Tests** (`apps/api/test/accounting.test.ts`, new file): expense-head/expense CRUD + RBAC-denial; an export test asserting a real payment lands as an `INCOME` row and a logged expense lands as an `EXPENSE` row in the same CSV.
  - **Honest verification gap, same as every Docker-down unit this session (52–62)**: none of the new tests were actually executed — Docker was down the entire session. Verified via `tsc --noEmit` only, clean across every touched package. This is now **eleven** consecutive units built and typechecked but never run against a live database.

- **Unit 63 — Payroll & Salary (B3, export-first path). ✅ Implemented.** Same class of decision as Unit 62 — asked the user directly (native PF/ESI/TDS/PT calculator vs. salary-structure data + a Paybooks-format export), recommended export-first (a compliance-risk, not just a timing, call — statutory calculations carry real penalties if wrong), and **the user confirmed export-first.**
  - **Schema** (new `packages/db/prisma/schema/payroll.prisma`, 1 model): `SalaryStructure` (`staffId` unique, `basic`, `hra`, `allowances`/`deductions` as `Json`) — the inputs, not the statutory output (spec scope #1). **Added `LeaveType.UNPAID`** to Unit 09's existing `LeaveRequest` enum — required by scope #2 ("unpaid-leave days reduce gross"), no such type existed; a small additive enum change, not a new model. One migration + RLS migration, hand-written (Docker still down).
  - **New `payroll.manage` permission** — OWNER/PRINCIPAL/ADMIN plus **ACCOUNTANT** (same reasoning as Unit 62's `accounting.manage`).
  - **New `apps/api/src/modules/payroll/`** module: salary-structure upsert/list; `GET /payroll/export?branchId=&month=&year=` computes gross pay per staff (`basic+hra+allowances-deductions`, minus a flat `(basic+hra)/30`-per-day reduction for `UNPAID`/`APPROVED` leave overlapping the month) as CSV. No native PF/ESI/TDS/PT — Open Question 1's non-default path, not built.
  - **Web UI**: new `/payroll` page (OWNER/PRINCIPAL/ACCOUNTANT-gated) — Salary Structures and Export tabs.
  - **Tests** (`apps/api/test/payroll.test.ts`, new file): salary-structure upsert-not-duplicate + RBAC-denial; an export test asserting real `LeaveRequest` data correctly reduces the computed gross pay.
  - **Honest verification gap (52–63)**: none of the new tests were actually executed — Docker was down the entire session. Verified via `tsc --noEmit` only. Now **twelve** consecutive units in this state.

- **Unit 64 — Inventory, Assets & Procurement (B4, includes the parent-facing store). ✅ Implemented.** Open Question 1 (internal-only inventory vs. a parent-facing store — meaningfully different scope sizes) was put to the user directly; **the user confirmed the parent store is in scope.**
  - **Schema** (new `packages/db/prisma/schema/inventory.prisma`, 9 models): `Store`/`InventoryItem` (running `quantity`, kept in sync by movements, not recomputed from history), `StockMovement` (`IN`/`OUT`), `PurchaseOrder`/`Grn`/`GrnLine` (a simple two-step procurement flow, no vendor RFQ — scope #2), `Asset` (a register, not a depreciation-accounting engine — scope #3, explicitly Unit 62's territory if ever built natively), `StoreItem`/`StoreOrder` (the confirmed parent-facing store, scope #4). All RLS-scoped. Two migrations (schema + RLS), hand-written (Docker still down).
  - **New `inventory.manage` permission** (OWNER/PRINCIPAL/ADMIN/ACCOUNTANT).
  - **New `apps/api/src/modules/inventory/`** module: store/item CRUD, stock movements (rejects an `OUT` that would go negative), purchase-order create + GRN receipt (creates real `StockMovement(IN)` rows and closes the order), asset register CRUD, and the parent store: `POST /inventory/store-items` (lists an `InventoryItem` for sale) and `POST /inventory/store-orders` (scope #4 — reuses Unit 11/12's fee engine exactly like Unit 58's library fine and Unit 59's hostel fee: finds-or-creates a `MISC` `FeeHead`, creates a one-off `Invoice`/`InvoiceItem`, and debits stock via a real `StockMovement(OUT)`, all in one transaction).
  - **Low-stock alerts** (scope #5) — same nightly-cron-scan shape as Unit 14/57's own pattern: new `inventory.lowStockScan`/`inventory.lowStockAlert` jobs, `apps/worker/src/processors/inventory-lowstock-{scan,alert}.ts`, registered as a third nightly repeatable job in `apps/worker/src/index.ts`. IN_APP-only alert to OWNERs (internal ops alert, not parent-facing).
  - **Web UI**: new `/inventory` page (OWNER/PRINCIPAL/ADMIN-gated) — Stores & Stock, Purchase Orders, Assets, and Parent Store tabs.
  - **Tests** (`apps/api/test/inventory.test.ts`, new file): store/item CRUD + stock-movement over-draw rejection + RBAC-denial; a GRN-receipt test asserting real stock increment and PO closure; a parent-store-order test asserting a real `Invoice`/`InvoiceItem` (MISC FeeHead) and a real stock decrement.
  - **Honest verification gap (52–64)**: none of the new tests were actually executed — Docker was down the entire session. Verified via `tsc --noEmit` only. Now **thirteen** consecutive units in this state.

- **Unit 65 — Misc Engagement & Productivity Tools (H-bis remainder, the final unit of this batch). ✅ Implemented**, adopting the spec's own recommendation as-is: only task assignment and polls, everything else in this unit explicitly stays deferred (discussion forums, a school blog/CMS, a form builder, a question-paper generator, video conferencing — all recorded as genuinely speculative, not silently dropped).
  - **Schema** (new `packages/db/prisma/schema/productivity.prisma`, 1 model): `StaffTask` (`assignedToId`, `title`, `dueDate?`, `status: OPEN|DONE`). **Polls reuse Unit 49's existing `Survey` model** — added an `isPoll Boolean` flag (`engagement.prisma`) rather than a parallel model, per the spec's own scope #2 instruction; no new poll-specific endpoints, the existing `/surveys` create/respond/results routes just accept and return the flag now. One migration (StaffTask table + the `Survey.isPoll` column) + RLS migration for `StaffTask` only (Survey already has RLS from Unit 49).
  - **New `task.manage` permission** (OWNER/PRINCIPAL/ADMIN) for staff-task assignment. Polls reuse the existing `engagement.manage` permission — no new permission needed.
  - **New `apps/api/src/modules/productivity/`** module: task create/list/complete, branch-scoped.
  - **Web UI**: new `/tasks` page (OWNER/PRINCIPAL/ADMIN-gated) — assign and mark-done. No separate polls UI — polls are created through the existing Engagement page's survey form with the poll flag (not built as a distinct screen this pass, since the underlying model and endpoints are already fully shared).
  - **Tests** (`apps/api/test/productivity.test.ts`, new file): task assign/complete/list + RBAC-denial; a poll test confirming `isPoll: true` round-trips through the existing `/surveys` endpoint.
  - **Honest verification gap (52–65) — closing note for this batch**: none of the new tests across Units 52–65 (fourteen consecutive units) were actually executed against a live database — Docker was down for the entire session. Every unit was verified via `tsc --noEmit` only, always clean. This is the single largest accumulated risk in the codebase right now: the hand-written migration SQL, every Prisma upsert composite-key path, every RLS policy, and every cron-job round-trip is unproven against real Postgres/Redis. **Before any further feature work, the highest-value next step is a Docker-up verification pass**: run both migrations for all fourteen units against `vidyut`/`vidyut_test`, then the full test suite, and fix whatever a live database catches that a type-checker structurally cannot.

- **Unit 66 — Student Lifecycle Depth (closes A1's remaining rows). ✅ Implemented.** Extends the existing Unit 07 `apps/api/src/modules/students/` module rather than a new module.
  - **Schema** (`packages/db/prisma/schema/students.prisma`): new `StudentTimelineType` enum (`DISCIPLINE`/`ACHIEVEMENT`/`NOTE`); `Student.siblingGroupId` + new `SiblingGroup` model (tenant-owned, no `branchId` — a group can span branches) and `StudentTimelineEntry` model (branch-owned). One schema migration + one RLS migration, hand-written (Docker still down).
  - **Open Question 1 (sibling linking)**: built as the recommended plain tag — `linkSiblings` reuses an existing group if one of the given students already has one, else creates a new one; no auto fee-discount.
  - **Open Question 2 (cross-branch transfer)**: `Enrollment` has `@@unique([studentId, sessionId])`, which rules out the spec's literal "close and open a new Enrollment" wording — `transferStudent` instead updates the current session's `Enrollment` row in place (`branchId`/`classId`/`sectionId`) and moves `Student.branchId`, the same shape as a promotion. History stays put: old attendance/marks/fee records already carry their own `branchId` at creation time and are never rewritten — verified with a fixture `AttendanceRecord` created before the transfer and asserted unchanged after (Unit 33's verification style).
  - **Open Question 3 (re-admission)**: `readmitStudent` is a standalone flow, distinct from Unit 33's rollover-time REPEAT — upserts on the same `studentId_sessionId` key (reactivates a `LEFT` current-session Enrollment if one exists, else creates a fresh one), then flips `Student.status` back to `ACTIVE`.
  - Also added: `markAlumni` (`Student.status = ALUMNI`) + `GET /students/alumni?branchId=` filtered list (no separate alumni-portal login, per spec); `StudentTimelineEntry` create/list (append-only log).
  - No new permissions needed — reuses `student.view`/`student.edit`.
  - **Web UI**: extends the existing `/students/[id]` detail page (Unit 07) with a "Lifecycle" card — transfer (branch/class/section pickers), mark-alumni/readmit toggle based on current status, sibling linking, and timeline log — rather than a new route.
  - **Tests** (`apps/api/test/student-lifecycle.test.ts`, new file): sibling linking + listing; cross-branch transfer with a real pre-transfer `AttendanceRecord` provably untouched; alumni transition + alumni-list filter + readmission; timeline create/list.
  - **Honest verification gap**: Docker was unreachable again this unit — verified via `tsc --noEmit` only across `apps/api` and `apps/web-app`, not against a live Postgres. Now **fifteen** consecutive units (52–66) in this state; the Docker-up verification pass flagged after Unit 65 remains the highest-value next step before further feature work.

- **Unit 67 — Lesson Planning, Curriculum & LMS (A10, full On-Demand module — first re-audit gap that never had a spec written until this unit). ✅ Implemented.** Both Open Questions were already resolved by the spec's own recommendation, so no fresh user question was needed.
  - **Schema** (new `packages/db/prisma/schema/lms.prisma`, 4 models): `SyllabusChapter` (a plain checklist, `order`+`completedAt`), `LessonPlan` (`staffId`/`subjectId`/`sectionId`/`date`/`topic`/`notes`, no approval-gate field — the spec's own default), `ContentItem` (`type: FILE|LINK`, reuses Unit 04's S3 wrapper for `FILE`, a plain `linkUrl` for `LINK` — Open Question 1, no video hosting), `LiveClassLink` (`joinUrl` pasted in from an externally-created meeting — Open Question 2, link-out only, no SDK integration). One schema migration + one RLS migration, hand-written (Docker still down).
  - **New `lms.manage` permission**, granted to OWNER/PRINCIPAL/ADMIN/TEACHER.
  - **New `apps/api/src/modules/lms/`** module: syllabus chapter create/list/complete, lesson plan create/list (staffId resolved from the authenticated user via the existing `getStaffByUserId` helper, not passed in the body), content item create/list (Zod-level refine rejects a `FILE` item with no `fileUrl` or a `LINK` item with no `linkUrl`), live class link create/list. All list endpoints resolve the parent Class/Section first and branch-check it, matching Unit 45's homework-listing pattern (not a raw unchecked filter).
  - **Web UI**: new `/lms` page — four tabs (Syllabus, Lesson Plans, Content Library, Live Classes), reusing the existing class/section Select pickers and the free-text subjectId input convention already used by the Homework/Timetable pages (no dedicated subjects-list endpoint exists yet).
  - **Tests** (`apps/api/test/lms.test.ts`, new file): syllabus chapter create/complete/list; a teacher creating their own lesson plan (staffId resolved, not spoofable); content-item type/URL validation (LINK ok, FILE-without-fileUrl rejected); live-class scheduling; RBAC-denial.
  - **Honest verification gap**: Docker was unreachable again this unit — verified via `tsc --noEmit` only across `apps/api`, `apps/web-app`, `packages/validation`, and `packages/types`, not against a live Postgres. Now **sixteen** consecutive units (52–67) in this state; the Docker-up verification pass remains the highest-value next step before further feature work.

- **Unit 68 — Communication Extras (Contact Fields, Preferences, Newsletter, Birthday Automation). ✅ Implemented.** Open Question 2 (birthday-automation default) was put to the user directly via `AskUserQuestion` before building that piece; **the user confirmed on-by-default (opt-out)**, not assumed.
  - **Schema**: `Guardian.alternatePhone`/`Guardian.whatsappOptIn` added to the existing model (`guardians.prisma`); new `packages/db/prisma/schema/comm-extras.prisma` — `CommunicationPreference` (`userId`/`channel`/`optedIn`, `@@unique([userId, channel])`, absence of a row means opted in) and `Newsletter` (`title`/`body`/`sentAt?`, branch-owned). One schema migration + one RLS migration (RLS on the two new tables only — `Guardian`'s new columns need no new policy), hand-written (Docker still down).
  - **Real send-time gate (scope #2)**: `apps/worker/src/comm-preference.ts`'s `isOptedIn()` helper is checked in `announcement-fanout.ts`'s PUSH loop before every send — the same shared fan-out mechanism Newsletter also reuses, so one gate covers both. `AnnouncementFanoutPayload` gained an optional `templateKey` (defaults to `"announcement.published"`).
  - **Newsletter (scope #3)**: `apps/api/src/modules/newsletters/` — `createAndSendNewsletter` creates a real `Announcement` row (audience = every class in the branch) and enqueues the existing `announcement.fanout` job unchanged with `templateKey: "newsletter.sent"`, exactly Unit 56's own "reuse the mechanism, not a parallel pipeline" pattern. No new permission — reuses `announcement.send`.
  - **Birthday automation (scope #4)**: new `comm.birthdayScan` job (Unit 14/57/64's nightly-scan shape), `apps/worker/src/processors/comm-birthday-scan.ts`, registered at 07:00 daily in `apps/worker/src/index.ts`. **Student birthdays only** — `Staff` has no `dob` field in the existing schema (a real spec-vs-schema gap, same category as Unit 66's `Enrollment` constraint, flagged not silently dropped). PUSH-then-SMS-fallback to guardians, gated by the same `isOptedIn()` helper, confirmed on-by-default.
  - **`/me/communication-preferences`** (GET list, PUT upsert) added to the existing Unit 24 self-scope `/me` module — no new top-level permission, self-service like the other `/me` endpoints.
  - **Web UI**: Guardians page extended with alternate-phone/WhatsApp-opt-in fields; new `/newsletters` page (title/body compose + sent-log table). No dedicated preference-center UI — the toggle is primarily parent/guardian-facing (mobile-app territory) and the spec itself scopes out anything beyond a simple toggle list; the `/me` endpoints are fully functional without a bespoke admin screen.
  - **Tests** (`apps/api/test/comm-extras.test.ts`, new file): guardian alternate-contact round-trip; opt-out correctly suppresses a PUSH send (real worker + real queue, not a mock, matching Unit 40's own integration-test style); newsletter reaches guardians via a real fan-out with the distinct template key; birthday scan sends to a guardian whose child's dob is today.
  - **Honest verification gap**: Docker was unreachable again this unit — verified via `tsc --noEmit` across all five touched packages (clean), and the new test file was run against `vitest`, confirming it's structurally sound (fails only on the expected `Can't reach database server at localhost:5432`, the same wall every unit since 52 has hit). Now **seventeen** consecutive units (52–68) in this state; the Docker-up verification pass remains the single highest-value next step before further feature work.

- **Unit 69 — Platform Extras (the catch-all closing this entire re-audit batch, Units 36–69). ✅ Implemented.** Open Question 1 (password policy) was put to the user directly via `AskUserQuestion`; **the user confirmed length-only, no complexity, no rotation** — the existing Unit 03 `.min(8)` already implements it, no code change. Open Questions 2 (SSO) and 3 (webhooks) stay explicitly deferred, per the spec's own gated-integration and no-partner-request rules.
  - **Schema**: `User.hasSeenTour` (onboarding-tour flag, identity.prisma); `Tenant.primaryColor`/`customDomain` (branding, alongside the existing `logoUrl`); `SupportTicket.type: SUPPORT|FEEDBACK` (feedback reuses the existing model with a tag, per the spec's own suggestion, not a parallel one). One migration, no new tables so no new RLS migration needed.
  - **Onboarding tour (scope #4)**: `GET`/`PATCH /me/tour-seen` + a new `OnboardingTour` component (`components/onboarding-tour.tsx`) — a 3-step hardcoded dismissible dialog shown once on first dashboard load, no CMS content-management behind it (the spec's own scope note).
  - **Feedback (scope #5)**: new `POST /feedback` (any authenticated user, unlike `/support-tickets` which stays gated behind `settings.manage`) — reuses `SupportTicket` with `type: FEEDBACK`, `category` folding into `subject`. Surfaces in the existing super-admin ticket-listing screen unchanged (feedback rows are just tickets with a different type). Web UI: a "Feedback" tab on the existing Settings page, mirroring the existing Support tab.
  - **Teacher/accountant dashboard slices (scope #6)**: `GET /dashboard/teacher-summary` (assigned section count via `TeacherAssignment`, today's attendance-marked %, this month's homework posted — all self-scoped via the caller's own `Staff` row) and `GET /dashboard/accountant-summary` (today's real `Payment` collection, `receivedById`-scoped). Backend only — the natural home for these is the existing mobile Teacher/Parent app screens, not a new web-admin page.
  - **White-label branding admin (scope #7, Open Question 4)**: `PATCH /platform/tenants/:id/branding` (audited like every other platform mutation) + a new Branding card on the existing super-admin tenant-detail page (`/super-admin/tenants/[id]`) — logo URL, primary color (hex-validated), custom domain.
  - **Security audit (scope #3)**: a review pass, not new code — `context/security-audit-unit69.md` reviews JWT expiry (15min access / 30d refresh, Unit 03), refresh-token rotation-on-use (Unit 03), and rate-limit thresholds (300 req/min/IP, Unit 04). No gaps found; every reviewed area was already correctly built.
  - **Shared template library (scope #8)**: **not built** — needs real curated report-card/certificate content to seed from, which doesn't exist yet; a content-authoring gap, not a code gap. Flagged explicitly rather than shipping an empty seed endpoint.
  - **Tests** (`apps/api/test/platform-extras.test.ts`, new file): tour-seen default-unseen read; a TEACHER with no `settings.manage` still submitting feedback (real RBAC-bypass-by-design check); teacher summary's assigned-section-count/homework-count against a real `TeacherAssignment`+`Homework` fixture; accountant summary's zero-state; branding patch + its audit-log entry.
  - **Honest verification gap — closing note for the full 52–69 batch**: Docker was unreachable this entire session — verified via `tsc --noEmit` only, across all five touched packages, clean. The new test file structurally validated via `vitest` (fails only on the expected DB-connection wall). This closes the fifth consecutive milestone-11 batch (Units 52–69, **eighteen** units) never verified against a live Postgres/Redis. **The Docker-up verification pass remains the single highest-value next step before any further feature work** — run every hand-written migration in this batch against a real `vidyut`/`vidyut_test` database, then the full test suite, and fix whatever a live database catches that a type-checker structurally cannot.

## Open Questions

- **Resolved:** AWS end-to-end — ECS/Fargate + ALB + RDS Postgres + ElastiCache Redis + S3 + SES, ap-south-1 (Mumbai). (See `prerequisites.md`.)
- **Open:** SMS + WhatsApp provider (OTP + notifications, DLT) — MSG91 vs Gupshup. `apps/api`'s OTP/2FA send calls are isolated behind `sendOtpSms`/`sendTwoFaCode`, and background delivery now has a real jobs interface (`enqueue`) to run through — wiring the real provider in as a job is a small, contained change whenever this is decided.
- **Resolved:** brand name = **Vidyut** (indigo/cyan; see `brand.md`); ⏳ trademark + domain check pending.
- **Resolved (founder decision, 2026-07-25):** `tenantSlug` on `/auth/otp/request` and `/auth/login` is an internal placeholder, not permanent. A separate short, human-friendly `schoolCode` (distinct from the URL `slug`) will resolve to a tenant for the mobile shared-app login (architecture-context.md Part 2 §3A) and replace `tenantSlug` in these endpoints — that lookup is built whenever Unit 24 (mobile shell) or an API-gateway concern introduces it; no action needed before then, `tenantSlug` keeps working as-is until it's swapped in.
- **Open:** full `docker-compose up --build` for `api`+`worker` needs verification on a real network — untested end-to-end in this sandboxed session (see Unit 04 note above). Low risk (Dockerfiles are straightforward, got most of the way through in both attempts) but not yet proven.

## Architecture Decisions (log as made)

- Server-based on **AWS** (ECS/Fargate + ALB + RDS + ElastiCache + S3 + SES, ap-south-1); modular monolith.
- Multi-tenant shared-schema + Postgres RLS via `withTenant()`; **multi-branch (school groups) in v1**.
- Express (Node server) API · Next.js web (2 apps) · one role-based RN/Expo app · Redis + BullMQ persistent worker.
- **CBSE first**; plans/pricing per market research as-is; **Razorpay** + **FCM** (SMS/WhatsApp provider TBD); AI-agents build / founder reviews.
- Full decision + setup tracker: `prerequisites.md`.
- Build complete CORE before selling; on-demand modules after.
- Single role-based mobile app; dual delivery (shared themed + dedicated branded), plan-gated.
- Prisma schema/client/`withTenant()` live in `packages/db` (not `apps/api`), since Unit 02 predates the Express skeleton (Unit 04); `apps/api` will depend on `@vidyut/db` rather than duplicating it.
- Local Postgres runs as a non-superuser app role (`vidyut_app`), not the Docker image's bootstrap `POSTGRES_USER` — the bootstrap user is a true superuser and bypasses RLS/FORCE RLS unconditionally, which would make local RLS tests a false green. This mirrors real RDS master-user behavior.
- `apps/api` was started in Unit 03, not Unit 04, because Unit 03's own spec requires real, testable HTTP endpoints. It deliberately stops at `createApp()` — no `.listen()`, no rate-limit/OpenAPI/X-Request-Id — leaving those to Unit 04.
- The pipeline-wide rate limiter runs before auth (per `api-conventions.md`'s stated order), so it's necessarily IP-keyed, not per-tenant — a per-tenant limiter would need to run after tenant-context and hasn't been added; revisit if abuse patterns need it.
- OpenAPI is generated directly from the same Zod schemas used for request validation (`@asteasolutions/zod-to-openapi`), not hand-written separately — one source of truth, can't drift.
- `packages/api-client`'s generated `openapi.json`/`schema.ts` are committed, not gitignored, so a fresh clone typechecks immediately without a codegen step.
- Permissions are checked live from the DB on every `requirePermission()` call, never cached into the JWT — an owner's role-permission edit takes effect immediately rather than waiting out the access token's lifetime.
- Refresh tokens are hashed with SHA-256, not argon2 — argon2 is for low-entropy secrets (passwords); a refresh token is already a high-entropy random JWT, so a fast hash is the correct, standard choice and avoids unnecessary latency on every refresh.
- `RolePermission`, `UserRole`, and `BranchMembership` all carry a denormalized `tenantId` beyond `data-model.md` §3's shorthand, so RLS can be enforced directly on each row rather than only transitively through a `Role`/`User` join — required by §1's own "every tenant-owned table carries tenantId" convention.
- Added a `RefreshToken` model beyond the §3 sketch — an unambiguous, direct implication of `api-conventions.md`'s already-explicit "refresh token stored hashed, revocable" requirement, not a new invented feature.
- `/auth/otp/request` and `/auth/login` require a `tenantSlug` field not shown in `api-conventions.md`'s shorthand — phone/email are only unique per tenant, not globally, and `User` is RLS-scoped, so tenant must be resolved before the user lookup. A future hostname/school-code resolution layer can populate this field instead of the client.
- Platform tables (`Plan`/`Subscription`/`ModuleToggle`/`SmsWallet`/`AppBuild`) carry no RLS, per `data-model.md` §13's own "NOT tenant-scoped" label — they're config the platform manages and internal guards read, not a tenant-facing CRUD resource. `AuditLog` (§12) is the exception and stays fully RLS'd like every other domain table.
- Super-admin identity is a separate `PlatformUser` table with its own JWT secret/type/guard, never a nullable `tenantId` on `User` — keeps "every tenant-owned table has a real tenantId" a structural fact, not a convention someone can violate by mistake.
- `packages/db` now owns cross-cutting seeding logic (`seedDefaultRoles`, `seedModuleToggles`), not just `apps/db/prisma/seed.ts` — both the demo seed and real tenant provisioning call the same functions, so they can't silently drift apart the way two independent implementations would.
- Adopted `@tanstack/react-query` in `apps/web-app` starting with the super-admin screens (Unit 05's first client-side data fetching) — already the stated convention in `code-standards.md`, and confirmed necessary in practice: the newer `react-hooks/set-state-in-effect` ESLint rule fails plain `useEffect`+`setState` data-fetching outright.

## Session Notes

- Context scaffold adapted from a spec-driven template; all content now reflects School ERP.
- All build docs live in `context/` (architecture, feature-catalog, build-approach, standards, plan); market & GTM research in `docs/market-research/`.
